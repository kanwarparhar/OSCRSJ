# OSCRSJ Website — Claude Code Context

> Live at oscrsj.com · pre-launch as of May 2026.

## Git Safety — READ BEFORE ANY COMMIT OR PUSH

**Background.** On 2026-04-22 a routine favicon-push session accidentally deleted 195 files from `origin/main`. The broken commits (`c46d818` → tree of 2 files, `57c236b` → tree of 4 files) were pushed to GitHub before anyone noticed. The live site kept serving only because Vercel's build of the broken tree failed and it stayed on the prior good deploy. Recovery shipped 2026-04-23 in commits `1f29795` (file restore by a parallel push) + `5150550` (the transparent-favicon change layered on top). Root cause: a `git add -A && commit && push` ran against a corrupted index that thought most project files didn't exist.

**Sanity checks before every commit:**

1. `git ls-tree -r HEAD --name-only | wc -l` — healthy OSCRSJ tree should be ~196-200 files. If HEAD's tree is dramatically smaller than the working tree, **STOP**. The index or HEAD is corrupted; do not commit.
2. `git diff --cached --stat | tail -1` — most feature commits touch 1-15 files. A staged set of 100+ files, especially mostly `new file:` entries, is a **red flag**, not a normal commit. Investigate before pushing.
3. **Never `git add -A` or `git add .` blindly.** Stage by explicit path: `git add app/foo.tsx public/bar.png`. Bulk-staging against a corrupted index is exactly what caused the 2026-04-22 incident.
4. **Never `git reset --hard` or `git push --force` (incl. `--force-with-lease`) without explicit Kanwar approval.** Recovery on 2026-04-23 was only possible because the working tree on disk still had all 197 files; a `git reset --hard` after the destruction would have made recovery require a fresh clone.
5. **If local `main` and `origin/main` diverge in ways you don't fully understand, FETCH and inspect** — `git log HEAD..origin/main` and `git ls-tree -r <SHA> --name-only | wc -l` for each side. Never merge or push until you've explained the divergence in your own words to Kanwar.
6. **Separate concerns across sessions.** If `git status` shows pre-staged files you did not touch in the current session (previous session's agent left work in the index), STOP. Check CLAUDE.md against recent commit history — if a session described as "shipped" has files still sitting staged and never committed, that commit never actually landed. Verify the migration/schema dependency of the stale work has been run in Supabase before bundling it into your commit. If in doubt, `git reset HEAD -- <those paths>` to unstage, commit only the current session's work by explicit path, and flag the leftover work to Kanwar for its owner-agent to resume. Never bundle a different session's feature work into your style/fix commit.

**Recovery cheat-sheet:** the pre-destruction commit is usually findable in `git reflog`. Compare tree sizes with `git ls-tree -r <SHA> --name-only | wc -l` to identify the last healthy commit. Restore by adding back the missing files in a single fast-forward commit titled `fix(repo): restore N source files erroneously deleted by <broken-SHA>`. **Never force-push.** If the FUSE mount between Kanwar's Mac and the Cowork sandbox blocks local git operations with stale lock files (`.git/index.lock`, `.git/HEAD.lock`, `.git/refs/heads/main.lock` that can't be `rm`'d from inside the sandbox — `mv` works around `unlink` failures), do a fresh `git clone` into `/sessions/.../OSCRSJ-push-workspace/`, apply the fix there, push from that clone. After pushing, Kanwar runs `rm -f .git/*.lock .git/refs/heads/*.lock && git fetch && git reset --hard origin/main` from his Mac Terminal to resync the local working copy. When deleting app routes via bash, leftover `.fuse_hidden*` files in the emptied directory are FUSE bookkeeping and don't affect Next.js routing; they clean up on next filesystem sync.

---

## What This Project Is

The official website for **OSCRSJ** (Orthopedic Surgery Case Reports & Series Journal) — Kanwar's independent, open-access orthopedic research journal targeting the global orthopedic surgery community. Domain: **OSCRSJ.com**. Pre-launch as of May 2026.

---

## Conventions

Operational rules every session must follow. New entries land here only when a new rule is locked (see [docs/session-history.md#operational--meta-git-safety-conventions-refactors](docs/session-history.md) for surfacing context).

- **Explicit-path stage; never `add -A` or `add .`.** Stage every file by path. The 2026-04-22 file-deletion incident was caused by `add -A` against a corrupted index — see Git Safety above. Surfaced Session 14 (2026-04-22 incident).
- **TypeScript clean before commit.** Run `npx tsc --noEmit -p tsconfig.json` from repo root; commit only when it returns exit 0. Surfaced Sessions 11+.
- **FUSE stale-lock workaround.** When `git` errors with "Operation not permitted" on `.git/HEAD.lock` / `.git/index.lock` / `.git/refs/heads/*.lock` / `tmp_obj_*`, the file can't be `rm`'d from inside the sandbox but `mv` works around the unlink failure: `mv $f .git/.stale-junk/$(basename $f).$(date +%s%N)`. Non-fatal warning; the operation will complete cleanly. Surfaced Sessions 20+, codified Session 32.
- **PostgREST schema cache after column-add.** Every `ALTER TABLE ... ADD COLUMN` migration that's referenced by application code must end with `NOTIFY pgrst, 'reload schema'`. Without it, application writes hit "Could not find the 'X' column in the schema cache" until PostgREST's ~10-minute auto-refresh fires. Surfaced Session 34 (orphan migration 012).
- **Verify Vercel env-var state via Chrome MCP, not via prior CLAUDE.md claims.** Session 43's wrap-up incorrectly claimed `EMAIL_REPLY_TO` was set on Vercel; Session 46's verification revealed it was never set. Env-var state in CLAUDE.md changelogs should be verified before being trusted as ground truth. Surfaced Session 46.
- **www-canonical rule for outbound URLs.** Every emitted URL — `metadataBase`, `alternates.canonical`, `openGraph.url`, JSON-LD `@graph`, sitemap, robots.txt, server-action fallbacks, email footer hrefs, webhook destinations — must use `https://www.oscrsj.com/...`. Apex `oscrsj.com` 307s to www and most webhooks don't re-issue the body on redirect (Resend Svix-webhook silent-failure root cause, Session 5). Codified Session 6 commit `14d03e3`.
- **Reply-To on transactional emails.** `lib/email/resend.ts` `DEFAULT_REPLY_TO` defaults to `oscrsjournal@gmail.com` (env var `EMAIL_REPLY_TO` overrides). Inquiry-form replies override to the inquirer's email so editorial replies route directly back. Surfaced Sessions 6, 27, 43, 46.
- **DNS-level checks on any send-destination domain.** Run `dig MX <domain>` before assuming a domain accepts inbound mail. The contact-form mailbox-void bug (Session 46) silently swallowed every submission to `info@`/`submit@`/`editorial@`/`waivers@oscrsj.com` for ~9 days because `oscrsj.com` apex has no MX records. Surfaced Session 46.
- **Cross-session orphan-files protocol.** If `git status` shows pre-staged files you did not touch this session, STOP — it's another session's in-flight work. Verify migration dependency, unstage via `git reset HEAD -- <paths>`, commit only your work by explicit path, flag the orphans to Kanwar. Codified by §7.6 in [Organization Plan](../Documents/Kanwar%27s%20Second%20Brain/_Meta/Organization%20Plan.md). Surfaced Sessions 28, 32, 33, 44, 45.
- **Migration-slot arithmetic.** Before claiming a migration slot, run `ls supabase/migrations/` AND `git log --diff-filter=A -- supabase/migrations/`. Orphan migrations on disk that were never committed (Session 15's spec for migration 012 sat orphaned until Session 34 committed it as `5bea320`) cause "claim slot" collisions. Surfaced Sessions 28, 29, 34.
- **Audience copy is locked.** "The global orthopedic surgery community — practicing surgeons, fellows, residents, students, and researchers." Replaces all prior trainee-only framing. Surfaced Session 25, codified across `/about`, `/aims-scope`, `/faq`, `/submit`, homepage, layout metadata, `/media` boilerplate.
- **Ortho/Orth-marker credential strip.** Post-MBBS specialty markers in editorial board honorific suffixes are stripped — `MS (Orth)`, `MS(Ortho)`, `DNB (Orth)`, etc. all collapse to bare `MS` / `DNB`. Applies to both the `name` field and `honorificSuffix` in `lib/schema/editorialBoard.ts` `BoardMember` entries. Bio narrative prose may still reference orthopaedic training (degree names, fellowship titles in `BOARD_MEMBER_BIOS.education`/`experience` arrays). Surfaced Session 48 (Sankalp Lal `MS (Orth), DNB (Orth)` → `MS, DNB`; Vikash Raj `MS(Ortho)` → `MS`).
- **Author non-technical preference.** Kanwar is non-technical; prefer self-contained components over abstracted systems; pages must ship complete with real content (no lorem ipsum); Neutral Elegance palette is the brand.
- **CLAUDE.md add/move discipline (post-2026-05-06).** Every wrap-up runs M1-M4 in order: write full narrative to `docs/session-history.md`; update §9 Recent Sessions (full) and demote oldest if >3; prune §10 Recent Sessions (pointers) for 14-day window + 30-entry cap; resolve checked-off items in §11 Open Follow-ups, append new ones. Spec at vault `02 - OSCRSJ/Projects/CLAUDE.md Refactor.md` §9.

---

## Tech Stack

| Item | Value |
|---|---|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript |
| Styling | Tailwind CSS with custom design tokens |
| Fonts | DM Serif Display (display serif) + Inter (body / UI) via Google Fonts |
| Backend / Auth | Supabase (PostgreSQL + Auth + RLS) |
| Email | Resend (transactional) + Svix-signed webhook |
| CAPTCHA | Cloudflare Turnstile (managed mode) |
| ORCID | Public API OAuth 2.0 (profile prefill + verified iD) |
| Deployment | Vercel (free tier) — auto-deploys from `main` |
| PDF render | WeasyPrint 68.1 → verapdf 1.30.1 (PDF/A-1b), separate renderer repo at `~/Documents/oscrsj-renderer/` |

---

## Deployment & Infrastructure

| Item | Details |
|---|---|
| **Live URL** | https://www.oscrsj.com |
| **GitHub Repo** | github.com/kanwarparhar/OSCRSJ (public) |
| **Hosting** | Vercel (free tier) — auto-deploys from `main` (~60s deploy) |
| **Domain Registrar** | GoDaddy — DNS configured |
| **DNS Records** | A: `76.76.21.21` / CNAME: `cname.vercel-dns.com` / SES MX on `send.oscrsj.com` for outbound |
| **SSL** | Auto-provisioned by Vercel (HTTPS) |
| **WWW redirect** | apex `oscrsj.com` → `www.oscrsj.com` (307). Canonical is `www`. Webhook URLs MUST use www form (apex 307 + most services don't re-issue body — Session 5 lesson). |
| **Inbound mail** | `oscrsj.com` apex has **NO MX records** (NXDOMAIN). Every send to `info@`/`submit@`/`editorial@`/`waivers@oscrsj.com` bounces. All form submissions route to `oscrsjournal@gmail.com` until Google Workspace is provisioned. See [docs/session-history.md#inbox-routing-history](docs/session-history.md). |
| **Backup URL** | oscrsj.vercel.app |

**Deploy:** push any commit to `main` → Vercel auto-rebuilds and goes live in ~60s.

---

## Design System

"Neutral Elegance" palette — dark gradient hero, editorial serif typography, warm tones, journal-grade reading ink. Tokens in `tailwind.config.ts` and `app/globals.css`. Current version: **v2.3 — Reading Mode** (rolled out 2026-04-21). Version history at [docs/session-history.md#design-system-history](docs/session-history.md).

| Token | Value | Usage |
|---|---|---|
| `peach` | `#FFDBBB` | CTA buttons on dark backgrounds, accent highlights |
| `peach-dark` | `#F0C49A` | CTA buttons on light/cream backgrounds |
| `taupe` | `#CCBEB1` | Decorative ONLY (borders, dividers). NEVER for text. |
| `tan` | `#997E67` | Hover border on interactive cards. **Banned from text classes** — fails WCAG AA on cream. Use `text-brown` for metadata. |
| `brown` | `#664930` | Accent text on light bg, button text, metadata |
| `brown-dark` | `#3d2a18` | **Headings only** (h1-h6 + `.page-title` + `.section-heading`). Also `.nav-link`. |
| `ink` | `#120D08` | **Primary body/paragraph text** on white and cream surfaces. ~19:1 contrast on cream, ~20:1 on white. Set as HTML body default. |
| `dark` | `#1c0f05` | Hero bg, nav bg, footer bg |
| `dark-card` | `#261609` | Dark card backgrounds |
| `cream` | `#FDFBF8` | Main page background — barely-warm near-white (~99% luminance) |
| `cream-alt` | `#F8F4ED` | Subtle-highlight surface — chips, pills, step-circles, table headers, empty states, hover |
| `white` | `#FFFFFF` | All cards, form inputs, article wells |
| `border` | `rgba(153,126,103,0.18)` | Subtle borders/dividers |

**Text color rule:** Body → `text-ink` (or inherited). Headings → `text-brown-dark` (paired with `font-serif`). Metadata → `text-brown`. Never `text-tan` for text.

**Background color rule:** Full-bleed wrappers → `bg-white`. Subtle highlights (chips/pills/headers/hover) → `bg-cream-alt`. `bg-cream` is near-invisible against white cards; reach for it only when the surface should read "almost the body background."

**Fonts:** Display serif (`font-serif`) — DM Serif Display, Georgia fallback, used for hero + page titles + section headings. UI / body / metadata (`font-sans`) — Inter, used for nav, buttons, body, dashboard chrome.

**Component classes** in `globals.css`: `.btn-primary`, `.btn-primary-light`, `.btn-outline`, `.btn-ghost`, `.card`, `.section-heading`, `.section-label`, `.nav-link`. **Card hover:** interactive cards get `hover:border-tan hover:shadow-sm` (no bg change). Static info cards get no hover.

---

## Business Context

- **No publisher.** Fully independent.
- **APC model:** Manuscripts submitted before **August 1, 2026** publish free; afterwards, single $499 standard rate per accepted manuscript. Discounts case-by-case via the inquiry form on `/apc`. Pricing history at [docs/session-history.md#apc-pricing-history](docs/session-history.md).
- **License:** CC BY 4.0 (reverted 2026-05-04 same-session after Janine surfaced the DOAJ + Plan S + NIH-funded-author indexing trade-off as a structured option-set; prior CC BY-NC-ND 4.0 lock from Session 25 superseded). Resolution at vault `02 - OSCRSJ/Notes/2026-05-04 License Reversion to CC BY 4.0 (Janine).md`.
- **Backend:** Custom LLM-powered submission system (built — see Phase 3.5 sessions). Renderer at `~/Documents/oscrsj-renderer/`.
- **Goal:** PubMed indexing (~2-year path requiring monthly releases).
- **Target audience:** The global orthopedic surgery community — practicing surgeons, fellows, residents, students, and researchers.
- **Revenue projections:** ~$13K Y1 → ~$72K Y2 → ~$170K Y3.

---

## Recent Sessions (full)

The 3 most recent sessions, full narrative. Older sessions surface as 1-line pointers in the next section; full narrative for any session lives in [docs/session-history.md](docs/session-history.md).

### Session 56 — 2026-05-15 — Sushant Cowork — First-publish operational push (env vars + Supabase creds + workspace.ts bucket-name fix) + Pre-Render Metadata Editor project planned (2 handoffs); 0 OSCRSJ code commits, 2 uncommitted renderer-side fixes

Half-and-half session. **First half**: closed Session 54's 4-step setup checklist + surfaced one real renderer bug, getting the publish workflow operational against Kanwar's first accepted manuscript `f8596ce8-2424-4a47-8537-5af9e8a2f5cd`. **Second half**: hit a workflow gap (no admin UI to fix the metadata warnings the renderer surfaces) which Kanwar declined to quick-fix and instead asked Sushant to plan properly — produced a comprehensive plan note + 2 cross-agent handoffs. **Zero OSCRSJ code commits except this M1-M4 doc commit.** Two renderer-side changes both uncommitted (renderer is local-only on Mac per Session 18+ pattern).

**Operational push closed Session 54's 4 follow-ups in order.** (1) Vercel envs `RENDERER_SHARED_SECRET` (`0fcdb493...`) + `NEXT_PUBLIC_RENDERER_URL=http://localhost:3001` set on Production + Preview (initial gotcha: Kanwar saved both as keys without values; caught from screenshots before troubleshooting downstream). (2) `git push origin main` from Mac terminal returned "Everything up-to-date" — commit `a31783d` was already on origin from a prior push. (3) Renderer `.env.local` synced: secret value flipped from Sushant Session 54's placeholder `db70a0230f...` → Kanwar's Vercel value `0fcdb493...` (must match exactly or bearer auth fails); `OSCRSJ_BASE_URL=http://localhost:3000` → `https://www.oscrsj.com` (renderer fetches FROM production OSCRSJ — comment block on lines 11-15 misleadingly conflates "renderer runs locally" with "OSCRSJ also runs locally"; cosmetic flag for follow-up). (4) `npm run dev` → Next.js 16.2.4 Turbopack `Ready in 316ms`; first page-load surfaced "Missing Supabase credentials" runtime error. `.env.local` lines 7-8 (`NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`) were empty placeholder from Session 17. Wired in the project URL (inferred from Supabase dashboard URL: `https://<project-ref>.supabase.co`) + new-format secret key (`sb_secret_…` — value redacted; Supabase's new sb_publishable_/sb_secret_ format that replaces the legacy JWT — verified `lib/supabase/admin.ts` uses `createClient(url, key)` from `@supabase/supabase-js` which accepts both).

**Renderer bucket-name bug surfaced.** Second page-load returned "Manuscript not found / Failed to create signed URL: Object not found." Diagnosis via `Grep STORAGE_BUCKET\s*=` across `lib/renderer/`: `workspace.ts:9` declared `'manuscripts'` (wrong); `storage.ts:37` declared `'submissions'` (right — fixed by Session 47). Both files use the same logical constant but the value drifted because Session 47's "STORAGE_BUCKET flipped" fix only touched `storage.ts`. Bug stayed dormant 9 days because all smoke runs use `/smoke/sample-payload` which doesn't touch Storage; today is the first time the download path fired against a real manuscript. Fixed via `Edit`: `lib/renderer/workspace.ts:9 'manuscripts' → 'submissions'`. **Renderer fix uncommitted** — Kanwar pushes from Mac terminal. Refresh: manuscript loaded successfully (Pandoc 3.9.0, 21 references parsed, three-pane workspace with title "Bilateral acute exertional compartment syndrome of the forearms in a sport climber").

**Three amber payload-synthesizer warnings surfaced** at the top of the cleanup pane (zero red errors): (1) `running_title is 97 chars (max 45). Editor must shorten before render.` (2) `manuscript_affiliations table is empty; synthesizing affiliations from manuscript_authors.affiliation free-text strings.` — informational. (3) `Abstract for Case Report expects 4 labeled sections; found 0 anchor(s). Editor must add labels (Introduction:/Case Presentation:/Discussion:/Conclusion: etc.) to the abstract text before render OR the sanity test will fail. Falling back to unstructured.`

**Workflow gap exposed.** Sushant pointed Kanwar to the OSCRSJ admin manuscript page to fix the running title + abstract. Kanwar's response: "I dont see an 'edit meta data' or similar section. Do you not think that once we should have a text editor page in which im able to make all these changes in once i click on render published PDF? because im assuming there's a high chance we always need to make some form a change?" Sushant verified via grep — the admin page has zero metadata editors (only display panels for title, abstract, authors, files; no `running_title` editor anywhere; no `abstract` editor; no `manuscript_metadata` editor). **Kanwar's directive:** "We will not go with fastest fix and make sure this is proper now. We need to thoroughly plan this. I like option A 'Metadata editor lives in OSCRSJ admin'. Have sushant plan this. Consider everything we should add..."

**Planning artifact.** Comprehensive plan note shipped at `02 - OSCRSJ/Projects/Pre-Render Metadata Editor & PDF Preview.md` (~330 lines, 13 sections). Companion to existing [[Manuscript Pre-Render QA Pipeline]] (Manvir-spec'd Phase 0 in progress). **Architecture**: new `MetadataEditorPanel.tsx` mounted in admin between `RevisionsPanel` and `PublishPipelinePanel`, gated on `status='accepted'`; six form sections (Article Identity / Abstract — article-type-aware labeled-fields editor / Authors with drag-reorder + CRediT chip-picker + ORCID auto-resolve / Declarations / Pre-Render Validation Summary three-tier red-amber-green with jump-to-fix anchors / Preview); new `/api/preview/[id]` endpoint on both repos extending the renderer's existing `runPublishChain({ performPublish: false })` smoke mode with `emitPreviewArtifact: true` flag uploading to `submissions/previews/` with 24h signed URL + 7-day cleanup cron. **Plan also contains**: comprehensive field inventory (every synthesizer-consumed field mapped to DB column + validator), honest scope assessment of Kanwar's body-content questions (spacing/tables/figures/fonts/citations all template-locked, editor surfaces warnings but doesn't expose CSS controls), citations defer-to-migration-021, 8-question Janine compliance ask, 10-question Franklin UX ask, 4-phase implementation plan (~3 Sushant Cowork sessions ~10 hours total post-sign-offs).

**Two cross-agent handoffs pushed** per Org Plan §7.6: **Janine** `^handoff-metadata-editor-compliance-audit-2026-05-15` (target 2026-05-22, blocks Sushant Phase 1.A — needs field-set sign-off + migration 022 column shape + JATS 1.3 missing-fields confirmation, deliverable doc at `02 - OSCRSJ/Indexing/Standards/Pre-Render Editor Compliance Spec.md`); **Franklin** `^handoff-metadata-editor-ux-design-2026-05-15` (target 2026-05-22, blocks Sushant Phase 1.B — wireframes for all 6 editor sections + component class spec + 7 specific visual decisions, deliverable section appended to existing [[Article PDF Design System]] project). Vault State changelog entry filed.

**§6 cross-session orphan-files protocol:** working tree at session start carried 21 untracked artifacts (Logo/, Track A Wave 1 Tracker.xlsx, pdf-design/, social-exports/, sources/, .cowork-push/, .test-uploads/, SR-MA Invited Topics docx packets, oscrsj-title-page-NEW.docx draft, etc.) — none overlapped with this session's edit set, all left untouched per §6. Two intervening commits since Session 54: `7c83197` Franklin portrait swap claiming "Session 55 — Franklin" + `c2b6142` Session 54 wrap-up doc commit. Franklin's portrait commit gets a §10 pointer-only entry (single-file UI fix + 1-line schema flip).

**Risks named:** (1) Renderer `workspace.ts` bucket fix uncommitted on Kanwar's Mac — needs `cd ~/Documents/oscrsj-renderer && git add lib/renderer/workspace.ts && git commit -m 'fix(renderer): workspace.ts STORAGE_BUCKET manuscripts → submissions' && git push origin main`. (2) Renderer `.env.local` carries Supabase credentials + OSCRSJ_BASE_URL flip + secret sync as local-only state (`.env.local` is gitignored, per-machine). (3) Renderer `.env.local` lines 11-15 comment block on `OSCRSJ_BASE_URL` is misleading; cosmetic. (4) First manuscript publish blocked pending Pre-Render Metadata Editor build (~2-3 weeks if Janine + Franklin sign off by 2026-05-22) OR Kanwar accepts Supabase Studio one-time exception. (5) Janine's compliance audit may surface migration 022 column shape changes (e.g., JSONB vs three TEXT columns for patient_consent_variant + statement + acknowledgments) — Sushant Phase 1.A scope adjusts on her sign-off.

**Self-improvement note for next session's owner-agent:** the workspace.ts bucket-name bug is exactly the pattern Convention §3 should codify — "**Constant-fix repo-wide grep.** When fixing a constant in one file (env var, magic string, type alias), ALWAYS grep the whole repo for the same constant name across all definitions before declaring the fix complete. Session 47's `storage.ts` bucket fix didn't grep `workspace.ts:9` and the bug stayed dormant for 9 days until the first real Storage download fired. The 30-second `grep -n 'STORAGE_BUCKET\s*=' lib/renderer/` would have caught it." Worth adding to `# Conventions` section.

**Kanwar follow-ups:** (1) **Decision needed** — quick-fix this manuscript via Supabase Studio (one-time protocol exception) OR wait for Pre-Render Metadata Editor (~2-3 weeks). (2) From Mac terminal: commit + push the renderer's `workspace.ts` fix. (3) Build-queue priority: Pre-Render Metadata Editor vs Migration 021 (structured references) vs JATS DTD path hotfix. Suggested order: this → Migration 021 → DTD hotfix. (4) Visual spot-check the Pre-Render Metadata Editor plan note + Janine + Franklin handoffs before they pick up the work. Full handoffs at [docs/session-history.md#session-56-pre-render-metadata-editor-planned](docs/session-history.md). **Handoffs pushed: Janine, Franklin.**

### Session 54 — 2026-05-15 — Sushant Cowork — Renderer payload synthesizer + manual go-live gate landed; OSCRSJ + renderer companion commits; first manuscript publish unblocked

Two coordinated commits across both repos. **OSCRSJ-side commit `1859d0c` on `main`** (fast-forward push `b52b2d0..1859d0c`, explicit-path stage on 7 files: `.env.example` + `app/dashboard/admin/manuscripts/[id]/page.tsx` + `lib/admin/actions.ts` + 4 new files (`app/api/publish/payload/[id]/route.ts`, `app/dashboard/admin/manuscripts/[id]/PublishPipelineActions.tsx`, `app/dashboard/admin/manuscripts/[id]/PublishPipelinePanel.tsx`, `lib/publish/synthesize.ts`); 1314 insertions; TypeScript clean exit 0; tree 269 → 273). **Renderer-side commit `a31783d` on local `main`** (5 files: `.env.local.example` + `app/render/[id]/CleanupPane.tsx` + `app/render/[id]/PublishProgress.tsx` + `app/render/[id]/page.tsx` + `lib/renderer/storage.ts`; 192 insertions / 10 deletions; TypeScript clean exit 0; not pushed — renderer repo HTTPS push requires Kanwar's Mac terminal credentials per Session 18+ pattern). **Session-numbering off-by-one:** commit messages + in-file comments both read "Session 53" because Franklin's earlier-today Session 53 commit cluster (`a81dd05` + `b52b2d0`) wasn't visible in the conversation's CLAUDE.md system-reminder context until after my commits landed. The git log shows two distinct commit clusters both labeled "Session 53"; this CLAUDE.md entry is the authoritative numbering (Sushant = Session 54). Code comments + commit messages remain as-shipped — rewriting via force-push is banned per Git Safety §4. Closes Manvir's inbound handoff `^handoff-renderer-payload-synthesizer-2026-05-15` (P0 — gates the first manuscript publish; the renderer chain shipped Session 18 + Session 47 fully functional against hand-crafted payloads, but `/api/publish/[id]` returned 501 until this seam landed).

**Why this fired now.** Kanwar accepted the first manuscript 2026-05-15 via admin UI. Without a payload synthesizer, every publish would require hand-crafting a Franklin v1.0 JSON. Manvir specced the work earlier today, filed the handoff, then Kanwar surfaced it directly: "Load sushant, pickup your handoff to make sure we can move forward with our new manuscript acceptance."

**Three structural mismatches between Manvir's brief and the existing renderer interface surfaced upfront via codebase scan + AskUserQuestion before any code was written.** (1) Manvir specced a one-click button from OSCRSJ admin that calls `RENDERER_URL/render` and gets NDJSON back; actual renderer requires a human-in-the-loop HTML-cleanup pane (Pandoc-on-.docx output rendered in a 3-pane workspace where editor manually cleans up Pandoc artifacts before clicking Publish). (2) Manvir's spec locked "render route does NOT auto-flip status to published" as the manual-gate decision; renderer's existing `writePublishStatus` did auto-flip via `status: 'published'` in updatePayload — direct conflict. (3) Payload is structurally richer than the DB: full body content (paragraphs / figures / tables / subsections / dropcaps / captions) is in the payload's `body` field but only metadata is in our DB (body comes from Pandoc + editor cleanup, not synthesis). Kanwar picked Option A ("build the real synthesizer this session" — metadata-only synthesizer + Start-render button + modify renderer's writePublishStatus + OSCRSJ-side go-live/unpublish buttons) over Option B (full Manvir spec, duplicates working code, ~6-8 hours) and Option C (hand-craft payload for this one manuscript, ~45 min). Two pre-coding decisions resolved via AskUserQuestion: (a) **references handling** — Option B chosen ("emit empty refs now; ship migration 021 next session" — references table doesn't exist anywhere in the schema, submission wizard never collected structured references, they live inside the manuscript .docx; PMC indexing blocked until refs land but PDF/A-1b ships fine); (b) **renderer folder access** — granted via `request_cowork_directory` for `~/Documents/oscrsj-renderer/` so I could read the canonical `public/template/v1.0/sample-payload.json` fixture and structurally diff against my synthesizer output.

**OSCRSJ-side scope (the synthesizer + payload endpoint + admin buttons):** (a) **`lib/publish/synthesize.ts`** (681 lines) — `synthesizeRendererPayload(manuscriptId): Promise<SynthesizeResult>` joins `manuscripts` + `manuscript_authors` (ordered by `author_order`) + `manuscript_affiliations` (often empty — table exists but submission wizard doesn't populate it; falls back to synthesizing from `manuscript_authors.affiliation` free-text strings with exact-match de-duplication, positional indices, all authors get `affiliation_refs: [1]` etc.) + `manuscript_metadata` + corresponding-author `users` row into Franklin v1.0 payload shape. **Structured-abstract subheading parser** is regex-anchored on canonical labels (`Introduction|Background|Case Presentation|Methods|Results|Discussion|Conclusion|Conclusions`) — case-insensitive, multiline-anchored, slices text between matches, falls back to unstructured with warning when anchor count < expected article-type cardinality (Case Report=4, Case Series=5, Review Article=4, Surgical Technique=unstructured, Letter/Images=optional). Returns `{ ok, payload, warnings: string[], errors: string[] }` — `ok: false` short-circuits the publish path with the structured errors visible in the cleanup pane banner. License hardcoded to CC BY 4.0 per Session 47 Janine reversion. ISSN placeholder `XXXX-XXXX` until ISSN application approves. DOI placeholder `10.XXXXX/oscrsj.{year}.{padded-elocation}` until Crossref membership lands. Running title truncated to 45 chars with warning per Janine §1.2.2. CoI short-form auto-derived as first sentence of long-form. CRediT contributions joined from `manuscript_authors.contribution` per author. XMP metadata block has 12 fields per Janine §6.5 (dc_title + dc_creator + dc_subject + dc_description + dc_rights + dc_identifier + prism_publicationName + prism_issn + prism_volume + prism_number + prism_doi + xmp_CreatorTool + pdfaid_part + pdfaid_conformance). (b) **`app/api/publish/payload/[id]/route.ts`** (75 lines) — GET endpoint, Bearer-token auth via `RENDERER_SHARED_SECRET` env var (not user-session — renderer is local-only, no browser hits this directly). UUID sanity check on `[id]` before fetching. Returns 200 + `{ ok: true, payload, warnings, errors: [] }` on success, 422 + `{ ok: false, errors, warnings }` on synthesizer failure (missing manuscript_type, empty title, no authors, etc.), 401 on bad bearer, 503 on missing env var. (c) **`lib/admin/actions.ts`** (+199 lines at end) — adds `publishGoLive(manuscriptId)` server action (gates on `requireAdminOnly`, requires `status='accepted'` + `published_pdf_storage_path` non-null, flips status to `'published'`, stamps `published_date` if renderer didn't, writes `audit_logs.action='publish_go_live'` row, revalidates `/articles` + `/articles/in-press` + admin manuscript page); `unpublishManuscript(manuscriptId, reason)` (gates on `requireAdminOnly`, requires `status='published'` + 10-char reason for the editorial-record audit log, flips back to `'accepted'`, writes `audit_logs.action='publish_unpublish'` with reason in details JSONB, revalidates same paths); `getRendererLaunchUrl(manuscriptId)` helper (centralizes the URL construction, falls back to documented dev default `http://localhost:3001` when `NEXT_PUBLIC_RENDERER_URL` unset, strips trailing slashes defensively). All audit-log inserts use the established `(admin.from(...) as any)` jsonb-cast convention per CLAUDE.md §Conventions. (d) **`PublishPipelinePanel.tsx`** (server component, 124 lines) + **`PublishPipelineActions.tsx`** (client component, 220 lines) — mounted between `<RevisionsPanel>` and `<PublishedPdfPanel>` on `/dashboard/admin/manuscripts/[id]`. Three lifecycle states: `accepted` + no artifacts → "Render published PDF →" button opens `${RENDERER_URL}/render/${id}` in a new tab with `noopener,noreferrer` (editor cleans HTML in renderer, clicks Publish there, renderer writes paths back; editor returns to admin page + refreshes); `accepted` + artifacts present → "Re-render in renderer" + "Publish (go live) →" button surfaces (single-step confirmation modal: "Article will appear on /articles and /articles/in-press. Has the author signed off on the rendered PDF proof?"); `published` → "Unpublish (emergency)" button surfaces (extra-friction confirm with 10-char reason textarea + type-the-submission-ID-to-confirm field). Panel only renders when status ∈ `{accepted, published}`. (e) **`.env.example`** — documents `NEXT_PUBLIC_RENDERER_URL=http://localhost:3001` (dev default) + `RENDERER_SHARED_SECRET` (must match renderer's value; generate via `openssl rand -hex 32`).

**Renderer-side scope (3 coordinated changes + 1 bonus cleanup):** (a) **`app/render/[id]/page.tsx`** — added `fetchSynthesizedPayload(manuscriptId)` helper to the page-load parallel-await (now `Promise.all([prepareRenderWorkspace, loadTemplateCss, fetchSynthesizedPayload])`). Calls `${OSCRSJ_BASE_URL}/api/publish/payload/[id]` with `Authorization: Bearer ${RENDERER_SHARED_SECRET}`, `cache: 'no-store'`. Returns `{ payload, warnings, errors, fetchReason }` — `fetchReason` is non-null when the secret env var is unset, the fetch throws, or the endpoint returns non-200 (parsed body errors flow into `errors`). All four forwarded to CleanupPane. (b) **`app/render/[id]/CleanupPane.tsx`** — accepts 4 new props (`payload`, `payloadWarnings`, `payloadErrors`, `payloadFetchReason`). Surfaces a red banner above the 3-pane workspace on fetch failure / synthesis errors (lists errors as bulleted list), amber banner on warnings (lists warnings as bulleted list). Publish button is `disabled={!canPublish}` where `canPublish = payload !== null && payloadErrors.length === 0` — editor literally cannot publish without a valid synthesized payload. Publish body now includes the synthesized payload (was: payload field absent entirely, which made the existing 501 path fire). (c) **`lib/renderer/storage.ts` `writePublishStatus()`** — REMOVED `status: 'published'` from the `updatePayload` object per Manvir's 2026-05-15 manual go-live gate. Renderer now writes `published_pdf_storage_path` + `render_report_storage_path` + `published_date` + `jats_xml_storage_path` (when present) only. Status STAYS at `'accepted'` after render; OSCRSJ's `publishGoLive` flips it separately. Audit-log strings + retry codepath unchanged. (d) **Bonus fix on `app/render/[id]/PublishProgress.tsx`** — added `jats_generate` + `jats_validate` to the `ChainStage` type + `STAGE_LABEL` (with Session 47 attribution) + `STAGE_ORDER`. Session 47 (2026-05-06) added these stages to `runPublishChain` but the UI was never updated; events streamed but were silently dropped. Visible now. Writeback stage label updated from `'Writing manuscripts.status = published'` to `'Writing storage paths to manuscripts row'` to reflect the new behavior. (e) **`.env.local.example`** — documents `OSCRSJ_BASE_URL=http://localhost:3000` + `RENDERER_SHARED_SECRET` (must match main-repo value).

**Verification — structural diff against the canonical fixture (AC1).** Python script run against `public/template/v1.0/sample-payload.json` confirmed the synthesizer's 15 top-level keys (`$schema`, `article`, `issue`, `dates`, `authors`, `affiliations`, `corresponding_author`, `equal_contribution`, `abstract`, `keywords`, `body`, `declarations`, `references`, `suggested_citation_html`, `xmp_metadata`) match the fixture's metadata-level keys (ignoring `_note_*` documentation fields) exactly — empty symmetric difference. Article sub-keys (`article_type_slug`, `doi`, `doi_url`, `id`, `license`, `running_title`, `title`, `type`) all present. Declarations sub-keys (`acknowledgments`, `ai_disclosure`, `coi_short`, `conflicts_of_interest`, `credit_author_contributions`, `data_availability`, `funding`, `irb_ethics`, `patient_consent`) all present. Sanity-test contract from `lib/renderer/sanityTests.ts` schema_required_keys list satisfied 14/14 (running_title length check + ORCID URL regex + affiliation_refs cross-reference + corresponding_author consistency + abstract structure per article-type cardinality are all enforced by the synthesizer's defensive build path).

**§6 cross-session orphan-files protocol:** OSCRSJ working tree at session start carried 21 untracked artifacts (Logo/, Track A Wave 1 Tracker.xlsx, pdf-design/, social-exports/, sources/, .cowork-push/, .test-uploads/, SR-MA Invited Topics docx packets from earlier Manvir sessions, oscrsj-title-page-NEW.docx draft, push-callback-fix.sh, sample IG announcement images, wave3_leftovers_flagged.csv, etc.) — none overlapped with my 7-file edit + write set, all left untouched per §6 protocol. Renderer working tree at session start carried `resources/` untracked (JATS DTD bundle Kanwar fetched locally per Session 47 follow-up) — not mine, left untouched. **FUSE stale-lock event:** OSCRSJ-side `.git/index.lock` was already present at session start (not from this session); cleared via documented `mv .git/X.lock .git/.stale-junk/X.lock.$(date +%s%N)` workaround. Standard `tmp_obj_*` "Operation not permitted" unlink warnings during both commits non-fatal per Conventions.

**Risks named:** (1) Synthesizer not run against a live manuscript in-session — structural diff against fixture is empty, but per-author affiliations synthesized inline from `manuscript_authors.affiliation` strings (table empty) means multi-affiliation authors will collapse to single-affiliation; warn-and-proceed pattern, editor sees the warning in the cleanup pane banner. (2) `references[]` empty until migration 021 + admin paste-in lands; JATS XML will ship without `<ref-list>`; PMC indexing remains blocked. PDF/A-1b ships fine because Pandoc-converted body content carries inline `<sup>` reference citations. (3) Abstract subheading parser is best-effort — warnings on anchor-count mismatch; sanity test in renderer will catch real mismatches at render time and short-circuit the chain before upload. (4) Running title > 45 chars truncates with warning per Janine §1.2.2; editor must shorten before render or the truncated title ships in the @top-center running header. (5) DOI placeholder `10.XXXXX/oscrsj.{year}.{padded}` until Crossref membership lands ($275/yr, Brad territory post-LLC) — published PDF will surface this placeholder until Crossref registers a real DOI. (6) Session-numbering off-by-one as noted above. (7) Renderer commit `a31783d` is local-only until Kanwar pushes from his Mac terminal (HTTPS auth required, sandbox can't supply credentials). (8) Status auto-flip removal is a behavior change for any code path that relied on `writePublishStatus` to flip status — audit: nothing in renderer relies on it, `PublishedPdfPanel` reads path columns not status. Safe.

**Kanwar follow-ups (immediate):** (1) From Mac terminal: `cd ~/Documents/oscrsj-renderer && git push origin main` to ship the renderer-side commit. (2) Generate shared secret: `openssl rand -hex 32`. Set on Vercel (Production + Preview + Development) for OSCRSJ main app as `RENDERER_SHARED_SECRET`. Also paste into `~/Documents/oscrsj-renderer/.env.local` as `RENDERER_SHARED_SECRET`. Also add `OSCRSJ_BASE_URL=https://www.oscrsj.com` (or `http://localhost:3000` for local dev) to the renderer's `.env.local`. Set `NEXT_PUBLIC_RENDERER_URL=http://localhost:3001` on the OSCRSJ Vercel envs (dev value works because the renderer runs locally on your Mac for now). (3) Smoke test: `npm run dev` from `~/Documents/oscrsj-renderer/`, navigate to `http://localhost:3001/render/{manuscriptId}` for your accepted manuscript, verify the payload banner shows no errors (warnings are fine), clean up the Pandoc-converted HTML in the cleanup pane, click "Publish manuscript…", watch the NDJSON stream, verify the chain reaches `writeback ok` and the PublishedPdfPanel on OSCRSJ admin page populates after refresh. (4) Click "Publish (go live)" on the OSCRSJ admin page → verify article appears at `/articles/in-press` (current behavior — `/articles` route doesn't exist yet, future Franklin follow-up). (5) Visual spot-check `PublishPipelinePanel` rendering at three breakpoints post-Vercel-deploy. Full handoffs at [docs/session-history.md#session-54-renderer-payload-synthesizer](docs/session-history.md). **Handoffs pushed: None** (this work is self-contained Sushant; Janine's PMC/DOAJ compliance review on the first rendered output is the next downstream handoff but is gated on a real artifact existing in storage, which lands when Kanwar runs the smoke test).

### Session 53 — 2026-05-15 — Franklin Cowork — SR/MA Wave 1 site-verification GATE CLEARED — `/peer-review` + `/guide-for-authors` now state "double-blind peer review by reviewers outside of our network"

Single commit `a81dd05` on `main`, fast-forward push (`5506987..a81dd05`), explicit-path stage on 2 files (`app/peer-review/page.tsx` + `app/guide-for-authors/page.tsx`), +19/-8 lines, TypeScript clean (exit 0). Kanwar directive: "Load Franklin, pickup your handoff to make sure we can move forward with our new manuscript acceptance." Closed inbound Manvir handoff `^handoff-srma-wave1-peer-review-site-verification-2026-05-12` (filed 2026-05-12; sat un-pulled 3 days past 24h target until Kanwar surfaced it explicitly). **Why this matters:** the SR/MA Wave 1 v3 email's credibility paragraph asserts "The articles will undergo double-blind peer review by reviewers outside of our network when submitted to our journal." 40 cold-invited authors received that email 2026-05-12; recipients cross-check the public site before replying, and site-email inconsistency is a predatory-journal red flag. Prior to this commit `/peer-review` stated double-blind explicitly but never asserted the reviewer-source-network boundary, and `/guide-for-authors` mentioned "standard double-blind peer review" twice (SR/MA definition + FAQ) but had no peer-review section at all. **Verification step (1 + 2 of the handoff spec):** grep on both pages for `outside our network` / `outside the editorial` returned zero hits; manual read confirmed double-blind ✅ + out-of-network ❌ on both. **Fix step (3 of the handoff spec):** matched the email's exact phrasing across the two pages. **/peer-review changes (5 surfaces, 1 file):** (a) `metadata.description` extended to "OSCRSJ uses double-blind peer review by reviewers outside of our network."; (b) `openGraph.description` same; (c) Double-Blind Peer Review intro paragraph extended — "All manuscripts submitted to OSCRSJ undergo double-blind peer review by reviewers outside of our network. Neither the authors nor the reviewers know each other's identities during the review process, and reviewers are recruited from outside the OSCRSJ editorial board — subspecialty-matched orthopedic surgeons and clinician-researchers — so that no editor evaluates work by their own collaborators."; (d) Step 2 "Reviewer Assignment" `desc` extended — "Suitable manuscripts are assigned to at least two independent reviewers recruited from outside the OSCRSJ editorial board, with expertise in the relevant orthopedic subspecialty."; (e) FAQ JSON-LD Q1 ("What type of peer review does OSCRSJ use?") + Q2 ("How many reviewers evaluate each manuscript?") both extended with out-of-network language so structured-data crawlers + AI citation engines see the same claim. **/guide-for-authors changes (3 surfaces, 1 file):** (a) SR/MA article-type `definition` extended — "Unsolicited submissions are welcome and undergo our standard double-blind peer review by reviewers outside of our network."; (b) FAQ JSON-LD answer for "What types of manuscripts does OSCRSJ accept?" extended with the same trailing phrase; (c) NEW `Peer Review` entry inserted into the `generalSections` array (anchor `#peer-review`, between Ethics and Cover Letter — adds a fifth section + a fifth jump-link chip in the "On This Page" bar). The new section has 4 bullets: double-blind + neither-side-knows; reviewers recruited from outside the OSCRSJ editorial board; ≥2 independent reviewers using structured forms covering scientific merit/methodology/clarity/ethics; initial editorial response within 10 days + 30-35-day decision SLA + pointer to `/peer-review`. **Acceptance step (4 of the handoff spec) satisfied:** both pages state "double-blind peer review" AND "reviewers outside our network" — SR/MA Wave 1 site-vs-email consistency hard-gate cleared. **Risk-flag carryforward update on Manvir's `^handoff-srma-wave1-response-tracking-2026-05-12`:** the first §"Risk flags carrying forward from send" bullet ("Watch for any reply mentioning 'checked your site'") is now resolved — pushed an outbound notification handoff to Manvir Agent (`^handoff-srma-wave1-site-verification-cleared-2026-05-15`) so her next briefing folds in the cleared state. **§6 cross-session orphan-files protocol:** working tree at session start carried 21 untracked artifacts (lockfiles, draft trackers including `Track A Wave 1 Tracker.xlsx`, `.cowork-push/` recovery patch from Session 50, social-export drafts, Word doc CV uploads, sample IG announcement images, etc.) — none overlapped with the 2 files I needed to edit; all left untouched per §6 protocol. **FUSE stale-lock event:** hit `.git/index.lock` + `.git/HEAD.lock` simultaneously on first `git add`; cleared via documented `mv .git/X.lock .git/.stale-junk/X.lock.$(date +%s%N)` workaround. Standard `tmp_obj_*` "Operation not permitted" unlink warnings during `git commit` non-fatal per Conventions. **Verification:** `npx tsc --noEmit -p tsconfig.json` exit 0 pre-commit; HEAD tree size 269 (healthy, unchanged from Session 52; no new files); 2 files staged exactly; explicit-path-stage no `add -A`; fast-forward push confirmed via `git log -1 origin/main` = `a81dd05`. **Risks:** (1) `/guide-for-authors` jump-link bar now shows 5 chips instead of 4 — odd-count first-row layout at the `sm:flex-wrap` breakpoint should still wrap cleanly but visual spot-check at narrow viewports not run from sandbox; (2) the new `#peer-review` anchor isn't referenced by any transactional email today, but adding it now creates the surface so future "see our peer review process" links from outbound copy can deep-link without a content gap; (3) the FAQ JSON-LD Q1 answer on `/peer-review` is now 281 chars (was 144) — comfortably under Google's structured-data display ceilings but the longer answer renders fuller in AI citation contexts; net-positive for E-E-A-T. **Self-improvement note for next session's owner-agent:** the 3-day target slip on a 5-min-verification handoff is the same pattern §11 already flags as the "Stale Handoff Protocol" follow-up. For verification-shaped handoffs, the right pre-flight is a single grep against the acceptance criteria BEFORE scoping — here, `grep -i "outside our network" app/peer-review/page.tsx app/guide-for-authors/page.tsx` would have returned the diagnosis in one tool call (≤30 seconds), letting the agent decide "5-min close" vs "30-min fix" without reading the full pages. Codifying as Convention §3 entry: "**Verification-handoff grep-first.** When an inbound handoff frames as 'verify X is stated on Y' / 'confirm Z is true,' run a grep against the acceptance criteria BEFORE scoping the session — `grep -i "<exact-phrase>" <path>` answers verification-shaped handoffs in one tool call, letting downstream scope decide between 'close in 1 turn' vs 'open commit work.' Reading full pages before grepping is the wrong order for verification handoffs." This pattern would have surfaced the gap on 2026-05-12 (handoff filed day) and the fix would have shipped same-week. **Kanwar follow-ups:** (1) visual spot-check both pages post-Vercel-deploy (~60s after `a81dd05`) — `/peer-review` Double-Blind Peer Review section + Step 2 "Reviewer Assignment" card render the new copy + footer FAQ JSON-LD valid via [Rich Results Test](https://search.google.com/test/rich-results); `/guide-for-authors#peer-review` new section renders at correct position in jump-link bar + body bullets readable at three breakpoints (narrow mobile / tablet / desktop); (2) SR/MA Wave 1 response window closes 2026-05-17 per Manvir's tracking handoff — site is now consistent with email body for any recipient who cross-checks. Full narrative + handoffs at [docs/session-history.md#session-53-srma-site-verification-cleared](docs/session-history.md). **Handoffs pushed: Manvir** (`^handoff-srma-wave1-site-verification-cleared-2026-05-15` — outbound notification that the gate is clear + risk-flag carryforward update for her next briefing).

---

## Recent Sessions (pointers)

Sessions from the last 14 days as 1-line pointers (newest first). Older sessions reachable via grep on the archive.

- **Session 55 — 2026-05-15 — Franklin — Chingiz Alizade portrait swap (.jpg → .png) + schema photo path flip** — see [docs/session-history.md#session-55-chingiz-alizade-portrait-swap](docs/session-history.md)
- **Session 52 — 2026-05-14 — Franklin — /templates Revision Resources rewrite (Response Template .docx removed; replaced with Comment/Response/Changes structure cards)** — see [docs/session-history.md#session-52-templates-revision-resources-rewrite](docs/session-history.md)
- **Session 51 — 2026-05-14 — Sushant — Reviewer-feedback Word doc attaches to Minor/Major Revisions decision emails (orphan-cluster recovery + lib/admin/actions.ts wiring)** — see [docs/session-history.md#session-51-reviewer-feedback-attachment](docs/session-history.md)
- **Session 50 — 2026-05-14 — Franklin — Sitewide article-type taxonomy: 6-type canonical list with SR/MA replacing invited Review Articles + /aims-scope full review + stale CC BY-NC-ND fix** — see [docs/session-history.md#session-50-article-type-taxonomy](docs/session-history.md)
- **Session 49 — 2026-05-11 — Franklin — /aims-scope six article types + sitewide audience-reframe sweep + Sivaramakrishnan thin-bio noindex** — see [docs/session-history.md#session-49-aims-scope-audience-reframe-thin-bio](docs/session-history.md)
- **Session 48 — 2026-05-06 — Franklin — Editorial board roster: 2 new section editors + Sukhman/Yash role swap + ortho-marker credential strip** — see [docs/session-history.md#session-48-editorial-board-roster-expansion](docs/session-history.md)
- **Session 47 — 2026-05-06 — Sushant — JATS Publishing 1.3 XML capability landed in renderer + main-repo migration 020 + admin panel + first GitHub-ready push of renderer** — see [docs/session-history.md#session-47-jats-xml-implementation](docs/session-history.md)
- **Session 46 — 2026-05-05 — Franklin — Guide for Reviewers rebrand + editorial inbox-routing flip + contact-form mailbox-void quick-fix** — see [docs/session-history.md#session-46-guide-for-reviewers-rebrand](docs/session-history.md)
- **Session 45 — 2026-05-05 — Sushant — Daily editorial-ops digest cron** — see [docs/session-history.md#session-45-daily-editorial-ops](docs/session-history.md)
- **Session 44 — 2026-05-04 — Sushant — Admin manuscript "Reviewer pool" panel sources from unified roster** — see [docs/session-history.md#session-44-reviewer-pool-unified-roster](docs/session-history.md)
- **Session 43 — 2026-05-04 — Sushant — Password reset email pipeline unblocked end-to-end** — see [docs/session-history.md#session-43-password-reset-pipeline](docs/session-history.md)
- **Session 42 — 2026-05-03 — John — Social media integration + P0 zero-index verification + OSCRJ collision refinement** — see [docs/session-history.md#session-42-social-media-integration](docs/session-history.md)
- **Session 41 — 2026-05-03 — Manvir — Three-task SEO + a11y session (John AI citation baseline + Sprint 2 ORCID `sameAs` + Sprint 3 text-tan finding)** — see [docs/session-history.md#session-41-john-ai-citation-baseline](docs/session-history.md)
- **2026-05-02 — Intervening commit `fb43dc2` — Manuscript template formatting locks (8 unified rules across 6 article types)** — see [docs/session-history.md#entry-2026-05-02-template-formatting-locks](docs/session-history.md)
- **2026-05-01 — Intervening commit `f7c3bf7` — Parmida + Yash headshot extraction** — see [docs/session-history.md#entry-2026-05-01-headshot-extraction](docs/session-history.md)
- **Session 40 — 2026-05-01 — Franklin — Editorial board roster expansion 14 → 17 confirmed members** — see [docs/session-history.md#session-40-editorial-board-roster-expansion](docs/session-history.md)
- **Session 39 — 2026-04-30 — Franklin — Sprint 1 SEO bundle: site-wide canonical sweep + soft-404 guards** — see [docs/session-history.md#session-39-sprint-1-seo-canonical-sweep](docs/session-history.md)

Sessions older than 14 days (Session 38 and earlier, including the 2026-04-26 Sushant cluster + Sessions 22–37) are archived in [docs/session-history.md](docs/session-history.md) — `grep '#session-NN-slug' docs/session-history.md` or scan with `grep '^### Session' docs/session-history.md`.

---

## Open Follow-ups

Live punch list. Items resolve as Kanwar (or downstream agents) confirm completion; the archive entry for the surfacing session preserves the provenance.

- [x] ~~**Run Migration 020 in Supabase Studio**~~ — ✅ done 2026-05-06 same-session (Kanwar confirmed via Supabase Studio). Column `manuscripts.jats_xml_storage_path TEXT` is live in prod.
- [x] ~~**GitHub closeout for renderer**~~ — ✅ done 2026-05-06 same-session. Renderer pushed to https://github.com/kanwarparhar/oscrsj-renderer (private, Session 17/18/20 commits all on `origin/main`). Closes Session 18 deferral.
- [x] ~~**Franklin — Verify `/peer-review` + `/guide-for-authors` state "double-blind peer review by reviewers outside our network"** before SR/MA Wave 1 send.~~ — ✅ done 2026-05-15 (Session 53, Franklin Cowork). Commit `a81dd05` on `main`: `/peer-review` (metadata + OG + Double-Blind Peer Review intro + Step 2 Reviewer Assignment + FAQ JSON-LD Q1/Q2) and `/guide-for-authors` (SR/MA definition + FAQ JSON-LD + new `Peer Review` general-sections entry, anchor `#peer-review`) all explicitly state "double-blind peer review by reviewers outside of our network" + "recruited from outside the OSCRSJ editorial board." TypeScript clean. SR/MA Wave 1 site-vs-email consistency gate cleared; Manvir notified via `^handoff-srma-wave1-site-verification-cleared-2026-05-15`.
- [ ] **JATS DTD fetch script + validator path hotfix (renderer repo)** — three coordinated bugs surfaced when Kanwar tried to fetch the JATS 1.3 DTD bundle on his Mac, post-Session-47-ship: (a) `scripts/fetch-jats-dtd.sh` baked URL `https://jats.nlm.nih.gov/publishing/1.3/JATS-Publishing-1-3-DTD.zip` returns 404 — NCBI doesn't ship a single zip there, they ship a directory of variant-specific bundles at `https://public.nlm.nih.gov/projects/jats/publishing/1.3/`. (b) The actual main DTD inside the OASIS+MathML3 bundle is named `JATS-journalpublishing-oasis-article1-3-mathml3.dtd`, not `JATS-journalpublishing1.dtd` (older naming convention). (c) Both `lib/renderer/jatsValidate.ts JATS_DTD_DEFAULT_PATH` and `lib/renderer/jatsXml.ts` DOCTYPE `JATS_DTD_SYSTEM_ID` reference the older filename. Kanwar's local install works via a `JATS-journalpublishing1.dtd → JATS-journalpublishing-oasis-article1-3-mathml3.dtd` symlink workaround, but a fresh clone of the renderer to another Mac (or CI) will fail until this lands. ~10-min Sushant hotfix: rewrite `fetch-jats-dtd.sh` to `wget --recursive --no-parent --no-host-directories --cut-dirs=4 -e robots=off -R "index.html*" https://public.nlm.nih.gov/projects/jats/publishing/1.3/` then `unzip JATS-Publishing-1-3-OASIS-MathML3-DTD.zip` then flatten the resulting `JATS-Publishing-1-3-OASIS-MathML3-DTD/` subdirectory; update `JATS_DTD_DEFAULT_PATH` constant + `JATS_DTD_SYSTEM_ID` constant + the DOCTYPE template literal to use `JATS-journalpublishing-oasis-article1-3-mathml3.dtd` consistently. Bonus: capture canonical SHA256 of the OASIS-MathML3 zip after first run and fold into the script for future-fetch integrity verification. Surfaced Session 47 wrap-up.
- [ ] **Smoke renderer end-to-end on Mac** — `npm run dev` from `~/Documents/oscrsj-renderer/`, exercise `/smoke/sample-payload`, `/smoke/sample-payload-8-authors`, `/smoke/sample-payload-reused-figure`. DTD bundle is installed locally via Session 47 wget+symlink workaround; smoke confirms all 3 fixtures produce DTD-clean JATS (acceptance criteria AC1-AC2 of the spec). Carries the only remaining acceptance check from Session 47's 13-criteria list. Surfaced Session 47.
- [x] ~~**Correct stale CC BY-NC-ND mention** in CLAUDE.md `## Business Context`~~ — ✅ done 2026-05-11 (Janine Cowork — opportunistic during status-check session). Line now reads "License: CC BY 4.0 (reverted 2026-05-04...)" with provenance link.
- [ ] **Visual spot-check Session 49 surfaces post-Vercel-deploy** (~60s after `e4affb9` push) — `/aims-scope` (six items render cleanly in bullet list + trailing Guide for Authors link works), `/editorial-board` (new Founding Editor card blurb + page subtitle + Join CTA), `/guide-for-authors` (subtitle one-liner), `/news` + `/subscribe` (audience-reframe lines), `/manifest.webmanifest` (PWA description) at three breakpoints — surfaced Session 49
- [ ] **Verify `/editorial-board/adithyaa-sivaramakrishnan` emits noindex post-deploy** — `view-source:` should contain `<meta name="robots" content="noindex,follow">`; URL should drop from `https://www.oscrsj.com/sitemap.xml`. Auto-flips back to indexable when his BOARD_MEMBER_BIOS entry gets fleshed out — surfaced Session 49
- [x] ~~**Route 4 orphan working-tree files to owner-agent**~~ — ✅ resolved 2026-05-14 (Session 50). Cluster preserved as patch at `.cowork-push/session-49-50-orphan-decision-composer-feature.patch` (492 lines covering DecisionComposerPanel + 2 editorial decision email templates). `lib/reviewer-feedback/` untracked dir kept intact on disk. The 3 orphan code files reverted to HEAD via `git show + mv` workaround so this session's tsc passed; `lib/admin/actions.ts` orphan additions (import, interface field, 2 in-function feedbackAttachment blocks, big new `previewReviewerFeedback` + `resolveReviewerFeedbackAttachment` exports) were already wiped during my TYPE_LABELS revert — reconstructable by Sushant from the email-template prop shape + DecisionComposerPanel call shape. New Sushant handoff filed (see entries below).
- [x] ~~**Sushant — Restore decision-composer feature from orphan patch**~~ — ✅ done 2026-05-14 (Session 51). Commit `3c3d8f5` on `main`: patch applied for the 3-file UI + email-template cluster; `lib/admin/actions.ts` wiring rebuilt from scratch per Session 50 spec (import of `buildReviewerFeedbackDocx`, `reviewerFeedbackOverride` field, `REVIEWER_FEEDBACK_MAX_BYTES` constant, `resolveReviewerFeedbackAttachment` + `previewReviewerFeedback` exports, attachment threading + `hasReviewerFeedbackAttachment` flag in both Minor/Major Revisions branches, `reviewer_feedback_attachment_resolved` audit-log row). `lib/reviewer-feedback/build.ts` added to git. TypeScript clean.
- [ ] **Sushant — PRISMA hard-gate enforcement** — Session 50 promoted PRISMA from "recommended" to "required" copy on `/aims-scope` + `/guide-for-authors` + `/templates` + `/faq`. Site-vs-enforcement gap until backend wizard lands. Required work: (a) migration adding `prisma_checklist` value to FileType enum (mirror of Session 43's `care_checklist` + `jbi_case_series_checklist` migration 019); (b) Step 2 wizard validation update — block Submit if `manuscript_type === 'review_article'` and no `prisma_checklist` file uploaded; (c) submission portal copy update on Step 2 to surface the new required slot; (d) update `supabase/migrations/019_care_jbi_checklist_file_types.sql` comment (the comment still lists review_article as a type WITHOUT a mandatory checklist — stale once PRISMA enforcement lands). Estimated effort ~3 hours mirroring Session 43 scope. Surfaced Session 50
- [ ] **Janine — Stale CC BY-NC-ND on /templates + /faq** — Session 47's license-revert sweep missed three references that Session 50 surfaced but only fixed the one on `/aims-scope` (in-scope page). Remaining: (a) `/templates` page "Publication License" section (lines ~470–488 of `app/templates/page.tsx`) — full License section paragraph + blockquote + CC BY-NC-ND URL reference; (b) `/faq` Q "Is OSCRSJ open access?" answer text; (c) `/faq` Q "Can I share my published article on social media or my personal website?" answer text ("As an open-access journal with CC BY-NC-ND 4.0 licensing..."). All three need flip to CC BY 4.0 with attribution language. ~10-min Janine edit. Surfaced Session 50
- [ ] **End-to-end smoke test reviewer-feedback Word-doc attachment** — pick (or seed) a manuscript in `status='under_review'` with ≥1 submitted non-draft review carrying `comments_to_author` text. (a) Admin manuscript page → draft Minor Revisions decision → verify the "Reviewer feedback attachment" card renders below the deadline picker. (b) Click "Preview / download attachment" → verify the `.docx` downloads + opens cleanly in Word with the OSCRSJ title block + per-reviewer "Reviewer 1" / "Reviewer 2" Heading 1 sections + correct body paragraphs. (c) Submit the Minor Revisions decision → verify the corresponding-author receives the email with the `.docx` attached + the body copy references the attachment + the `audit_logs` table has a `reviewer_feedback_attachment_resolved` row with `source: 'auto'`. (d) Repeat for Major Revisions decision. (e) Override path: download the auto-generated `.docx`, edit one sentence in Word, save, re-upload via "Upload edited version", submit decision → verify the override `.docx` attaches instead + audit-log row shows `source: 'override'`. (f) Empty-state: pick a manuscript without any submitted reviews → verify the card shows the amber "No reviewer comments are available" notice + decision still sends without attachment + audit-log row shows `source: 'none'`. Surfaced Session 51
- [ ] **Visual spot-check Session 51 surfaces post-Vercel-deploy** (~60s after commit pushes) — admin `/dashboard/admin/manuscripts/[id]` for any in-review manuscript: draft Minor Revisions or Major Revisions decision, verify the "Reviewer feedback attachment" card renders only when the decision is Minor/Major Revisions (NOT for Accept/Reject/Desk Reject); confirm the Preview button + Upload + Clear override interactions render at expected widths at three breakpoints. Surfaced Session 51
- [ ] **Codify "Outbound email anchor grep-verification" convention §3** (vault-side and CLAUDE.md `# Conventions`) — every email template that hardcodes a URL fragment (`/templates#revision-resources`, `/dashboard#submissions`, etc.) must be grep-verified pre-flip; if the anchor moves, the email and the page must update in lockstep. Surfaced Session 51 by the Revision Response Template link flip from `/downloads/oscrsj-revision-response-template.docx` to `/templates#revision-resources`. Suggested wording: "Outbound email anchors must be grep-verified pre-flip. If a transactional email links to `/path#anchor`, the matching `id="anchor"` must exist; rename it without updating the email and recipients get a soft-404."
- [ ] **Codify "Cross-verify HEAD state during orphan recovery" convention §3** — during any orphan-recovery operation, file state must be verified via bash `git show HEAD:path | grep ...` BEFORE trusting earlier-turn `Read` tool output. The `Read` tool can return stale cached content during long sessions; bash `git show HEAD:` is the authoritative source of truth. Surfaced Session 51 (earlier-turn `Read` showed `hasReviewerFeedbackAttachment` already present on the email templates when HEAD lacked it entirely; bash verification corrected the diagnosis).
- [ ] **Visual spot-check Session 52 surfaces post-Vercel-deploy** (~60s after `846ac2a`) — load `/templates#revision-resources` and verify (a) the **How to structure your Response to Reviewers document** card renders cleanly with the code-style `<pre>` block (`Comment:` / `Response:` / `Changes:` in mono on cream-alt background), readable bullet list defining each label, and "No change + rationale-under-Response" convention text; (b) the **How to format your tracked-changes manuscript** card renders below with the RED font + YELLOW highlight callout preserved; (c) the right-rail download button is gone (no orphan `Download Response Template (.docx)` button); (d) layout flows at three breakpoints (narrow mobile, tablet, desktop) — the `<pre>` block doesn't overflow horizontally on mobile. Surfaced Session 52
- [ ] **Downstream-surface sweep — 4 references to the removed Revision Response Template** — Session 52 removed the `.docx` offering from `/templates#revision-resources` but 4 downstream surfaces still name the template by reference. Required edits in one coherent commit: (a) `lib/email/templates/editorialDecisionMinorRevisions.ts` lines 81 (HTML body) + 119 (plain-text body) — replace "Use the OSCRSJ Revision Response Template (worked example + pre-formatted response tables): https://www.oscrsj.com/templates#revision-resources" with a description of the Comment/Response/Changes structure pointing at `/templates#revision-resources` for the spec; (b) `lib/email/templates/editorialDecisionMajorRevisions.ts` lines 82 + 120 — same rewrite; (c) `app/dashboard/submit/Step2Files.tsx:193` Response-to-Reviewers `description` field — rewrite from "Use the OSCRSJ Revision Response Template (download from /templates) — it contains a worked example and pre-formatted response tables. Accepted formats: .docx or .pdf (max 20 MB)." to describe the three-label structure inline + drop the template reference; (d) `lib/reviewer-feedback/build.ts:228` author-facing italic note inside the blinded reviewer-feedback `.docx` — rewrite from "using the Revision Response Template available at oscrsj.com/templates" to "following the Comment/Response/Changes structure described at oscrsj.com/templates#revision-resources." Estimated ~15-min Franklin sweep. Surfaced Session 52
- [ ] **Cleanup — delete orphan `oscrsj-revision-response-template.docx` + build script** — after the downstream-surface sweep above lands, `public/downloads/oscrsj-revision-response-template.docx` (the `.docx` file itself) and `scripts/build-revision-response-template.py` (build script that generates it) become fully unreferenced. Safe to delete both via explicit-path `git rm`. Low priority — both files are harmless on disk if left, but cleanup removes a confusing "why does this exist?" question for future agents. Surfaced Session 52
- [ ] **Codify "Removed-artifact downstream-surface inventory" convention §3** (vault-side and CLAUDE.md `# Conventions`) — when removing a user-facing artifact (downloadable template, link target, brand asset, file from `public/`), grep for every surface that names it by reference BEFORE scoping the commit. Either bundle the rewrite into one coherent commit with permission, or flag every stale surface explicitly in the wrap-up so the user can decide. Silent half-fixes that leave emails or form descriptions pointing at deleted assets are worse than a slightly-broader commit. Suggested wording: "Before removing a user-facing artifact, run `grep -r '<artifact-name>'` across the repo. Every surface that names the artifact must either be updated in the same commit or flagged in the wrap-up — never leave a transactional email pointing at a deleted asset." Surfaced Session 52
- [ ] **Visual spot-check Session 50 surfaces post-Vercel-deploy** (~60s after commit pushes) — verify (a) `/aims-scope` shows 6-type list with SR/MA as item 3 + Purpose paragraph mentions all 6 types + License section says CC BY 4.0; (b) `/guide-for-authors` Article Types Comparison table column order matches the new article-type ordering (SR/MA between Case Series and Surgical Technique) + word limit row reads 2,000 / 3,000 / 3,500 / 1,500 / 500 / 600; (c) `/templates` Reporting Checklists section shows the PRISMA card with new "For Systematic Reviews & Meta-Analyses" heading + "Required for every Systematic Review & Meta-Analysis submission" copy; (d) `/dashboard/submit` Step 1 picker shows "Systematic Review & Meta-Analysis" in the type-picker radio list at position 3; (e) admin manuscripts list shows new label when a `review_article` manuscript is shown (no pre-launch manuscripts yet so this won't surface until first SR/MA submission). Surfaced Session 50
- [ ] **ManuscriptType DB enum value rename (low priority, cosmetic)** — DB column constraint still says `'review_article'`. Display label is now "Systematic Review & Meta-Analysis" everywhere. If we want column-value alignment, that's a migration: ALTER constraint, add new value `'systematic_review_meta_analysis'`, backfill any pre-launch rows (zero today), drop the old value. ~30-min Sushant work but no functional impact (display label is what users see). Defer until first published SR/MA exists in DB. Surfaced Session 50
- [ ] **Cosmetic — rename `oscrsj-template-review-article.docx` + `oscrsj-example-review-article.docx`** to `oscrsj-template-systematic-review-meta-analysis.docx` (+ example) — when a user downloads the SR/MA template they currently see "review-article" in the filename. Requires renaming files in `public/downloads/` + updating `app/templates/page.tsx` `templateFile` + `exampleFile` strings + updating `scripts/build-article-templates.py` and `scripts/build-article-examples.py` filename + function-name references. Low priority; cosmetic only. Surfaced Session 50
- [ ] **Codify "Stale Handoff Protocol"** in Organization Plan §7.6 (vault-side) — any inbound handoff older than 7 days gets a pre-flight `rg` scan against acceptance criteria BEFORE scoping; handoff status text is a starting hypothesis, not ground truth. Surfaced by Session 49 self-improvement note (audience-reframe handoff sat "🔲 not started" 16 days while ~70% was already done by Session 25's sitewide sweep)
- [ ] **Smoke-test contact form post-Vercel-deploy** — submit any test message via `/contact` and confirm it lands at oscrsjournal@gmail.com (not bouncing) — surfaced Session 46 — see [docs/session-history.md#session-46-guide-for-reviewers-rebrand](docs/session-history.md)
- [ ] **Delete the test reviewer application** — Supabase Studio → Table Editor → `reviewer_applications` → delete row where `email = 'kanwarparhar+oscrsj-smoke-2026-05-05@gmail.com'` — surfaced Session 46
- [ ] **Daily-digest manual-trigger smoke test** — `curl -H "Authorization: Bearer $CRON_SECRET" https://www.oscrsj.com/api/cron/daily-digest` — surfaced Session 45
- [ ] **Daily-digest non-empty test** — sign up a fake user via private window with throwaway email, re-run manual trigger curl, verify digest lands at oscrsjournal@gmail.com with subject `[OSCRSJ Daily] 1 registration` — surfaced Session 45
- [ ] **Set up Gmail filter** for subject `[OSCRSJ Daily]` to label "OSCRSJ ops" — surfaced Session 45
- [ ] **E2E test Invite flow on the unified reviewer pool** — click "Invite" on both Manvir (ACTIVE bucket) and Rawnak (APPROVED bucket) on a test manuscript; verify mode='email' + mode='application' dispatch paths both work — surfaced Session 44
- [ ] **Optional bulk `mailto:` sweep** across the 9 user-facing pages still pointing at dead-letter `@oscrsj.com` addresses (`/peer-review`, `/faq`, `/privacy`, `/terms`, `/accessibility`, `/dashboard/settings`, `/api/dashboard/export`, co-author-dispute final HTML, `/for-reviewers/apply` success-state) — surfaced Session 46
- [ ] **Google Workspace provisioning** for `editorial@`/`info@`/`submit@`/`waivers@oscrsj.com` — when ready, flip routing back to per-department aliases (or refactor to env-var-driven `lib/email/inboxes.ts` per Session 46 risk #1) — surfaced Session 46
- [ ] **DMARC `rua` flip at GoDaddy** — `_dmarc.oscrsj.com` aggregate-report destination still routes to kanwarparhar@gmail.com — optional consolidation to oscrsjournal@gmail.com — surfaced Session 46
- [ ] **Run the 5-surface manual AI citation audit** (~30 min) — fill 30 stubbed cells (ChatGPT / Claude / Perplexity / Bing Chat / Gemini × 6 queries) using capture template at `02 - OSCRSJ/Notes/2026-05-03 John — AI Citation Audit Baseline.md` §5 — surfaced Session 41
- [ ] **Decide on OSCRJ brand-collision response** — Manvir-territory: surface disambiguation paragraph on `/about` + homepage hero, OR alternateName-only path, OR wait until ISSN registers and resolve via Wikidata — surfaced Sessions 41, 42
- [ ] **End-of-Phase-3 smoke test** — single large testing pass covering all 4 decision types + rescind + revision submission + revision-deadline reminder + Major-Rev re-invite + bulk decision modal + Revisions card. Carried over from Session 14. — see [docs/session-history.md#session-14-batch-decisions-revisions-panel](docs/session-history.md)
- [ ] **Decide post-Phase-3 priority** — (a) Stripe payment integration (LLC-gated), (b) Phase 4 publishing pipeline kickoff, (c) full E2E auth retest, (d) reviewer recruitment push, (e) revision-metadata snapshot migration — surfaced Session 14
- [ ] **Full E2E auth retest** — long-overdue from Sessions 5-14
- [ ] **Custom auth domain `auth.oscrsj.com`** — runbook ready at `docs/supabase-custom-auth-domain.md`; gated on Supabase Pro upgrade decision — surfaced Session 8
- [ ] **Crossref membership decision** (~$275/yr) — DOI registration becomes the post-publish DOI flow — surfaced Session 18
- [ ] **First manuscript reaches `accepted` → wire payload synthesizer** for renderer — surfaced Session 18, carried through Session 47
- [ ] **Submit sitemap to Google Search Console** at https://www.oscrsj.com/sitemap.xml — long-pending
- [ ] **Wire up `/contact` + `/subscribe`** form backends fully (Session 27 wired contact + discount-inquiry; subscribe still static) — surfaced Sessions 27+
- [ ] **Sample/template articles** showing what good submissions look like — long-pending
- [ ] **Reviewer recruitment push** + first 3-5 submissions — gated on Kanwar bandwidth
- [ ] **Section Editors grid balance** at `sm:grid-cols-2` — count went 9 → 12 with Session 48 (Yash promoted in, Sukhman demoted out, Alejandro + Jean added). 12 = 6 even rows of 2, so the original odd-count last-row issue likely self-resolves. Verify on next visual pass — optional polish — surfaced Session 40, count updated Session 48
- [ ] **Visual spot-check Session 53 surfaces post-Vercel-deploy** (~60s after `a81dd05` push) — `/peer-review` Double-Blind Peer Review section paragraph + Step 2 "Reviewer Assignment" card body both render the new out-of-network language; FAQ JSON-LD on `/peer-review` validates clean via [Rich Results Test](https://search.google.com/test/rich-results); `/guide-for-authors#peer-review` new section renders at the correct anchor position with the new `Peer Review` jump-link chip visible in the "On This Page" bar (5 chips total now — narrow-mobile wrap behavior worth a glance); all four bullets of the new section readable at three breakpoints. Surfaced Session 53
- [ ] **Codify "Verification-handoff grep-first" convention §3** (vault + CLAUDE.md `# Conventions`) — when an inbound handoff frames as "verify X is stated on Y" / "confirm Z is true," run a grep against the acceptance criteria BEFORE scoping the session. `grep -i "<exact-phrase>" <path>` answers verification-shaped handoffs in one tool call, letting the agent decide between "close in 1 turn" vs "open commit work." Reading full pages before grepping is the wrong order for verification handoffs. The 3-day target slip on Session 53's 5-min `^handoff-srma-wave1-peer-review-site-verification-2026-05-12` (filed 2026-05-12, resolved 2026-05-15) is the canonical precedent — a single `grep -i "outside our network" app/peer-review/page.tsx app/guide-for-authors/page.tsx` on day-of would have returned the diagnosis instantly. Suggested wording for Conventions: "Verification-handoff grep-first. For any handoff framed as 'verify' or 'confirm,' run `grep -i "<exact-acceptance-phrase>" <target-paths>` BEFORE reading full files. The grep result answers the verification step; the file-read happens only if the fix step needs to scope a copy edit." Surfaced Session 53
- [x] ~~**Renderer payload synthesizer — wire `/api/publish/[id]` end-to-end** (Manvir handoff `^handoff-renderer-payload-synthesizer-2026-05-15`)~~ — ✅ done 2026-05-15 (Session 54, Sushant Cowork). OSCRSJ commit `1859d0c` on `main` + renderer commit `a31783d` on local `main`. `lib/publish/synthesize.ts` joins manuscripts + manuscript_authors + manuscript_affiliations + manuscript_metadata + users into Franklin v1.0 payload shape; GET `/api/publish/payload/[id]` Bearer-token endpoint; manual go-live gate via OSCRSJ-side `publishGoLive` / `unpublishManuscript` actions + `PublishPipelinePanel`. Renderer's `writePublishStatus` now writes paths only (status stays `accepted`); `app/render/[id]/page.tsx` fetches payload from OSCRSJ; CleanupPane surfaces synth warnings/errors banner. Structural diff against `sample-payload.json` fixture empty (all 15 top-level keys match). TypeScript clean both repos.
- [x] ~~**Push renderer commit `a31783d` from Mac terminal**~~ — ✅ done 2026-05-15 (Session 56). `git push origin main` from Mac terminal returned "Everything up-to-date" — commit was already on origin from a prior push.
- [x] ~~**Generate + set shared secret across both repos' envs**~~ — ✅ done 2026-05-15 (Session 56). `RENDERER_SHARED_SECRET` (`0fcdb493...`) + `NEXT_PUBLIC_RENDERER_URL=http://localhost:3001` set on OSCRSJ Vercel (Production + Preview). Renderer `.env.local` synced (`db70a0230f...` placeholder → `0fcdb493...` to match Vercel) + `OSCRSJ_BASE_URL=http://localhost:3000` → `https://www.oscrsj.com`. Note: `.env.local` lines 11-15 comment block on `OSCRSJ_BASE_URL` is misleadingly worded; cosmetic follow-up.
- [ ] **End-to-end smoke test the first publish — IN PROGRESS, BLOCKED on metadata gap** — Session 56 got the publish workflow operational against `f8596ce8-2424-4a47-8537-5af9e8a2f5cd`: renderer loaded, Pandoc converted, three-pane workspace rendered with title + 21 references parsed. Synthesizer surfaced 3 amber warnings (running title 97 chars > 45 max; abstract 0 labeled sections; affiliations table empty). Kanwar declined to use Supabase Studio quick-fix; instead asked for Pre-Render Metadata Editor (see new follow-up below). Publish blocked until either (a) editor ships OR (b) Kanwar accepts one-time Supabase Studio exception. Surfaced Session 54, advanced Session 56
- [ ] **Renderer `lib/renderer/workspace.ts` STORAGE_BUCKET fix uncommitted** — Session 56 surfaced + fixed a real bug: `workspace.ts:9` declared `STORAGE_BUCKET = 'manuscripts'` (wrong; file lives in `submissions` bucket). Session 47's "STORAGE_BUCKET flipped" fix only touched `storage.ts:37`; `workspace.ts:9` was missed. Bug stayed dormant 9 days because all smoke runs use `/smoke/sample-payload` which doesn't touch Storage. Fix applied in-session via `Edit` tool but uncommitted (renderer is local-only on Mac per Session 18+ pattern). Kanwar to ship: `cd ~/Documents/oscrsj-renderer && git add lib/renderer/workspace.ts && git commit -m 'fix(renderer): workspace.ts STORAGE_BUCKET manuscripts → submissions' && git push origin main`. Surfaced Session 56
- [ ] **Pre-Render Metadata Editor & PDF Preview project — awaiting Janine + Franklin sign-offs (target 2026-05-22)** — Session 56 planning artifact at `02 - OSCRSJ/Projects/Pre-Render Metadata Editor & PDF Preview.md` (~330 lines, 13 sections). Editor lives in OSCRSJ admin between RevisionsPanel and PublishPipelinePanel; six form sections covering every field the renderer's payload synthesizer consumes; live three-tier validation (red errors / amber warnings / green clear); new `/api/preview/[id]` endpoint extending renderer's `runPublishChain({ performPublish: false })` with `emitPreviewArtifact: true` flag. Two outbound handoffs blocking Sushant build phases: Janine `^handoff-metadata-editor-compliance-audit-2026-05-15` (field-set sign-off + migration 022 column shape + JATS 1.3 missing-fields confirmation, deliverable doc at `02 - OSCRSJ/Indexing/Standards/Pre-Render Editor Compliance Spec.md`); Franklin `^handoff-metadata-editor-ux-design-2026-05-15` (wireframes for all 6 editor sections + component class spec + 7 visual decisions, deliverable section appended to existing [[Article PDF Design System]] project). Build effort post-sign-offs: ~3 Sushant Cowork sessions ~10 hours. Surfaced Session 56
- [ ] **Decision needed on first manuscript publish — quick-fix or wait?** — Kanwar's first accepted manuscript `f8596ce8-2424-4a47-8537-5af9e8a2f5cd` is blocked at the editor-step. Two options: (a) one-time Supabase Studio direct edit on `manuscripts.running_title` (≤45 chars) + `manuscripts.abstract` (with structured labels) to unblock THIS publish only — documented as known protocol exception; (b) wait ~2-3 weeks for Pre-Render Metadata Editor to ship. Kanwar's stated preference today: wait. Confirmation needed. Surfaced Session 56
- [ ] **Codify "Constant-fix repo-wide grep" convention §3** (vault + CLAUDE.md `# Conventions`) — when fixing a constant in one file (env var, magic string, type alias), ALWAYS grep the whole repo for the same constant name across all definitions before declaring the fix complete. Session 47's `storage.ts` STORAGE_BUCKET fix didn't grep for `workspace.ts:9` and the bug stayed dormant 9 days until Session 56's first real Storage download fired. The 30-second `grep -n 'STORAGE_BUCKET\s*=' lib/renderer/` would have caught it. Suggested wording: "Constant-fix repo-wide grep. When changing a constant value (env var name, magic string, bucket name, type alias), run `grep -n '<CONSTANT_NAME>\s*=' <repo>` BEFORE declaring the fix complete. Two files defining the same constant with different values is a real bug class; the grep takes 30 seconds and surfaces every drifted definition." Surfaced Session 56
- [ ] **Migration 021 + structured references collection** — references table doesn't exist; submission wizard never collected structured refs; Vancouver-style refs live inside manuscript .docx. Path to fix: (a) add migration 021 creating `manuscript_references` table (manuscript_id FK + ref_order + citation_text TEXT + doi TEXT NULL + pmid TEXT NULL); (b) admin paste-in form on `/dashboard/admin/manuscripts/[id]` to populate refs retroactively for accepted manuscripts; (c) future submission-wizard upgrade adds a Step 3.5 collector. Until this lands, JATS XML ships without `<ref-list>` element + PMC indexing remains blocked. PDF/A-1b is unaffected because Pandoc body content has inline `<sup>` citation markers. Surfaced Session 54
- [ ] **Structured affiliations backfill** — `manuscript_affiliations` table exists (migration 013) but is never populated by submission code. Synthesizer falls back to building affiliations from `manuscript_authors.affiliation` free-text strings (lossy for multi-affiliation authors — they get collapsed to single-affiliation `[1]`). For the first accepted manuscript, verify whether any authors are multi-affiliation; if so, run a one-off SQL INSERT into `manuscript_affiliations` for each unique (department, name, city, country) combo + update `manuscript_authors` rows to link via `manuscript_author_id`. Future submission-wizard upgrade should populate this directly. Surfaced Session 54
- [ ] **Codify "Two-app payload sync via shared-secret" convention §3** (vault + CLAUDE.md `# Conventions`) — Session 54 introduced the OSCRSJ ⇄ renderer payload sync pattern: OSCRSJ owns the synthesizer + payload GET endpoint; renderer fetches with `Authorization: Bearer ${RENDERER_SHARED_SECRET}` on page-load parallel-await. The shared secret must match in both `.env.local` files; rotation requires updating both. Worth documenting so future cross-repo auth (Janine's eventual hosted-renderer migration, Brad's Stripe webhook, etc.) doesn't reinvent the pattern. Surfaced Session 54

---

## Where to Look

Pointers, not duplicates. Filesystem commands return current truth.

- **Session history:** `docs/session-history.md` — `grep '#session-NN-slug' docs/session-history.md` for any entry; `grep '^### Session' docs/session-history.md | head -30` for chronological scan; `grep '^### ' docs/session-history.md` for topic + section heads.
- **Routes:** `find app -name 'page.tsx'` or `tree -L 3 app/`. Active count ~64 (drift acceptable).
- **Components:** `ls components/ app/dashboard/admin/manuscripts/[id]/ app/dashboard/submit/ app/review/[token]/`.
- **Migrations:** `ls supabase/migrations/`. Migration index with shipped-by-session map at [docs/session-history.md#migration-index](docs/session-history.md).
- **Schema types:** `lib/types/database.ts`.
- **Architecture plan:** vault `02 - OSCRSJ/Projects/Submission Portal Architecture Plan.md`.
- **CLAUDE.md refactor brief + add/move rules (M1-M4):** vault `02 - OSCRSJ/Projects/CLAUDE.md Refactor.md`.
- **Renderer repo:** `~/Documents/oscrsj-renderer/`. Render chain shipped Session 18.
- **Editorial board schema:** `lib/schema/editorialBoard.ts`. 21 confirmed members as of Session 48.
- **Email templates:** `lib/email/templates/`. v2 dark-brown shell from Session 30.
- **Brand assets:** `public/brand/` (Session 15 lockup library) + `/media` Press Kit page.
- **Vault startup ritual:** read `_Meta/Organization Plan.md` then `_Meta/Vault State.md` at every session start (per user preferences).
