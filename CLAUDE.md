# OSCRSJ Website — Claude Code Context

> Live at oscrsj.com · **first article published 2026-05-19** (Volume 1, Issue 1, e0001 — Bilateral acute exertional compartment syndrome). Operational rhythm phase begins.

## Git Safety — READ BEFORE ANY COMMIT OR PUSH

**Background.** On 2026-04-22 a routine favicon-push session accidentally deleted 195 files from `origin/main`. The broken commits (`c46d818` → tree of 2 files, `57c236b` → tree of 4 files) were pushed to GitHub before anyone noticed. The live site kept serving only because Vercel's build of the broken tree failed and it stayed on the prior good deploy. Recovery shipped 2026-04-23 in commits `1f29795` (file restore by a parallel push) + `5150550` (the transparent-favicon change layered on top). Root cause: a `git add -A && commit && push` ran against a corrupted index that thought most project files didn't exist.

**Sanity checks before every commit:**

1. `git ls-tree -r HEAD --name-only | wc -l` — healthy OSCRSJ tree should be ~196-200 files. If HEAD's tree is dramatically smaller than the working tree, **STOP**. The index or HEAD is corrupted; do not commit.
2. `git diff --cached --stat | tail -1` — most feature commits touch 1-15 files. A staged set of 100+ files, especially mostly `new file:` entries, is a **red flag**, not a normal commit. Investigate before pushing.
3. **Never `git add -A` or `git add .` blindly.** Stage by explicit path: `git add app/foo.tsx public/bar.png`. Bulk-staging against a corrupted index is exactly what caused the 2026-04-22 incident.
4. **Never `git reset --hard` or `git push --force` (incl. `--force-with-lease`) without explicit Kanwar approval.** Recovery on 2026-04-23 was only possible because the working tree on disk still had all 197 files; a `git reset --hard` after the destruction would have made recovery require a fresh clone.
5. **If local `main` and `origin/main` diverge in ways you don't fully understand, FETCH and inspect** — `git log HEAD..origin/main` and `git ls-tree -r <SHA> --name-only | wc -l` for each side. Never merge or push until you've explained the divergence in your own words to Kanwar.
6. **Separate concerns across sessions.** If `git status` shows pre-staged files you did not touch in the current session (previous session's agent left work in the index), STOP. Check CLAUDE.md against recent commit history — if a session described as "shipped" has files still sitting staged and never committed, that commit never actually landed. Verify the migration/schema dependency of the stale work has been run in Supabase before bundling it into your commit. If in doubt, `git reset HEAD -- <those paths>` to unstage, commit only the current session's work by explicit path, and flag the leftover work to Kanwar for its owner-agent to resume. Never bundle a different session's feature work into your style/fix commit.

**Recovery cheat-sheet:** the pre-destruction commit is usually findable in `git reflog`. Compare tree sizes with `git ls-tree -r <SHA> --name-only | wc -l` to identify the last healthy commit. Restore by adding back the missing files in a single fast-forward commit titled `fix(repo): restore N source files erroneously deleted by <broken-SHA>`. **Never force-push.** If the FUSE mount between Kanwar's Mac and the Cowork sandbox blocks local git operations with stale lock files (`.git/index.lock`, `.git/HEAD.lock`, `.git/refs/heads/main.lock` that can't be `rm`'d from inside the sandbox — `mv` works around `unlink` failures), do a fresh `git clone` into `/sessions/.../OSCRSJ-push-workspace/`, apply the fix there, push from that clone. After pushing, Kanwar runs `rm -f .git/*.lock .git/refs/heads/*.lock && git fetch && git reset --hard origin/main` from his Mac Terminal to resync the local working copy. When deleting app routes via bash, leftover `.fuse_hidden*` files in the emptied directory are FUSE bookkeeping and don't affect Next.js routing; they clean up on next filesystem sync.

---

## What This Project Is

The official website for **OSCRSJ** (Orthopedic Surgery Case Reports & Series Journal) — Kanwar's independent, open-access orthopedic research journal targeting the global orthopedic surgery community. Domain: **OSCRSJ.com**. **First article published 2026-05-19** (Vol 1, Iss 1, e0001). Operational rhythm phase begins; ISSN gate now 1/5 published articles toward LOC eligibility.

---

## Conventions

Operational rules every session must follow. New entries land here only when a new rule is locked (see [docs/session-history.md#operational--meta-git-safety-conventions-refactors](docs/session-history.md) for surfacing context).

- **Explicit-path stage; never `add -A` or `add .`.** Stage every file by path. The 2026-04-22 file-deletion incident was caused by `add -A` against a corrupted index — see Git Safety above. Surfaced Session 14 (2026-04-22 incident).
- **TypeScript clean before commit.** Run `npx tsc --noEmit -p tsconfig.json` from repo root; commit only when it returns exit 0. Surfaced Sessions 11+.
- **FUSE stale-lock workaround.** When `git` errors with "Operation not permitted" on `.git/HEAD.lock` / `.git/index.lock` / `.git/refs/heads/*.lock` / `tmp_obj_*`, the file can't be `rm`'d from inside the sandbox but `mv` works around the unlink failure: `mv $f .git/.stale-junk/$(basename $f).$(date +%s%N)`. Non-fatal warning; the operation will complete cleanly. Surfaced Sessions 20+, codified Session 32.
- **PostgREST schema cache after column-add.** Every `ALTER TABLE ... ADD COLUMN` migration that's referenced by application code must end with `NOTIFY pgrst, 'reload schema'`. Without it, application writes hit "Could not find the 'X' column in the schema cache" until PostgREST's ~10-minute auto-refresh fires. Surfaced Session 34 (orphan migration 012).
- **Verify Vercel env-var state via Chrome MCP, not via prior CLAUDE.md claims.** Session 43's wrap-up incorrectly claimed `EMAIL_REPLY_TO` was set on Vercel; Session 46's verification revealed it was never set. Env-var state in CLAUDE.md changelogs should be verified before being trusted as ground truth. Surfaced Session 46.
- **www-canonical rule for outbound URLs.** Every emitted URL — `metadataBase`, `alternates.canonical`, `openGraph.url`, JSON-LD `@graph`, sitemap, robots.txt, server-action fallbacks, email footer hrefs, webhook destinations — must use `https://www.oscrsj.com/...`. Apex `oscrsj.com` 307s to www and most webhooks don't re-issue the body on redirect (Resend Svix-webhook silent-failure root cause, Session 5). Codified Session 6 commit `14d03e3`.
- **Reply-To on transactional emails.** `lib/email/resend.ts` `DEFAULT_REPLY_TO` defaults to `oscrsjournal@gmail.com` (env var `EMAIL_REPLY_TO` overrides). Inquiry-form replies override to the inquirer's email so editorial replies route directly back. Surfaced Sessions 6, 27, 43, 46.
- **DNS-level checks on any send-destination domain.** Run `dig MX <domain>` before assuming a domain accepts inbound mail. The contact-form mailbox-void bug (Session 46) silently swallowed every submission to `info@`/`submit@`/`editorial@`/`waivers@oscrsj.com` for ~9 days because `oscrsj.com` apex has no MX records. Surfaced Session 46.
- **Cross-session orphan-files protocol.** If `git status` shows pre-staged files you did not touch this session, STOP — it's another session's in-flight work. Verify migration dependency, unstage via `git reset HEAD -- <paths>`, commit only your work by explicit path, flag the orphans to Kanwar. Codified by §7.6 in [Organization Plan](../Documents/Kanwar%27s%20Second%20Brain/_Meta/Organization%20Plan.md). Surfaced Sessions 28, 32, 33, 44, 45.
- **Migration-slot arithmetic.** Before claiming a migration slot, run `ls supabase/migrations/` AND `git log --diff-filter=A -- supabase/migrations/`. Orphan migrations on disk that were never committed (Session 15's spec for migration 012 sat orphaned until Session 34 committed it as `5bea320`) cause "claim slot" collisions. Surfaced Sessions 28, 29, 34.
- **Audience copy is locked.** "The global orthopedic surgery community — practicing surgeons, fellows, residents, students, and researchers." Replaces all prior trainee-only framing. Surfaced Session 25, codified across `/about`, `/aims-scope`, `/faq`, `/submit`, homepage, layout metadata, `/media` boilerplate.
- **Ortho/Orth-marker credential strip.** Post-MBBS specialty markers in editorial board honorific suffixes are stripped — `MS (Orth)`, `MS(Ortho)`, `DNB (Orth)`, etc. all collapse to bare `MS` / `DNB`. Applies to both the `name` field and `honorificSuffix` in `lib/schema/editorialBoard.ts` `BoardMember` entries. Bio narrative prose may still reference orthopaedic training (degree names, fellowship titles in `BOARD_MEMBER_BIOS.education`/`experience` arrays). Surfaced Session 48 (Sankalp Lal `MS (Orth), DNB (Orth)` → `MS, DNB`; Vikash Raj `MS(Ortho)` → `MS`).
- **Author non-technical preference.** Kanwar is non-technical; prefer self-contained components over abstracted systems; pages must ship complete with real content (no lorem ipsum); Neutral Elegance palette is the brand.
- **CLAUDE.md add/move discipline (post-2026-05-06).** Every wrap-up runs M1-M4 in order: write full narrative to `docs/session-history.md`; update §9 Recent Sessions (full) and demote oldest if >3; prune §10 Recent Sessions (pointers) for 14-day window + 30-entry cap; resolve checked-off items in §11 Open Follow-ups, append new ones. Spec at vault `02 - OSCRSJ/Projects/CLAUDE.md Refactor.md` §9.
- **Constant-fix repo-wide grep.** When changing a constant value (env var name, magic string, bucket name, type alias), run `grep -n '<CONSTANT_NAME>\s*=' <repo>` BEFORE declaring the fix complete. Two files defining the same constant with different values is a real bug class; the grep takes 30 seconds and surfaces every drifted definition. The Session 47 STORAGE_BUCKET fix in `storage.ts` left `workspace.ts:9` drifted for 9 days until Session 56's first real Storage download fired. Codified Session 57.
- **FUSE-mount sync divergence diagnosis.** When sandbox-side `git status` shows files staged but Mac terminal `git status` shows "working tree clean" (or vice versa), ALWAYS re-run `git status` from a fresh `cd` into the repo before declaring divergence. Stale shell views are common during long sessions where the Mac terminal was opened before the sandbox writes landed. The second invocation typically picks up the writes correctly. Real FUSE divergence (sandbox writes not landing on Mac filesystem at all) is rare; stale shell view is the common case. Surfaced Session 57 Phase 1.C boundary.
- **CLAUDE.md §9/§11 is authoritative current-state — NEVER trust agent-file handoff status without cross-checking.** When answering "what's blocking?" / "what's the state of X?" / "are there blockers on your end?", read CLAUDE.md §9 Recent Sessions (full) + §11 Open Follow-ups FIRST. Those reflect every wrap-up's M1-M4 protocol output and are the canonical current-state source. Agent-file Inbound Handoff blocks (`02 - OSCRSJ/Resources/Agents/<Agent> Agent.md` §Inbound Handoffs) carry stale 🔲 statuses when the wrap-up's agent-file-update step was skipped — easy to miss because it's the last step. On 2026-05-18 a Sushant Cowork session pointed at `^handoff-sushant-docx-metadata-extract-2026-05-16` as a blocker for first publish; Session 58 had already shipped it end-to-end (renderer `81d7985` + OSCRSJ `b0c0cf7` merged as `83eebe8`) and CLAUDE.md §9 + §11 reflected that correctly, but Sushant Agent.md still read 🔲 not started. Cross-check protocol: run `grep '^### Session ' docs/session-history.md | head -3` + scan CLAUDE.md §11 strikethroughs BEFORE claiming any handoff is still open. Surfaced 2026-05-18.

---

## Tech Stack

| Item | Value |
|---|---|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript |
| Styling | Tailwind CSS with custom design tokens |
| Fonts | DM Serif Display (display serif) + Inter (body / UI) via Google Fonts |
| Backend / Auth | Supabase (PostgreSQL + Auth + RLS) |
| Email | Resend (transactional) + Svix-signed webhook |
| CAPTCHA | Cloudflare Turnstile (managed mode) |
| ORCID | Public API OAuth 2.0 (profile prefill + verified iD) |
| Deployment | Vercel (free tier) — auto-deploys from `main` |
| PDF render | WeasyPrint 68.1 → verapdf 1.30.1 (PDF/A-1b), separate renderer repo at `~/Documents/oscrsj-renderer/` |

---

## Deployment & Infrastructure

| Item | Details |
|---|---|
| **Live URL** | https://www.oscrsj.com |
| **GitHub Repo** | github.com/kanwarparhar/OSCRSJ (public) |
| **Hosting** | Vercel (free tier) — auto-deploys from `main` (~60s deploy) |
| **Domain Registrar** | GoDaddy — DNS configured |
| **DNS Records** | A: `76.76.21.21` / CNAME: `cname.vercel-dns.com` / SES MX on `send.oscrsj.com` for outbound |
| **SSL** | Auto-provisioned by Vercel (HTTPS) |
| **WWW redirect** | apex `oscrsj.com` → `www.oscrsj.com` (307). Canonical is `www`. Webhook URLs MUST use www form (apex 307 + most services don't re-issue body — Session 5 lesson). |
| **Inbound mail** | `oscrsj.com` apex has **NO MX records** (NXDOMAIN). Every send to `info@`/`submit@`/`editorial@`/`waivers@oscrsj.com` bounces. All form submissions route to `oscrsjournal@gmail.com` until Google Workspace is provisioned. See [docs/session-history.md#inbox-routing-history](docs/session-history.md). |
| **Backup URL** | oscrsj.vercel.app |

**Deploy:** push any commit to `main` → Vercel auto-rebuilds and goes live in ~60s.

---

## Design System

"Neutral Elegance" palette — dark gradient hero, editorial serif typography, warm tones, journal-grade reading ink. Tokens in `tailwind.config.ts` and `app/globals.css`. Current version: **v2.3 — Reading Mode** (rolled out 2026-04-21). Version history at [docs/session-history.md#design-system-history](docs/session-history.md).

| Token | Value | Usage |
|---|---|---|
| `peach` | `#FFDBBB` | CTA buttons on dark backgrounds, accent highlights |
| `peach-dark` | `#F0C49A` | CTA buttons on light/cream backgrounds |
| `taupe` | `#CCBEB1` | Decorative ONLY (borders, dividers). NEVER for text. |
| `tan` | `#997E67` | Hover border on interactive cards. **Banned from text classes** — fails WCAG AA on cream. Use `text-brown` for metadata. |
| `brown` | `#664930` | Accent text on light bg, button text, metadata |
| `brown-dark` | `#3d2a18` | **Headings only** (h1-h6 + `.page-title` + `.section-heading`). Also `.nav-link`. |
| `ink` | `#120D08` | **Primary body/paragraph text** on white and cream surfaces. ~19:1 contrast on cream, ~20:1 on white. Set as HTML body default. |
| `dark` | `#1c0f05` | Hero bg, nav bg, footer bg |
| `dark-card` | `#261609` | Dark card backgrounds |
| `cream` | `#FDFBF8` | Main page background — barely-warm near-white (~99% luminance) |
| `cream-alt` | `#F8F4ED` | Subtle-highlight surface — chips, pills, step-circles, table headers, empty states, hover |
| `white` | `#FFFFFF` | All cards, form inputs, article wells |
| `border` | `rgba(153,126,103,0.18)` | Subtle borders/dividers |

**Text color rule:** Body → `text-ink` (or inherited). Headings → `text-brown-dark` (paired with `font-serif`). Metadata → `text-brown`. Never `text-tan` for text.

**Background color rule:** Full-bleed wrappers → `bg-white`. Subtle highlights (chips/pills/headers/hover) → `bg-cream-alt`. `bg-cream` is near-invisible against white cards; reach for it only when the surface should read "almost the body background."

**Fonts:** Display serif (`font-serif`) — DM Serif Display, Georgia fallback, used for hero + page titles + section headings. UI / body / metadata (`font-sans`) — Inter, used for nav, buttons, body, dashboard chrome.

**Component classes** in `globals.css`: `.btn-primary`, `.btn-primary-light`, `.btn-outline`, `.btn-ghost`, `.card`, `.section-heading`, `.section-label`, `.nav-link`. **Card hover:** interactive cards get `hover:border-tan hover:shadow-sm` (no bg change). Static info cards get no hover.

---

## Business Context

- **No publisher.** Fully independent.
- **APC model:** Manuscripts submitted before **August 1, 2026** publish free; afterwards, single $499 standard rate per accepted manuscript. Discounts case-by-case via the inquiry form on `/apc`. Pricing history at [docs/session-history.md#apc-pricing-history](docs/session-history.md).
- **License:** CC BY 4.0 (reverted 2026-05-04 same-session after Janine surfaced the DOAJ + Plan S + NIH-funded-author indexing trade-off as a structured option-set; prior CC BY-NC-ND 4.0 lock from Session 25 superseded). Resolution at vault `02 - OSCRSJ/Notes/2026-05-04 License Reversion to CC BY 4.0 (Janine).md`.
- **Backend:** Custom LLM-powered submission system (built — see Phase 3.5 sessions). Renderer at `~/Documents/oscrsj-renderer/`.
- **Goal:** PubMed indexing (~2-year path requiring monthly releases).
- **Target audience:** The global orthopedic surgery community — practicing surgeons, fellows, residents, students, and researchers.
- **Revenue projections:** ~$13K Y1 → ~$72K Y2 → ~$170K Y3.

---

## Recent Sessions (full)

The 3 most recent sessions, full narrative. Older sessions surface as 1-line pointers in the next section; full narrative for any session lives in [docs/session-history.md](docs/session-history.md).

### Session 72 — 2026-05-22 — Sushant Cowork — Remove main manuscript upload from submission wizard — 1 OSCRSJ commit `8cbb869`

Kanwar request: remove the "Main Manuscript" (`file_type='manuscript'`) upload slot from the submission portal. The unblinded copy was a third redundant file given that OSCRSJ already requires (a) a Blinded Manuscript for peer review and (b) a Title Page carrying all author information.

**Commit `8cbb869` — 3 files, net -24 lines.** `Step2Files.tsx`: removed `manuscript` entry from `BASE_CATEGORIES_FIXED` and "Original Unblinded Manuscript" entry from `REVISION_CATEGORIES`; updated Step 2 hint text. `SubmissionWizard.tsx`: dropped `hasMainManuscript` from `step2Complete` gate and `computeInitialStep`. `lib/submission/actions.ts`: removed `manuscript` file-type server-side check from both `submitManuscript` and `submitRevision`. TypeScript clean; pushed `9f590f2..8cbb869`.

Full narrative at [docs/session-history.md#session-72-remove-main-manuscript-upload](docs/session-history.md). **Handoffs pushed: None.**

### Session 71 — 2026-05-22 — John Cowork — Article discoverability audit: Highwire Press `citation_*` tags + `MedicalScholarlyArticle` JSON-LD + async sitemap with dynamic article URLs + GSC sitemap resubmission — 1 OSCRSJ commit `9f590f2`

Audited discoverability of OSCRSJ's first published article (e0001, manuscript ID `f8596ce8`, "Bilateral acute exertional compartment syndrome of the forearms in a sport climber: a case report") across Google Scholar, Google Search, and AI citation tools. Shipped fixes as Franklin, then did the GSC resubmission.

**Audit findings — 4 🔴, 4 🟡, 8 🟢.** Critical gaps: (1) No Highwire Press `citation_*` meta tags on `/articles/[id]` — Google Scholar's sole indexing signal; without them Scholar will never crawl the article regardless of how long it's live. (2) Dynamic article URLs absent from `sitemap.xml` — sitemap was sync/static, never queried Supabase; Googlebot not scheduled to crawl the article page. (3) No `MedicalScholarlyArticle` JSON-LD — AI citation tools (Perplexity, ChatGPT, Gemini) have no machine-readable metadata. (4) `elocation_id` NULL in DB — `citation_firstpage` would emit empty until Kanwar runs a SQL fix. Attention items: OG image absent from article pages; Bing Webmaster Tools never set up; GSC sitemap stale; AI citation audit stub exists in vault but never filled. Clean: robots.txt allows `Googlebot-Scholar`; canonical tags present; homepage `#periodical` + `#organization` JSON-LD present; CC BY 4.0 license metadata; PDF publicly accessible; HTTPS; existing OG/Twitter card tags.

**Franklin commit `9f590f2` — 2 files.** (1) `app/articles/[id]/page.tsx` — `generateMetadata()` extended with full Highwire Press tag set: `citation_title`, per-author `citation_author` (array value → multiple `<meta>` tags via Next.js `other: Record<string, string | string[]>`), `citation_journal_title`, `citation_publisher`, `citation_volume`, `citation_issue`, `citation_pdf_url` (pointing at `/api/articles/${id}/pdf` proxy), `citation_fulltext_html_url`, `citation_publication_date` (YYYY/MM/DD from `published_date`), `citation_firstpage` (from `elocation_id`), `citation_abstract`, per-keyword `citation_keyword` (array). Added `MedicalScholarlyArticle` JSON-LD block to page JSX (inside `<script type="application/ld+json" dangerouslySetInnerHTML={...}>`): `@id`, `headline`, `url`, `datePublished`, `description` (abstract slice 0-300), per-author objects with `affiliation` + `sameAs: orcid.org/{id}`, `publisher: { '@id': '#organization' }`, `isPartOf: { '@id': '#periodical' }`, `isAccessibleForFree: true`, `license`, `about` (subspecialty as `MedicalCondition`), `pagination`, `keywords`. (2) `app/sitemap.ts` — converted from sync to `async function sitemap(): Promise<MetadataRoute.Sitemap>`, added `createAdminClient()` query on `manuscripts` for all `status='published'` rows (`id`, `updated_at`), with `.returns<{ id: string; updated_at: string | null }[]>()` type override to fix TypeScript `never` inference on partial `.select()`. Maps each row to a `changeFrequency: 'monthly'`, `priority: 0.9` entry at `${baseUrl}/articles/${m.id}`. Try/catch ensures static-only fallback if Supabase unavailable at build time.

**Production verification.** Post-deploy `curl`: `grep citation_title` returned the article title in a `<meta>` tag. JSON-LD block visible in page source. `https://www.oscrsj.com/sitemap.xml` now includes `<loc>https://www.oscrsj.com/articles/f8596ce8-...</loc>`.

**GSC sitemap resubmission.** Confirmed no manual Google Scholar notification form exists — Scholar indexing is fully crawler-driven once technical prerequisites (citation_* tags, public PDF, robots.txt allowing `Googlebot-Scholar`) are in place. Navigated to GSC Sitemaps page via Chrome MCP. `form_input` tool set the URL field value but didn't fire React synthetic events; used `computer` screenshot + coordinate-based `left_click` at `[1366, 247]` to click SUBMIT. Confirmed success: "Sitemap submitted successfully" toast appeared; last-submission date updated to May 22, 2026.

**Self-improvement notes.** `form_input` doesn't fire React onChange — when GSC/similar React apps don't enable the Submit button after `form_input`, take a screenshot first to check if the button is already clickable (React may pre-validate), then use `computer left_click` at its coordinates. Zsh comment-in-paste issue (Session 70 §11 codification) confirmed again — curl verification commands with `# N.` style comments caused `zsh: command not found: #`; the push itself succeeded.

Full narrative at [docs/session-history.md#session-71-article-discoverability-audit](docs/session-history.md). **Handoffs pushed: None.**

### Session 70 — 2026-05-21 — Sushant Cowork — Cleanup-pane Publish button fix: force renderer production mode via `launchd-start.sh` + dev-mode warning banner — 1 renderer commit `6078130` + 1 OSCRSJ commit `9c3db3f`

Closed the Session 62 publication-arc follow-up "Diagnose cleanup pane Publish button UI bug." Symptom: clicking the green "Publish manuscript…" button at the renderer's `/render/[id]` cleanup pane (via Tailscale Funnel URL `https://macbook-pro-5.tail55b3cd.ts.net`) produced no visible response — modal didn't open. Kanwar had been working around it for two sessions by running `scripts/publish-one.mjs <id>` from terminal.

**Diagnostic breakthrough — `curl` the URL.** After reading all 3 cleanup-pane files and confirming the click handler + modal markup were structurally correct, ruled out the CLAUDE.md follow-up's hypothesis (React Strict Mode + Turbopack file-confusion) and ran `curl -s https://macbook-pro-5.tail55b3cd.ts.net/`. HTML response ended with `"b":"development"` (Next.js 16 runtime mode marker) and preloaded `[turbopack]_browser_dev_hmr-client_hmr-client_ts` + `next-devtools`. **Diagnosis lock-in: renderer was running `next dev` (Turbopack + HMR) instead of `next start`.** Tailscale Funnel cannot proxy WebSockets, so the HMR client never completes handshake. In Next.js 16 + React 19 + Turbopack dev mode, when HMR is broken, the next-devtools client fails to attach React event handlers to *some* client components — the textarea + preview iframe kept working (their handlers attached, that's why Kanwar didn't see a global break), but the Publish button's onClick listener silently failed to bind.

**Why production wasn't running despite the launchd plist.** Session 58 installed `~/Library/LaunchAgents/com.oscrsj.renderer.plist` to auto-start production. Session 64's wrap-up noted that a manual `npm run dev` was winning the port-3001 race against the plist's `npm start` — dev server stayed up; production never bound. The CLAUDE.md §11 follow-up "Verify launchd auto-start" had been effectively unresolved for ~10 days.

**Renderer commit `6078130` — 2 files, +99/-0.** (1) `scripts/launchd-start.sh` (NEW — existed since Session 58 but never committed): patched to evict stale port-3001 processes BEFORE starting production. `lsof -ti :3001 | xargs kill -9` displaces them, `sleep 1` lets the OS release the binding, belt-and-suspenders `pkill -9 -f 'next dev'` catches dev-servers on other ports. (2) `app/render/[id]/CleanupPane.tsx` (+25 LoC): red dev-mode warning banner renders above the publish controls when `NODE_ENV !== 'production'`, with the exact `launchctl unload && load` reload command + `publish-one.mjs` workaround. If the bug regresses it's diagnosable at-a-glance.

**OSCRSJ commit `9c3db3f` — bookkeeping bundle.** (a) CLAUDE.md §11: marked the Session 62 follow-up ✅ done with full provenance + diagnosis chain; added 3 new follow-ups (push renderer commit, activate fix on Mac, visual-verify the banner). (b) CLAUDE.md §9 + docs/session-history.md: bundled Session 67's narrative orphan with explicit attribution (Session 67's 4 commits had landed in git but the M1-M4 narrative was uncommitted working-tree content; Convention §6 working-tree-orphan refinement applies).

**Five-round activation on Kanwar's Mac.** (1) Initial command block pasted; `git push` returned "Everything up-to-date" (commit had landed via prior path). (2) Post-launchctl-reload curl STILL showed dev mode — pkill killed the original dev server but something restarted it. (3) Broader diagnostic block surfaced PID 57177 (`next-server v16.2.4`) reading from `.next/dev/build/postcss.js` — definitively dev mode. (4) **After pkill cleared the port (`PORT_FREE`), the launchd plist's wrapper script started failing repeatedly with `/bin/bash: /Users/parhar/Documents/oscrsj-renderer/scripts/launchd-start.sh: Operation not permitted`** — macOS refused to execute the script. Hypothesis: FUSE-mounted file owned by sandbox user interacts with macOS Sequoia/Tahoe's stricter Documents-folder TCC restrictions on launchd's exec context. (5) Pivoted to launchd-bypass: `launchctl unload && cd ~/Documents/oscrsj-renderer && chmod +x scripts/launchd-start.sh && xattr -c scripts/launchd-start.sh && npm run build && npm start`. Build clean in 1912ms + TypeScript 1346ms; `next start` Ready in 70ms. Post-fix curl returned zero `hmr-client`, zero `next-devtools`, zero `"b":"development"` markers — production confirmed.

**Trade-off accepted.** Renderer now runs foreground in a Terminal tab; closes when tab closes or Mac reboots. Kanwar re-runs `npm run build && npm start` post-reboot. Permanent launchd fix filed as §11 follow-ups (three plausible paths: copy script outside FUSE, grant launchd Full Disk Access, inline-bash in plist).

**Self-improvement: codify "curl + grep `"b":"development"` is the first diagnostic for any Funnel-served / Vercel-served UI button bug"** — saved hours of speculative debugging. Full narrative at [docs/session-history.md#session-70-cleanup-pane-publish-button-fix](docs/session-history.md). **Handoffs pushed: None.**

---

## Recent Sessions (pointers)

Sessions from the last 14 days as 1-line pointers (newest first). Older sessions reachable via grep on the archive.

- **Session 69 — 2026-05-21 — Sushant Cowork — C1/C3 post-publish hotfixes (handling editor name + verapdf false-positive) + article 1 re-publish — 1 renderer commit `8f1888b`** — see [docs/session-history.md#session-69-c1-c3-hotfixes-republish](docs/session-history.md)
- **Session 68 — 2026-05-21 — Janine Cowork — First-publish compliance audit of e0001 (3 🔴 C1/C2/C3 found, C2 resolved same-session) + /faq CC BY-NC-ND → CC BY 4.0 fix + ISSN Application Prep vault file** — see [docs/session-history.md#session-68-janine-first-publish-compliance-audit](docs/session-history.md)
- **Session 67 — 2026-05-21 — Sushant Cowork — Full public article display pipeline: PDF proxy + figure proxy + article landing page + in-press status fix + homepage wiring; 4 OSCRSJ commits `8a55c08` + `6218853` + `4006ab6` + `a77920d`** — see [docs/session-history.md#session-67-public-article-display-pipeline](docs/session-history.md)
- **Session 66 — 2026-05-20 — Franklin Cowork — Narrative Review category launch (Phase 1: site copy sweep across 4 author-facing pages + CC BY-NC-ND → CC BY 4.0 bundled); 1 OSCRSJ commit `d1207bc`** — see [docs/session-history.md#session-66-narrative-review-site-copy](docs/session-history.md)
- **Session 65 — 2026-05-20 — Sushant Cowork — Desk reject promoted to primary radio option in editorial decision composer; 1 OSCRSJ commit `d09e541`** — see [docs/session-history.md#session-65-desk-reject-primary-radio](docs/session-history.md)
- **Session 64 — 2026-05-19 — Sushant Cowork — Phase 2 HTML body editor (TipTap MVP) + migration 024 (`manuscript_body_cleaned_html`); 1 OSCRSJ commit `a01ef4e`** — see [docs/session-history.md#session-64-phase-2-html-body-editor](docs/session-history.md)
- **Session 63 — 2026-05-19 — Franklin Cowork — /scholars program overview rewrite — 2026 NRMP ortho match data cited + structured "Who should apply" section + trimmed peer-review/cohort framing; 1 OSCRSJ commit `dfd5bd0`** — see [docs/session-history.md#session-63-scholars-overview-nrmp-rewrite](docs/session-history.md)
- **Session 62 — 2026-05-19 — Sushant Cowork — Publication arc: extractBody wired into publish endpoint + tier-2 figure-zip + tier-3 Supabase-Storage figure fallback + headless `publish-one.mjs` ops tool; 3 renderer commits `fb914ae` + `d44ef23` + `4b49660`; OSCRSJ Volume 1 Issue 1 e0001 published 2026-05-19 (first article in journal history)** — see [docs/session-history.md#session-62-publish-extractbody-figure-zip](docs/session-history.md)
- **Session 61 — 2026-05-19 — Franklin Cowork — Scholars apply form trim (ORCID + Why OSCRSJ + References removed; acknowledgement softened) + Google Sheets pipeline shipped end-to-end via Chrome MCP; 2 OSCRSJ commits `4cff302` + `e31b020` + migration 023 + live production smoke** — see [docs/session-history.md#session-61-scholars-form-trim-sheets-pipeline](docs/session-history.md)
- **Session 60 — 2026-05-18/19 — Sushant — Preview shows full article (body + figures + references) via new `extractBody.ts` module; 3 JATS validation fixes + Pandoc AST walker + figure-numerical-order invariant + Zotero/CSL ref-stripper; 1 renderer commit `182d884` (~1500 LoC) + 1 OSCRSJ commit `22a9d90`** — see [docs/session-history.md#session-60-preview-shows-full-article](docs/session-history.md)
- **Session 59 — 2026-05-18 — Franklin — IMG Scholar tier-card hidden on /scholars/apply (1 OSCRSJ commit `a30ffb4`; cross-session CLAUDE.md orphan bundled with attribution)** — see [docs/session-history.md#session-59-img-scholar-tier-card-hidden](docs/session-history.md)
- **Session 58 — 2026-05-16/17 — Sushant — Phase 1.5 docx-extract pre-fills shipped + Tailscale Funnel deployed (Vercel→localhost reach gap fixed); 1 renderer commit + 1 OSCRSJ PR merged; extraction verified end-to-end on live oscrsj.com** — see [docs/session-history.md#session-58-docx-extract-tailscale-funnel](docs/session-history.md)
- **Session 57 — 2026-05-15/16 — Sushant — Pre-Render Metadata Editor shipped end-to-end (Phases 1.A–1.C + 1.B+ collapsibles); 4 OSCRSJ commits + 1 renderer commit; both PRs merged to main; 4 outbound handoffs** — see [docs/session-history.md#session-57-pre-render-metadata-editor-shipped](docs/session-history.md)
- **Session 56 — 2026-05-15 — Sushant — First-publish operational push (envs + Supabase creds + workspace.ts bucket-name fix) + Pre-Render Metadata Editor project planned (2 handoffs)** — see [docs/session-history.md#session-56-pre-render-metadata-editor-planned](docs/session-history.md)
- **Session 55 — 2026-05-15 — Franklin — Chingiz Alizade portrait swap (.jpg → .png) + schema photo path flip** — see [docs/session-history.md#session-55-chingiz-alizade-portrait-swap](docs/session-history.md)
- **Session 54 — 2026-05-15 — Sushant — Renderer payload synthesizer + manual go-live gate landed; OSCRSJ + renderer companion commits; first manuscript publish unblocked** — see [docs/session-history.md#session-54-renderer-payload-synthesizer](docs/session-history.md)
- **Session 53 — 2026-05-15 — Franklin — SR/MA Wave 1 site-verification GATE CLEARED — `/peer-review` + `/guide-for-authors` now state "double-blind peer review by reviewers outside of our network"** — see [docs/session-history.md#session-53-srma-site-verification-cleared](docs/session-history.md)
- **Session 52 — 2026-05-14 — Franklin — /templates Revision Resources rewrite (Response Template .docx removed; replaced with Comment/Response/Changes structure cards)** — see [docs/session-history.md#session-52-templates-revision-resources-rewrite](docs/session-history.md)
- **Session 51 — 2026-05-14 — Sushant — Reviewer-feedback Word doc attaches to Minor/Major Revisions decision emails (orphan-cluster recovery + lib/admin/actions.ts wiring)** — see [docs/session-history.md#session-51-reviewer-feedback-attachment](docs/session-history.md)
- **Session 50 — 2026-05-14 — Franklin — Sitewide article-type taxonomy: 6-type canonical list with SR/MA replacing invited Review Articles + /aims-scope full review + stale CC BY-NC-ND fix** — see [docs/session-history.md#session-50-article-type-taxonomy](docs/session-history.md)
- **Session 49 — 2026-05-11 — Franklin — /aims-scope six article types + sitewide audience-reframe sweep + Sivaramakrishnan thin-bio noindex** — see [docs/session-history.md#session-49-aims-scope-audience-reframe-thin-bio](docs/session-history.md)
Sessions older than 14 days (Sessions 48, 47, 46, 45, 44, and earlier — newly dropped from §10 in this wrap: Session 48 Franklin 2026-05-06 editorial board roster expansion + Session 47 Sushant 2026-05-06 JATS Publishing 1.3 XML capability. Plus prior drops Sessions 46 + 45 + 44 + 43 — 2026-05-05 Guide for Reviewers rebrand + 2026-05-05 daily editorial-ops digest cron + 2026-05-04 admin reviewer-pool unified roster + 2026-05-04 password-reset pipeline + 2026-05-03 John social-media integration + 2026-05-03 Manvir SEO+a11y trio + Session 40 + 2026-05-02 intervening commit `fb43dc2` template-formatting-locks + 2026-05-01 Parmida/Yash headshot extraction + Session 39 Sprint 1 SEO bundle + the 2026-04-26 Sushant cluster + Sessions 22–38) are archived in [docs/session-history.md](docs/session-history.md) — `grep '#session-NN-slug' docs/session-history.md` or scan with `grep '^### Session' docs/session-history.md`.

---

## Open Follow-ups

Live punch list. Items resolve as Kanwar (or downstream agents) confirm completion; the archive entry for the surfacing session preserves the provenance.

- [x] ~~**Run Migration 020 in Supabase Studio**~~ — ✅ done 2026-05-06 same-session (Kanwar confirmed via Supabase Studio). Column `manuscripts.jats_xml_storage_path TEXT` is live in prod.
- [x] ~~**GitHub closeout for renderer**~~ — ✅ done 2026-05-06 same-session. Renderer pushed to https://github.com/kanwarparhar/oscrsj-renderer (private, Session 17/18/20 commits all on `origin/main`). Closes Session 18 deferral.
- [x] ~~**Franklin — Verify `/peer-review` + `/guide-for-authors` state "double-blind peer review by reviewers outside our network"** before SR/MA Wave 1 send.~~ — ✅ done 2026-05-15 (Session 53, Franklin Cowork). Commit `a81dd05` on `main`: `/peer-review` (metadata + OG + Double-Blind Peer Review intro + Step 2 Reviewer Assignment + FAQ JSON-LD Q1/Q2) and `/guide-for-authors` (SR/MA definition + FAQ JSON-LD + new `Peer Review` general-sections entry, anchor `#peer-review`) all explicitly state "double-blind peer review by reviewers outside of our network" + "recruited from outside the OSCRSJ editorial board." TypeScript clean. SR/MA Wave 1 site-vs-email consistency gate cleared; Manvir notified via `^handoff-srma-wave1-site-verification-cleared-2026-05-15`.
- [ ] **JATS DTD fetch script + validator path hotfix (renderer repo)** — three coordinated bugs surfaced when Kanwar tried to fetch the JATS 1.3 DTD bundle on his Mac, post-Session-47-ship: (a) `scripts/fetch-jats-dtd.sh` baked URL `https://jats.nlm.nih.gov/publishing/1.3/JATS-Publishing-1-3-DTD.zip` returns 404 — NCBI doesn't ship a single zip there, they ship a directory of variant-specific bundles at `https://public.nlm.nih.gov/projects/jats/publishing/1.3/`. (b) The actual main DTD inside the OASIS+MathML3 bundle is named `JATS-journalpublishing-oasis-article1-3-mathml3.dtd`, not `JATS-journalpublishing1.dtd` (older naming convention). (c) Both `lib/renderer/jatsValidate.ts JATS_DTD_DEFAULT_PATH` and `lib/renderer/jatsXml.ts` DOCTYPE `JATS_DTD_SYSTEM_ID` reference the older filename. Kanwar's local install works via a `JATS-journalpublishing1.dtd → JATS-journalpublishing-oasis-article1-3-mathml3.dtd` symlink workaround, but a fresh clone of the renderer to another Mac (or CI) will fail until this lands. ~10-min Sushant hotfix: rewrite `fetch-jats-dtd.sh` to `wget --recursive --no-parent --no-host-directories --cut-dirs=4 -e robots=off -R "index.html*" https://public.nlm.nih.gov/projects/jats/publishing/1.3/` then `unzip JATS-Publishing-1-3-OASIS-MathML3-DTD.zip` then flatten the resulting `JATS-Publishing-1-3-OASIS-MathML3-DTD/` subdirectory; update `JATS_DTD_DEFAULT_PATH` constant + `JATS_DTD_SYSTEM_ID` constant + the DOCTYPE template literal to use `JATS-journalpublishing-oasis-article1-3-mathml3.dtd` consistently. Bonus: capture canonical SHA256 of the OASIS-MathML3 zip after first run and fold into the script for future-fetch integrity verification. Surfaced Session 47 wrap-up.
- [ ] **Smoke renderer end-to-end on Mac** — `npm run dev` from `~/Documents/oscrsj-renderer/`, exercise `/smoke/sample-payload`, `/smoke/sample-payload-8-authors`, `/smoke/sample-payload-reused-figure`. DTD bundle is installed locally via Session 47 wget+symlink workaround; smoke confirms all 3 fixtures produce DTD-clean JATS (acceptance criteria AC1-AC2 of the spec). Carries the only remaining acceptance check from Session 47's 13-criteria list. Surfaced Session 47.
- [x] ~~**Correct stale CC BY-NC-ND mention** in CLAUDE.md `## Business Context`~~ — ✅ done 2026-05-11 (Janine Cowork — opportunistic during status-check session). Line now reads "License: CC BY 4.0 (reverted 2026-05-04...)" with provenance link.
- [ ] **Visual spot-check Session 49 surfaces post-Vercel-deploy** (~60s after `e4affb9` push) — `/aims-scope` (six items render cleanly in bullet list + trailing Guide for Authors link works), `/editorial-board` (new Founding Editor card blurb + page subtitle + Join CTA), `/guide-for-authors` (subtitle one-liner), `/news` + `/subscribe` (audience-reframe lines), `/manifest.webmanifest` (PWA description) at three breakpoints — surfaced Session 49
- [ ] **Verify `/editorial-board/adithyaa-sivaramakrishnan` emits noindex post-deploy** — `view-source:` should contain `<meta name="robots" content="noindex,follow">`; URL should drop from `https://www.oscrsj.com/sitemap.xml`. Auto-flips back to indexable when his BOARD_MEMBER_BIOS entry gets fleshed out — surfaced Session 49
- [x] ~~**Route 4 orphan working-tree files to owner-agent**~~ — ✅ resolved 2026-05-14 (Session 50). Cluster preserved as patch at `.cowork-push/session-49-50-orphan-decision-composer-feature.patch` (492 lines covering DecisionComposerPanel + 2 editorial decision email templates). `lib/reviewer-feedback/` untracked dir kept intact on disk. The 3 orphan code files reverted to HEAD via `git show + mv` workaround so this session's tsc passed; `lib/admin/actions.ts` orphan additions (import, interface field, 2 in-function feedbackAttachment blocks, big new `previewReviewerFeedback` + `resolveReviewerFeedbackAttachment` exports) were already wiped during my TYPE_LABELS revert — reconstructable by Sushant from the email-template prop shape + DecisionComposerPanel call shape. New Sushant handoff filed (see entries below).
- [x] ~~**Sushant — Restore decision-composer feature from orphan patch**~~ — ✅ done 2026-05-14 (Session 51). Commit `3c3d8f5` on `main`: patch applied for the 3-file UI + email-template cluster; `lib/admin/actions.ts` wiring rebuilt from scratch per Session 50 spec (import of `buildReviewerFeedbackDocx`, `reviewerFeedbackOverride` field, `REVIEWER_FEEDBACK_MAX_BYTES` constant, `resolveReviewerFeedbackAttachment` + `previewReviewerFeedback` exports, attachment threading + `hasReviewerFeedbackAttachment` flag in both Minor/Major Revisions branches, `reviewer_feedback_attachment_resolved` audit-log row). `lib/reviewer-feedback/build.ts` added to git. TypeScript clean.
- [ ] **Sushant — PRISMA hard-gate enforcement** — Session 50 promoted PRISMA from "recommended" to "required" copy on `/aims-scope` + `/guide-for-authors` + `/templates` + `/faq`. Site-vs-enforcement gap until backend wizard lands. Required work: (a) migration adding `prisma_checklist` value to FileType enum (mirror of Session 43's `care_checklist` + `jbi_case_series_checklist` migration 019); (b) Step 2 wizard validation update — block Submit if `manuscript_type === 'review_article'` and no `prisma_checklist` file uploaded; (c) submission portal copy update on Step 2 to surface the new required slot; (d) update `supabase/migrations/019_care_jbi_checklist_file_types.sql` comment (the comment still lists review_article as a type WITHOUT a mandatory checklist — stale once PRISMA enforcement lands). Estimated effort ~3 hours mirroring Session 43 scope. Surfaced Session 50
- [x] ~~**Janine — Stale CC BY-NC-ND on /templates + /faq**~~ — ✅ fully resolved across 3 sessions: (a) `/templates` Publication License section → Session 66 commit `d1207bc`; (b) `/faq` "Is OSCRSJ open access?" + (c) `/faq` "Can I share..." → Session 67 commit `6218853` (Janine working-tree orphan bundled per Convention §6). All CC BY-NC-ND references across the site now read CC BY 4.0. Surfaced Session 50
- [ ] **End-to-end smoke test reviewer-feedback Word-doc attachment** — pick (or seed) a manuscript in `status='under_review'` with ≥1 submitted non-draft review carrying `comments_to_author` text. (a) Admin manuscript page → draft Minor Revisions decision → verify the "Reviewer feedback attachment" card renders below the deadline picker. (b) Click "Preview / download attachment" → verify the `.docx` downloads + opens cleanly in Word with the OSCRSJ title block + per-reviewer "Reviewer 1" / "Reviewer 2" Heading 1 sections + correct body paragraphs. (c) Submit the Minor Revisions decision → verify the corresponding-author receives the email with the `.docx` attached + the body copy references the attachment + the `audit_logs` table has a `reviewer_feedback_attachment_resolved` row with `source: 'auto'`. (d) Repeat for Major Revisions decision. (e) Override path: download the auto-generated `.docx`, edit one sentence in Word, save, re-upload via "Upload edited version", submit decision → verify the override `.docx` attaches instead + audit-log row shows `source: 'override'`. (f) Empty-state: pick a manuscript without any submitted reviews → verify the card shows the amber "No reviewer comments are available" notice + decision still sends without attachment + audit-log row shows `source: 'none'`. Surfaced Session 51
- [ ] **Visual spot-check Session 51 surfaces post-Vercel-deploy** (~60s after commit pushes) — admin `/dashboard/admin/manuscripts/[id]` for any in-review manuscript: draft Minor Revisions or Major Revisions decision, verify the "Reviewer feedback attachment" card renders only when the decision is Minor/Major Revisions (NOT for Accept/Reject/Desk Reject); confirm the Preview button + Upload + Clear override interactions render at expected widths at three breakpoints. Surfaced Session 51
- [ ] **Codify "Outbound email anchor grep-verification" convention §3** (vault-side and CLAUDE.md `# Conventions`) — every email template that hardcodes a URL fragment (`/templates#revision-resources`, `/dashboard#submissions`, etc.) must be grep-verified pre-flip; if the anchor moves, the email and the page must update in lockstep. Surfaced Session 51 by the Revision Response Template link flip from `/downloads/oscrsj-revision-response-template.docx` to `/templates#revision-resources`. Suggested wording: "Outbound email anchors must be grep-verified pre-flip. If a transactional email links to `/path#anchor`, the matching `id="anchor"` must exist; rename it without updating the email and recipients get a soft-404."
- [ ] **Codify "Cross-verify HEAD state during orphan recovery" convention §3** — during any orphan-recovery operation, file state must be verified via bash `git show HEAD:path | grep ...` BEFORE trusting earlier-turn `Read` tool output. The `Read` tool can return stale cached content during long sessions; bash `git show HEAD:` is the authoritative source of truth. Surfaced Session 51 (earlier-turn `Read` showed `hasReviewerFeedbackAttachment` already present on the email templates when HEAD lacked it entirely; bash verification corrected the diagnosis).
- [ ] **Visual spot-check Session 52 surfaces post-Vercel-deploy** (~60s after `846ac2a`) — load `/templates#revision-resources` and verify (a) the **How to structure your Response to Reviewers document** card renders cleanly with the code-style `<pre>` block (`Comment:` / `Response:` / `Changes:` in mono on cream-alt background), readable bullet list defining each label, and "No change + rationale-under-Response" convention text; (b) the **How to format your tracked-changes manuscript** card renders below with the RED font + YELLOW highlight callout preserved; (c) the right-rail download button is gone (no orphan `Download Response Template (.docx)` button); (d) layout flows at three breakpoints (narrow mobile, tablet, desktop) — the `<pre>` block doesn't overflow horizontally on mobile. Surfaced Session 52
- [ ] **Downstream-surface sweep — 4 references to the removed Revision Response Template** — Session 52 removed the `.docx` offering from `/templates#revision-resources` but 4 downstream surfaces still name the template by reference. Required edits in one coherent commit: (a) `lib/email/templates/editorialDecisionMinorRevisions.ts` lines 81 (HTML body) + 119 (plain-text body) — replace "Use the OSCRSJ Revision Response Template (worked example + pre-formatted response tables): https://www.oscrsj.com/templates#revision-resources" with a description of the Comment/Response/Changes structure pointing at `/templates#revision-resources` for the spec; (b) `lib/email/templates/editorialDecisionMajorRevisions.ts` lines 82 + 120 — same rewrite; (c) `app/dashboard/submit/Step2Files.tsx:193` Response-to-Reviewers `description` field — rewrite from "Use the OSCRSJ Revision Response Template (download from /templates) — it contains a worked example and pre-formatted response tables. Accepted formats: .docx or .pdf (max 20 MB)." to describe the three-label structure inline + drop the template reference; (d) `lib/reviewer-feedback/build.ts:228` author-facing italic note inside the blinded reviewer-feedback `.docx` — rewrite from "using the Revision Response Template available at oscrsj.com/templates" to "following the Comment/Response/Changes structure described at oscrsj.com/templates#revision-resources." Estimated ~15-min Franklin sweep. Surfaced Session 52
- [ ] **Cleanup — delete orphan `oscrsj-revision-response-template.docx` + build script** — after the downstream-surface sweep above lands, `public/downloads/oscrsj-revision-response-template.docx` (the `.docx` file itself) and `scripts/build-revision-response-template.py` (build script that generates it) become fully unreferenced. Safe to delete both via explicit-path `git rm`. Low priority — both files are harmless on disk if left, but cleanup removes a confusing "why does this exist?" question for future agents. Surfaced Session 52
- [ ] **Codify "Removed-artifact downstream-surface inventory" convention §3** (vault-side and CLAUDE.md `# Conventions`) — when removing a user-facing artifact (downloadable template, link target, brand asset, file from `public/`), grep for every surface that names it by reference BEFORE scoping the commit. Either bundle the rewrite into one coherent commit with permission, or flag every stale surface explicitly in the wrap-up so the user can decide. Silent half-fixes that leave emails or form descriptions pointing at deleted assets are worse than a slightly-broader commit. Suggested wording: "Before removing a user-facing artifact, run `grep -r '<artifact-name>'` across the repo. Every surface that names the artifact must either be updated in the same commit or flagged in the wrap-up — never leave a transactional email pointing at a deleted asset." Surfaced Session 52
- [ ] **Visual spot-check Session 50 surfaces post-Vercel-deploy** (~60s after commit pushes) — verify (a) `/aims-scope` shows 6-type list with SR/MA as item 3 + Purpose paragraph mentions all 6 types + License section says CC BY 4.0; (b) `/guide-for-authors` Article Types Comparison table column order matches the new article-type ordering (SR/MA between Case Series and Surgical Technique) + word limit row reads 2,000 / 3,000 / 3,500 / 1,500 / 500 / 600; (c) `/templates` Reporting Checklists section shows the PRISMA card with new "For Systematic Reviews & Meta-Analyses" heading + "Required for every Systematic Review & Meta-Analysis submission" copy; (d) `/dashboard/submit` Step 1 picker shows "Systematic Review & Meta-Analysis" in the type-picker radio list at position 3; (e) admin manuscripts list shows new label when a `review_article` manuscript is shown (no pre-launch manuscripts yet so this won't surface until first SR/MA submission). Surfaced Session 50
- [ ] **ManuscriptType DB enum value rename (low priority, cosmetic)** — DB column constraint still says `'review_article'`. Display label is now "Systematic Review & Meta-Analysis" everywhere. If we want column-value alignment, that's a migration: ALTER constraint, add new value `'systematic_review_meta_analysis'`, backfill any pre-launch rows (zero today), drop the old value. ~30-min Sushant work but no functional impact (display label is what users see). Defer until first published SR/MA exists in DB. Surfaced Session 50
- [ ] **Cosmetic — rename `oscrsj-template-review-article.docx` + `oscrsj-example-review-article.docx`** to `oscrsj-template-systematic-review-meta-analysis.docx` (+ example) — when a user downloads the SR/MA template they currently see "review-article" in the filename. Requires renaming files in `public/downloads/` + updating `app/templates/page.tsx` `templateFile` + `exampleFile` strings + updating `scripts/build-article-templates.py` and `scripts/build-article-examples.py` filename + function-name references. Low priority; cosmetic only. Surfaced Session 50
- [ ] **Codify "Stale Handoff Protocol"** in Organization Plan §7.6 (vault-side) — any inbound handoff older than 7 days gets a pre-flight `rg` scan against acceptance criteria BEFORE scoping; handoff status text is a starting hypothesis, not ground truth. Surfaced by Session 49 self-improvement note (audience-reframe handoff sat "🔲 not started" 16 days while ~70% was already done by Session 25's sitewide sweep)
- [ ] **Smoke-test contact form post-Vercel-deploy** — submit any test message via `/contact` and confirm it lands at oscrsjournal@gmail.com (not bouncing) — surfaced Session 46 — see [docs/session-history.md#session-46-guide-for-reviewers-rebrand](docs/session-history.md)
- [ ] **Delete the test reviewer application** — Supabase Studio → Table Editor → `reviewer_applications` → delete row where `email = 'kanwarparhar+oscrsj-smoke-2026-05-05@gmail.com'` — surfaced Session 46
- [ ] **Daily-digest manual-trigger smoke test** — `curl -H "Authorization: Bearer $CRON_SECRET" https://www.oscrsj.com/api/cron/daily-digest` — surfaced Session 45
- [ ] **Daily-digest non-empty test** — sign up a fake user via private window with throwaway email, re-run manual trigger curl, verify digest lands at oscrsjournal@gmail.com with subject `[OSCRSJ Daily] 1 registration` — surfaced Session 45
- [ ] **Set up Gmail filter** for subject `[OSCRSJ Daily]` to label "OSCRSJ ops" — surfaced Session 45
- [ ] **E2E test Invite flow on the unified reviewer pool** — click "Invite" on both Manvir (ACTIVE bucket) and Rawnak (APPROVED bucket) on a test manuscript; verify mode='email' + mode='application' dispatch paths both work — surfaced Session 44
- [ ] **Optional bulk `mailto:` sweep** across the 9 user-facing pages still pointing at dead-letter `@oscrsj.com` addresses (`/peer-review`, `/faq`, `/privacy`, `/terms`, `/accessibility`, `/dashboard/settings`, `/api/dashboard/export`, co-author-dispute final HTML, `/for-reviewers/apply` success-state) — surfaced Session 46
- [ ] **Google Workspace provisioning** for `editorial@`/`info@`/`submit@`/`waivers@oscrsj.com` — when ready, flip routing back to per-department aliases (or refactor to env-var-driven `lib/email/inboxes.ts` per Session 46 risk #1) — surfaced Session 46
- [ ] **DMARC `rua` flip at GoDaddy** — `_dmarc.oscrsj.com` aggregate-report destination still routes to kanwarparhar@gmail.com — optional consolidation to oscrsjournal@gmail.com — surfaced Session 46
- [ ] **Run the 5-surface manual AI citation audit** (~30 min) — fill 30 stubbed cells (ChatGPT / Claude / Perplexity / Bing Chat / Gemini × 6 queries) using capture template at `02 - OSCRSJ/Notes/2026-05-03 John — AI Citation Audit Baseline.md` §5 — surfaced Session 41
- [ ] **Decide on OSCRJ brand-collision response** — Manvir-territory: surface disambiguation paragraph on `/about` + homepage hero, OR alternateName-only path, OR wait until ISSN registers and resolve via Wikidata — surfaced Sessions 41, 42
- [ ] **End-of-Phase-3 smoke test** — single large testing pass covering all 4 decision types + rescind + revision submission + revision-deadline reminder + Major-Rev re-invite + bulk decision modal + Revisions card. Carried over from Session 14. — see [docs/session-history.md#session-14-batch-decisions-revisions-panel](docs/session-history.md)
- [ ] **Decide post-Phase-3 priority** — (a) Stripe payment integration (LLC-gated), (b) Phase 4 publishing pipeline kickoff, (c) full E2E auth retest, (d) reviewer recruitment push, (e) revision-metadata snapshot migration — surfaced Session 14
- [ ] **Full E2E auth retest** — long-overdue from Sessions 5-14
- [ ] **Custom auth domain `auth.oscrsj.com`** — runbook ready at `docs/supabase-custom-auth-domain.md`; gated on Supabase Pro upgrade decision — surfaced Session 8
- [ ] **Crossref membership decision** (~$275/yr) — DOI registration becomes the post-publish DOI flow — surfaced Session 18
- [x] ~~**First manuscript reaches `accepted` → wire payload synthesizer** for renderer~~ — ✅ done 2026-05-15 (Session 54). Synthesizer + GET payload endpoint + manual go-live gate live in production. Carried Session 18 → 47 → 54.
- [x] ~~**Submit sitemap to Google Search Console** at https://www.oscrsj.com/sitemap.xml~~ — ✅ done 2026-05-22 (Session 71, John Cowork). Submitted via Chrome MCP + computer-use coordinate click (GSC React form; `form_input` didn't fire synthetic events). "Sitemap submitted successfully" toast confirmed; last-submission date updated to May 22, 2026. Bundled with async sitemap fix that now dynamically includes all `status='published'` article URLs.
- [ ] **Wire up `/contact` + `/subscribe`** form backends fully (Session 27 wired contact + discount-inquiry; subscribe still static) — surfaced Sessions 27+
- [ ] **Sample/template articles** showing what good submissions look like — long-pending
- [ ] **Reviewer recruitment push** + first 3-5 submissions — gated on Kanwar bandwidth
- [ ] **Section Editors grid balance** at `sm:grid-cols-2` — count went 9 → 12 with Session 48 (Yash promoted in, Sukhman demoted out, Alejandro + Jean added). 12 = 6 even rows of 2, so the original odd-count last-row issue likely self-resolves. Verify on next visual pass — optional polish — surfaced Session 40, count updated Session 48
- [ ] **Visual spot-check Session 53 surfaces post-Vercel-deploy** (~60s after `a81dd05` push) — `/peer-review` Double-Blind Peer Review section paragraph + Step 2 "Reviewer Assignment" card body both render the new out-of-network language; FAQ JSON-LD on `/peer-review` validates clean via [Rich Results Test](https://search.google.com/test/rich-results); `/guide-for-authors#peer-review` new section renders at the correct anchor position with the new `Peer Review` jump-link chip visible in the "On This Page" bar (5 chips total now — narrow-mobile wrap behavior worth a glance); all four bullets of the new section readable at three breakpoints. Surfaced Session 53
- [ ] **Codify "Verification-handoff grep-first" convention §3** (vault + CLAUDE.md `# Conventions`) — when an inbound handoff frames as "verify X is stated on Y" / "confirm Z is true," run a grep against the acceptance criteria BEFORE scoping the session. `grep -i "<exact-phrase>" <path>` answers verification-shaped handoffs in one tool call, letting the agent decide between "close in 1 turn" vs "open commit work." Reading full pages before grepping is the wrong order for verification handoffs. The 3-day target slip on Session 53's 5-min `^handoff-srma-wave1-peer-review-site-verification-2026-05-12` (filed 2026-05-12, resolved 2026-05-15) is the canonical precedent — a single `grep -i "outside our network" app/peer-review/page.tsx app/guide-for-authors/page.tsx` on day-of would have returned the diagnosis instantly. Suggested wording for Conventions: "Verification-handoff grep-first. For any handoff framed as 'verify' or 'confirm,' run `grep -i "<exact-acceptance-phrase>" <target-paths>` BEFORE reading full files. The grep result answers the verification step; the file-read happens only if the fix step needs to scope a copy edit." Surfaced Session 53
- [x] ~~**Renderer payload synthesizer — wire `/api/publish/[id]` end-to-end** (Manvir handoff `^handoff-renderer-payload-synthesizer-2026-05-15`)~~ — ✅ done 2026-05-15 (Session 54, Sushant Cowork). OSCRSJ commit `1859d0c` on `main` + renderer commit `a31783d` on local `main`. `lib/publish/synthesize.ts` joins manuscripts + manuscript_authors + manuscript_affiliations + manuscript_metadata + users into Franklin v1.0 payload shape; GET `/api/publish/payload/[id]` Bearer-token endpoint; manual go-live gate via OSCRSJ-side `publishGoLive` / `unpublishManuscript` actions + `PublishPipelinePanel`. Renderer's `writePublishStatus` now writes paths only (status stays `accepted`); `app/render/[id]/page.tsx` fetches payload from OSCRSJ; CleanupPane surfaces synth warnings/errors banner. Structural diff against `sample-payload.json` fixture empty (all 15 top-level keys match). TypeScript clean both repos.
- [x] ~~**Push renderer commit `a31783d` from Mac terminal**~~ — ✅ done 2026-05-15 (Session 56). `git push origin main` from Mac terminal returned "Everything up-to-date" — commit was already on origin from a prior push.
- [x] ~~**Generate + set shared secret across both repos' envs**~~ — ✅ done 2026-05-15 (Session 56). `RENDERER_SHARED_SECRET` (`0fcdb493...`) + `NEXT_PUBLIC_RENDERER_URL=http://localhost:3001` set on OSCRSJ Vercel (Production + Preview). Renderer `.env.local` synced (`db70a0230f...` placeholder → `0fcdb493...` to match Vercel) + `OSCRSJ_BASE_URL=http://localhost:3000` → `https://www.oscrsj.com`. Note: `.env.local` lines 11-15 comment block on `OSCRSJ_BASE_URL` is misleadingly worded; cosmetic follow-up.
- [ ] **End-to-end smoke test the first publish — STEPS 1-3 DONE + extraction verified; STEPS 4-16 pure Kanwar-pacing** — Session 57 Phase 1.D: production admin page loads + 6 editor sections render + Validation Summary visible. Session 58 Phase 1.5.D: extraction works end-to-end on live `oscrsj.com` via Tailscale Funnel; full abstract sub-sections + HIGH-confidence chips populated against manuscript `f8596ce8`. Remaining steps 4-16: fill Running title (≤45 chars), Save (which persists `.docx`-extracted pre-fills back to DB), open Preview (now functional via Funnel — first time), render via renderer cleanup pane, Publish (go live). Pure user-pacing; no Sushant blockers. Carried Session 54 → 56 → 57 → 58.
- [x] ~~**Renderer `lib/renderer/workspace.ts` STORAGE_BUCKET fix uncommitted**~~ — ✅ done 2026-05-16 (Session 57, Phase 1.C). Folded into the Phase 1.C renderer commit alongside `xmpRights:WebStatement` + equal-contribution + handling-editor JATS emit + preview endpoint changes. Kanwar pushed from Mac terminal.
- [x] ~~**Pre-Render Metadata Editor & PDF Preview project — awaiting Janine + Franklin sign-offs**~~ — ✅ shipped end-to-end 2026-05-15/16 (Session 57). Janine + Franklin both signed off 2026-05-15 a week ahead of target. Phases 1.A (`cdfb0a3`) + 1.B (`07beb2a`) + 1.C (`ed6b168`) + 1.B+ collapsibles (`e1e423d`) all merged to main via PRs #1 + #2. Renderer companion commit on `~/Documents/oscrsj-renderer/main`. Migration 022 live in prod DB. Production admin page renders the editor per Kanwar's browser confirmation.
- [x] ~~**Decision needed on first manuscript publish — quick-fix or wait?**~~ — ✅ resolved 2026-05-16 (Session 57). Kanwar's first-touch UX review surfaced the deeper docx-extract architectural need; decision deferred to that follow-up. The metadata editor is built + working; first publish waits for one-time manual retype OR the docx-extract feature (Kanwar prefers the latter).
- [x] ~~**Sushant — Pre-Render Metadata Editor docx-extract follow-up (Phase 1.5)**~~ — ✅ done 2026-05-16/17 (Session 58, Sushant Cowork). Renderer commit `81d7985` on origin/main (`lib/renderer/extractMetadata.ts` + `app/api/extract-metadata/[id]/route.ts`). OSCRSJ commit `b0c0cf7` merged via PR #4 as `83eebe8` on main (`lib/publish/extractedMetadata.ts` + `app/api/extract-metadata/[id]/route.ts` proxy + `MetadataEditorPanel.tsx` server-side fetch + `MetadataEditorForm.tsx` 4-state decisions UI with confidence chips + divergence banners). Tailscale Funnel deployed to fix the latent Vercel→localhost reach gap (public URL `https://macbook-pro-5.tail55b3cd.ts.net` + launchd plist for renderer auto-start on Mac reboot + Vercel env updated). End-to-end extraction verified on live `oscrsj.com` against manuscript `f8596ce8`. TypeScript clean both repos. Closes `^handoff-sushant-docx-metadata-extract-2026-05-16`. NO LLM-assist this phase per Session 57 decision (regex-first heuristic + confidence scoring + manual fallback notice).
- [ ] **Sushant — Phase 1.5 auto-expand collapsed section on Jump-to-fix** — small follow-up surfaced Session 57 Phase 1.B+. Collapsed sections still respond to ValidationSummary's "Jump to fix ↗" anchor links by scrolling into view + ring-flash, but if the containing section is collapsed the target field isn't visible. ~5 LoC: in `MetadataEditorForm`'s `jumpToFix` handler, walk up the target element's parents looking for `[data-target="section-*"]`, derive the section id, call `toggleSection(id)` if currently collapsed. Defer until editors hit it in practice.
- [ ] **Sushant — Phase 2 transactional save via Supabase RPC** — `updateManuscriptMetadata` currently does sequential writes across manuscripts + manuscript_metadata + manuscript_authors (Supabase JS has no `.transaction()`). Partial-fail surfaces to UI with explicit error message; editor retries. Author replace pattern (delete + bulk insert) means insert failure after delete = data loss. Mitigation: write a Supabase stored procedure (`pg/plpgsql`) that wraps all 3 table updates + author replace in a single transaction; OSCRSJ calls via `supabase.rpc('update_manuscript_metadata', { ... })`. ~2 hours Sushant work. Defer until partial-fail surfaces in practice. Surfaced Session 57
- [ ] **Visual spot-check Session 57 surfaces post-Vercel-deploy** — load https://www.oscrsj.com/dashboard/admin/manuscripts/f8596ce8-2424-4a47-8537-5af9e8a2f5cd in browser; Cmd+Shift+R to bust cache; verify (a) 6 chevron-collapsible sections render with toggle working; (b) sticky desktop validation chip top-right shows live counts; (c) §5 Validation Summary at bottom shows the manuscript's current errors/warnings; (d) §6 Preview Render shows "Open preview ↗" button (will fail from prod because Vercel can't reach localhost:3001 — that's expected per Risk #2); (e) sticky save bar at bottom renders + "Discard changes" + "Save changes" buttons disabled until isDirty; (f) form responsive at three breakpoints (narrow mobile read-only-ish, tablet single-column, desktop label-left/input-right). Surfaced Session 57
- [ ] **Smoke-test cleanup-preview-artifacts cron post-Vercel-deploy** — `curl -H "Authorization: Bearer $CRON_SECRET" https://www.oscrsj.com/api/cron/cleanup-preview-artifacts` should return 200 + `{ deleted_count: 0, retention_days: 7 }` on first run since no preview artifacts exist yet. After first preview render lands an artifact in `submissions/previews/{id}/{iso}.pdf`, the cron will start reaping on the 8-day mark. Surfaced Session 57
- [ ] **Audit production manuscript_metadata for f8596ce8** via Supabase Studio — check what's actually in `conflict_of_interest` / `funding_sources` / `data_availability_statement` / `ethics_approval_number` / `ai_tools_used` / `patient_consent_*` columns. Informs the docx-extract follow-up scope (which fields are blank vs pre-filled in the editor right now → which fields the extractor needs to fill vs leave alone). Surfaced Session 57
- [x] ~~**Janine — End-to-end compliance review of first published artifact**~~ — ✅ done 2026-05-21 (Session 68, Janine Cowork). Closes `^handoff-janine-first-publish-compliance-review-2026-05-16`. 3 🔴 critical items found (C1 handling editor name, C2 contradictory consent, C3 verapdf false-positive) + 3 🟡 attention + 13 🟢 clean. All 3 🔴 items resolved in Sessions 68–69 + article 1 re-published. Audit note at vault `02 - OSCRSJ/Indexing/Manuscripts/0001 — OSCRSJ-2026-0001 [Case Report]/Janine First-Publish Compliance Audit.md`. Surfaced Session 57
- [ ] **Migration 021 + structured references collection** — references table doesn't exist; submission wizard never collected structured refs; Vancouver-style refs live inside manuscript .docx. Path to fix: (a) add migration 021 creating `manuscript_references` table (manuscript_id FK + ref_order + citation_text TEXT + doi TEXT NULL + pmid TEXT NULL); (b) admin paste-in form on `/dashboard/admin/manuscripts/[id]` to populate refs retroactively for accepted manuscripts; (c) future submission-wizard upgrade adds a Step 3.5 collector. Until this lands, JATS XML ships without `<ref-list>` element + PMC indexing remains blocked. PDF/A-1b is unaffected because Pandoc body content has inline `<sup>` citation markers. Surfaced Session 54
- [ ] **Structured affiliations backfill** — `manuscript_affiliations` table exists (migration 013) but is never populated by submission code. Synthesizer falls back to building affiliations from `manuscript_authors.affiliation` free-text strings (lossy for multi-affiliation authors — they get collapsed to single-affiliation `[1]`). For the first accepted manuscript, verify whether any authors are multi-affiliation; if so, run a one-off SQL INSERT into `manuscript_affiliations` for each unique (department, name, city, country) combo + update `manuscript_authors` rows to link via `manuscript_author_id`. Future submission-wizard upgrade should populate this directly. Surfaced Session 54
- [ ] **Codify "Two-app payload sync via shared-secret" convention §3** (vault + CLAUDE.md `# Conventions`) — Session 54 introduced the OSCRSJ ⇄ renderer payload sync pattern: OSCRSJ owns the synthesizer + payload GET endpoint; renderer fetches with `Authorization: Bearer ${RENDERER_SHARED_SECRET}` on page-load parallel-await. The shared secret must match in both `.env.local` files; rotation requires updating both. Worth documenting so future cross-repo auth (Janine's eventual hosted-renderer migration, Brad's Stripe webhook, etc.) doesn't reinvent the pattern. Surfaced Session 54
- [ ] **Verify launchd auto-start on next Mac reboot** — Session 58 installed `~/Library/LaunchAgents/com.oscrsj.renderer.plist` + wrapper script `~/Documents/oscrsj-renderer/scripts/launchd-start.sh`. Plist NOT loaded this session (existing `npm run dev` bound to :3001 conflicts with plist's `npm start`). On next reboot the plist auto-fires via RunAtLoad; if it never auto-starts cleanly the renderer is silently down + publishes fail. Verification: at next login, run `tail ~/Library/Logs/oscrsj-renderer/stderr.log` + `curl http://localhost:3001` should return 200; if broken, `launchctl unload && launchctl load ~/Library/LaunchAgents/com.oscrsj.renderer.plist` after fixing root cause from the log. Surfaced Session 58
- [ ] **Visual spot-check Session 58 surfaces post-Vercel-deploy** — load https://www.oscrsj.com/dashboard/admin/manuscripts/f8596ce8-2424-4a47-8537-5af9e8a2f5cd in browser; Cmd+Shift+R; verify (a) top-of-form banner shows green "✓ Pre-filled from accepted manuscript.docx via Pandoc + heuristic anchor parser" (NOT yellow "Could not pre-fill"); (b) §2 Abstract sub-section textareas all 4 populated with "Pre-filled from .docx (HIGH)" chips visible; (c) §4 CoI / IRB protocol / patient consent / acknowledgments textareas populated where extractable; (d) funding shows notice-only (not auto-filled), with "Dismiss" button on the notice; (e) clicking × on a chip dismisses it without clearing the field content; (f) Save button activates `isDirty` from mount (because extracted_applied counts as a draft change). Surfaced Session 58
- [ ] **Sushant — Body-structural validations in §5 Validation Summary** — small Phase 2 add (~30 min) — fold body word count vs article-type limit, figure count vs limit, table count, reference count, "References table is empty" into §5 amber warnings. Pull counts from Pandoc HTML output via extractMetadata or a sister endpoint. Body editing itself stays in the renderer cleanup pane (out of scope per Session 58 scope decision). Surfaced Session 58
- [ ] **Tier-2 renderer hosting migration (eventual)** — when 3-4 publishes have shipped + journal has revenue, decide on hosting renderer on Railway / Fly.io / Hetzner ($5-7/mo) so Mac doesn't need to be on for publishes. Required work: Dockerfile installing Pandoc + WeasyPrint via pip + verapdf + xmllint + JATS DTD bundle on top of Node 20; deploy from GitHub; flip Vercel `NEXT_PUBLIC_RENDERER_URL` to the public hostname; retire Tailscale Funnel + launchd plist. Renderer code requires no changes for the migration. ~1.5h Sushant Dockerfile + deploy work. Surfaced Session 58
- [ ] **Manually verify launchd plist load** — when convenient: stop the current `npm run dev` renderer; run `launchctl load ~/Library/LaunchAgents/com.oscrsj.renderer.plist` to confirm the wrapper script + `npm run build` + `npm start` chain works without waiting for next reboot. If it errors, log is at `~/Library/Logs/oscrsj-renderer/stderr.log`. Surfaced Session 58
- [ ] **Codify "Vercel→localhost reach gap" convention §3** (vault + CLAUDE.md `# Conventions`) — any Vercel server function fetching `http://localhost:*` is a silent misconfig (`fetch failed` errors with no other diagnostic). `NEXT_PUBLIC_RENDERER_URL=http://localhost:3001` works ONLY for client-side uses (browser fetches from a Mac that runs the renderer; `window.open` in new tabs). Server-side fetch from Vercel requires either a tunnel (Tailscale Funnel / Cloudflare Tunnel / ngrok) or hosting the service on a public platform. Pre-flight check: `curl -m 5 https://www.oscrsj.com/api/preview/<some-id>` from a NON-Mac network — a `localhost`-misconfigured endpoint returns 502/504 instantly. The trap caught Sessions 54 + 57 + 58 silently until Session 58 surfaced it on real-prod smoke. Surfaced Session 58
- [ ] **Codify "Tailscale Funnel as renderer tier-1" convention §3** (vault + CLAUDE.md `# Conventions`) — `https://macbook-pro-5.tail55b3cd.ts.net` is the current canonical Vercel `NEXT_PUBLIC_RENDERER_URL` value. Tied to Mac uptime + launchd service + Tailscale auth. If hostname or tailnet name changes, update Vercel env. Tier-2 upgrade path is renderer hosting on Railway/Fly/Hetzner; renderer code requires no changes for the migration. Both tiers interchangeable from OSCRSJ's perspective. Surfaced Session 58
- [ ] **Optional — delete duplicate Tailscale node `oscrsj-renderer`** — Session 58 setup created a userspace `oscrsj-renderer` node before consolidating on the Mac App's `macbook-pro-5` node. Userspace daemon is killed; node still appears in Tailscale admin until 90-day auto-prune. Manual delete: Tailscale admin panel → Machines → `oscrsj-renderer` → ⋯ → Remove. Cosmetic; no functional impact. Surfaced Session 58
- [ ] **Visual spot-check Session 59 surface post-Vercel-deploy** (~60s after `a30ffb4`) — load `https://www.oscrsj.com/scholars/apply`, select "IMG Scholar" track, confirm the prior 3-line card ("6-month program / $299 / 2-3 first-author projects...") is gone and the single-line notice "Program information will be sent after your application is complete." renders in its place inside the same `bg-cream-alt` border-rounded box. Other tracks (Pre-Med / Med Student) should still show their multi-tier radio selectors with tier labels + prices + summaries unchanged. Surfaced Session 59
- [ ] **Identify the Sushant Cowork session that left the CLAUDE.md Convention §3 orphan** — Session 59 bundled an in-flight working-tree modification to `CLAUDE.md` Conventions §3 (the "CLAUDE.md §9/§11 is authoritative current-state" rule, dated 2026-05-18) that another agent left uncommitted. Git blame will attribute it to Session 59's wrap commit. To retroactively attribute the convention line to its actual originating session, run `git log --since='2026-05-18 00:00' --until='2026-05-18 23:59' --all` + check any Sushant Cowork sessions that ran today but crashed before committing. If a session is identified, add a one-line clarification to docs/session-history.md Session 59 entry crediting the original author. Low priority. Surfaced Session 59
- [ ] **Codify "Working-tree orphan vs staged-index orphan" refinement to Convention §6** — Convention §6 (cross-session orphan-files protocol) frames the rule around "pre-staged" files (staged in the index). Session 59 hit a working-tree modification (not staged) and the rule's intent still applied but the friction was real: any session that writes to `CLAUDE.md` (every M1-M4 wrap-up) inherits any pending working-tree `CLAUDE.md` modifications from prior sessions, because Edit/Read can't see "just my changes" — it sees the whole file diff. Proposed §6 codification refinement: "When the current session must edit a file that already carries an uncommitted working-tree modification from another session, choose either (a) revert the orphan via `git checkout -- <path>`, do clean work, save the orphan content to `.cowork-push/<session-tag>.patch`, flag the owner; or (b) bundle the orphan into the current commit with explicit attribution in the session narrative. Discard is never correct." Worth adding to `# Conventions` if the pattern recurs. Surfaced Session 59
- [x] ~~**Push renderer commit `182d884` from Mac terminal**~~ — ✅ done 2026-05-19 early Session 62 (Kanwar ran the push at session start, confirmed via terminal screenshot showing `81d7985..182d884  main -> main` + `Resolving deltas: 100% (12/12), completed with 12 local objects`).
- [ ] **Visual spot-check Session 60 surface post-push** — load `https://www.oscrsj.com/dashboard/admin/manuscripts/f8596ce8-2424-4a47-8537-5af9e8a2f5cd` in browser; Cmd+Shift+R; click Open preview ↗; verify the 6-page preview includes: (a) title + abstract + license on page 1; (b) body sections (Introduction, Case Presentation with subsections, Discussion, Conclusion, Ethics statement) on pages 2-5 with paragraphs + tables; (c) figure placeholder boxes inline near first body mention, in numerical order (Fig 1 then Fig 2); (d) Declarations block at end of body 2-col; (e) References on their own 2-col page AFTER Declarations; (f) no blank trailing page. Surfaced Session 60
- [x] ~~**Sushant — Phase 2 HTML body editor in OSCRSJ admin metadata editor**~~ — ✅ MVP done 2026-05-19 (Session 64, Sushant Cowork). Commit `a01ef4e` on `main`: migration 024 (`manuscripts.manuscript_body_cleaned_html TEXT NULL`), TipTap-backed BodyEditor.tsx (~467 LoC) + BodyEditorPanel.tsx (~110 LoC) mounted between MetadataEditorPanel and PublishPipelinePanel, `saveManuscriptBodyCleanedHtml` server action in lib/admin/actions.ts with sanitize-html allowlist + 2MB cap + audit log, preview endpoint `/api/preview/[id]` reads the column and passes as `cleanedHtml` (NULL → Session 62 extractBody fallback), `/api/publish/payload/[id]` carries `manuscriptBodyCleanedHtml` forward-compatibly. Coexists with renderer cleanup pane per scope decision. Phase 2 deferrals filed as separate follow-ups below: figure-drag-to-position, native table cell editing, renderer cleanup pane seed from cleaned HTML, auto-seed editor from .docx, retire `/render/[id]`.
- [x] ~~**Sushant — Wire publish endpoint to call extractBody (parallel to preview)**~~ — ✅ done 2026-05-19 Session 62 (renderer commit `fb914ae`, sub-task 1). `/api/publish/[id]/route.ts` now mirrors the preview endpoint: when caller does not supply `cleanedHtml`, calls `prepareRenderWorkspace` + `extractBodyFromDocx` and injects body + references into the payload before `runPublishChain`. Cleanup pane workflow (existing `cleanedHtml` path) unchanged. Defensive payload patching: auto-extracted body/references only inject when synthesizer ships empty arrays. Bearer auth intentionally left off (cleanup pane is same-origin; tightening filed as new follow-up below). Pre-`fb914ae` commit hash was `6b4dc81` (amended for Session 62 number correction).
- [x] ~~**Sushant — Tier-2 figure embedding when Pandoc can't extract**~~ — ✅ done 2026-05-19 Session 62 (renderer commit `fb914ae`, sub-task 2). New `lib/renderer/extractFiguresFromZip.ts` — pure-Node ZIP reader, no new deps. Reads .docx zip directly via EOCD scan + Central Directory walk + Local Header parse + `zlib.inflateRawSync` (handles STORED + DEFLATE). Extracts `word/media/image*.{png,jpg,jpeg,gif,webp,bmp,tiff,svg}` as base64 data URIs. Skips EMF/WMF with author-actionable diagnostic. Integration in `extractBody.ts`: after Pandoc's `warmImageCache` pass, fills remaining null `image_src` by matching figure NUMBER (image1.png → block with number=1), robust to body-order variation. Diagnostics surface `zip-media-assigned=N unused-media=[...] figures-still-placeholder=[...]`. Smoke-verified end-to-end via pure-JS port against a real 3-image .docx (sandbox npm ENOSPC blocked tsx-based smoke).
- [ ] **Codify "Architectural cross-checks before scoping a small fix" convention §3** — Session 60 Cycle 4 surfaced that preview was metadata-only by architectural design (cleanedHtml vs payload.body split). Cost ~2 hours of iteration before the gap was visible. A pre-Cycle-1 read of `pandocHtmlToJatsBody` callers + the Jinja template's `{% for section in body %}` loop would have surfaced it immediately. Suggested wording: "Before scoping a 'small fix' to the preview or publish chain, grep for every `cleanedHtml` and `payload.body` reference in `lib/renderer/` + the Jinja template's body iteration. Verify what input each consumer reads — if a fix touches one input, confirm the consumer set you expect matches the actual consumer set." Surfaced Session 60
- [ ] **Codify "Test-fixture rot prevention" convention §3** — Sessions 47 + 57 added xmp.ts required fields (xmpRights_WebStatement) but the 3 smoke fixtures kept passing because they were exercised against the chain BEFORE the field became required (smoke runs ran during the same session that added the field, so the validator wasn't yet active). The fixture rot only surfaced Session 60 when re-smoking. Suggested wording: "When extending the chain with a new required field (sanity test, XMP field, payload key), update the 3 smoke fixtures in `public/template/v1.0/` IN THE SAME COMMIT. Verify by running all 3 fixture smokes against the live renderer post-edit." Surfaced Session 60
- [ ] **Codify "Verify with Janine before asserting indexing-policy facts" convention §3** — Session 60 Cycle 3: I asserted mid-session that "the 5-article rule is DOAJ's not ISSN's" — incorrect. Janine Agent.md line 79: "Library of Congress requires 5 published articles for OA journals before assigning ISSN." Kanwar's instinct was correct. Suggested wording: "Before asserting indexing-policy facts (ISSN, DOAJ, Crossref, PMC, MEDLINE, Scopus eligibility / requirements / timing), grep Janine Agent.md FIRST: `grep -i 'ISSN\|DOAJ\|Crossref\|PMC\|MEDLINE' '02 - OSCRSJ/Resources/Agents/Janine Agent.md'`. Her file is authoritative; agent intuitions about indexing requirements are not." Surfaced Session 60
- [ ] **Clean up Session 61 smoke test data in production** (~1 minute) — (a) delete Google Sheet rows 2-7 of the "Scholars Applications" tab (test rows from curl + Node fetch + form smoke during the build); (b) Supabase Studio → Table Editor → `cohort_applications` → delete rows matching `email = 'kanwarparhar+sheets-smoke-2026-05-19@gmail.com'` AND `email = 'smoke@test.local'`; (c) rename Google Sheet title from mangled "Untitled spOSCRSJ Form Submissionsreadsheet" to "OSCRSJ Form Submissions"; (d) right-click the empty `Sheet1` tab → Delete. Surfaced Session 61
- [ ] **Decide on `/scholars/apply` "What to expect" intro copy** — the landing page intro paragraph (`app/scholars/apply/page.tsx`, above the form component) still reads "the program promises **training, mentorship, and** structured projects, never publication itself" — same phrase Kanwar wanted stripped from the acknowledgement checkbox in Session 61. ~10-line edit if Kanwar wants it stripped from the intro too. Surfaced Session 61 (deferred decision)
- [ ] **Visual spot-check Session 61 surfaces post-Vercel-deploy** (~60s after `e31b020`) — `/scholars/apply`: (a) Personal Information section no longer shows ORCID iD input row (now 4 fields: First Name, Last Name, Email, Country); (b) "Tell us about yourself" section shows only 2 essays (no third "Why OSCRSJ specifically?" textarea); (c) References card entirely gone between CV upload and Acknowledgments; (d) Acknowledgments second checkbox reads "I understand that the program promises structured project opportunities — not a guarantee of publication..." (no "training, mentorship,"); (e) admin detail page at `/dashboard/admin/scholars/applications/[id]` still renders for existing rows but ORCID/Why-OSCRSJ/References sections render empty for new submissions (acceptable; cosmetic polish deferred). Surfaced Session 61
- [ ] **Optional rotate SHARED_SECRET** — I generated `01c12ece3cb00a3a7f1b34eb56e0dca70bd6465c0d92b2047a33a8180fd85da3` via sandbox `openssl rand -hex 32`. Functionally fine to keep. If Kanwar wants a self-generated value: run `openssl rand -hex 32` on his Mac, update BOTH Apps Script Script Property (`SHARED_SECRET`) AND Vercel env var (`GOOGLE_SHEETS_WEBHOOK_SECRET`) in lockstep, redeploy Vercel. Surfaced Session 61
- [ ] **Polish cosmetic — Scholars admin detail page conditional sections** — `app/dashboard/admin/scholars/applications/[id]/page.tsx` still renders the ORCID / Why-OSCRSJ / References sections unconditionally; they now show empty for new submissions (existing rows with old data render correctly). Add `{application.field && ...}` guards around the three sections so they hide cleanly when null/empty. ~10-line Franklin edit. Low priority. Surfaced Session 61
- [ ] **Codify "Apps Script Anyone web apps cannot read custom HTTP headers" convention §3** — body-based auth is the only viable path. Surfaced Session 61 during the Google Sheets pipeline build (`X-Shared-Secret` header was the initial draft; rewrote both client and script to carry secret in JSON body before deploying). Suggested wording: "Apps Script web apps with 'Anyone' access cannot read custom HTTP headers (Authorization, X-*, etc.). Carry auth tokens in the request body for `doPost(e)` to read via `e.postData.contents`. Never assume header reads will work for non-Google-account-authenticated Apps Script deployments." Surfaced Session 61
- [ ] **Codify "Apps Script /exec 302 drops POST body for curl but not Node fetch" convention §3** — Surfaced Session 61. Apps Script `/macros/s/.../exec` URLs respond with HTTP 302 redirecting to `script.googleusercontent.com/macros/echo?...` and curl's redirect-following (even with `-L --post301 --post302 --post303` flags) drops the POST body across the cross-domain hop. Node 18+ native fetch handles it cleanly. Suggested wording: "When smoke-testing Apps Script webhooks from sandbox, use `node -e 'fetch(url, {method: \"POST\", body: ..., redirect: \"follow\"})'`, NOT `curl --post302`. Curl's redirect-method semantics drop the body across cross-domain 302s. The server-side Vercel fetch is fine because it uses Node fetch."
- [ ] **Codify "Monaco editor setValue pattern" convention §3** — Surfaced Session 61. Apps Script editor (and any other Monaco-backed web editor — VS Code Web, GitHub's CodeMirror-on-Monaco, Notion code blocks) exposes `window.monaco` globally. Bulk-content paste via `monaco.editor.getEditors()[0].setValue(content)` is significantly faster + safer than character-by-character typing through computer-use (which risks autocomplete corruption). Pattern saves ~5 min per script paste. Suggested wording: "When automating any Monaco-backed web editor via Chrome MCP, prefer `javascript_tool` → `monaco.editor.getEditors()[0].setValue(content)` over `computer.type`. Bulk-content APIs are faster + avoid autocomplete-corruption risks."
- [ ] **Codify "OAuth popup escapes Chrome MCP tab group control" convention §3** — Surfaced Session 61. Google's OAuth consent flow + "unverified app" warning page opens in a separate browser window outside the Chrome MCP tab group. Authorization cannot be fully automated; manual user click-through is required (~30s for Advanced → Go to (unsafe) → Allow flow). Suggested wording: "Pre-warn the user before any Chrome MCP action that triggers an OAuth Authorize Access click. The consent popup opens in a separate window that Chrome MCP cannot reach; user must manually click through Google's unverified-app warning + Allow."
- [x] ~~**Push renderer commit `fb914ae` from Mac terminal**~~ — ✅ done 2026-05-19 mid-session (Kanwar pushed); see screenshot of `81d7985..182d884  main -> main` output earlier in session. (Note: hash amended from `6b4dc81` to `fb914ae` after the in-session commit-message correction renumbering Session 61→62.) Subsequent renderer commits `d44ef23` (Storage-fetch tier-3) + `4b49660` (publish-one.mjs script) still pending push as part of the publication-arc follow-up below.
- [x] ~~**End-to-end smoke against f8596ce8 — Session 62 figure-zip extraction verification**~~ — ✅ done 2026-05-19 same-session (Session 62 hotfix `d44ef23`). Initial Session 62 commit `fb914ae` (tier-2 .docx zip read) returned `zip-media: 0 images in word/media/` — diagnostic confirmed the .docx had ZERO embedded images. Root cause: OSCRSJ submission workflow uploads figures as SEPARATE files (manuscript_files rows with file_type='figure' in the 'submissions' Storage bucket), not embedded in the blinded_manuscript .docx. Hotfix `d44ef23` shipped tier-3 Storage-fetch fallback (new `lib/renderer/fetchFiguresFromStorage.ts` ~150 LoC + extractBody.ts integration + preview/publish endpoints pass manuscriptId). Kanwar confirmed real MRI images now embed correctly in preview. Three-tier resolution order: (1) Pandoc media extract, (2) direct .docx zip read, (3) Supabase Storage fetch — the OSCRSJ workflow uses tier-3.
- [x] ~~**Push renderer commits `d44ef23` + `4b49660` from Mac terminal**~~ — ✅ done 2026-05-21 (Session 67 session-open). Kanwar confirmed push at session start: `d44ef23..4b49660  main -> main`. Both tier-3 Storage-fetch fallback + `publish-one.mjs` now on `origin/main`.
- [ ] **Sushant — Bearer auth on `/api/publish/[id]`** — Session 62 wired auto-extract into the publish endpoint but left bearer auth OFF (cleanup pane is same-origin without auth today; adding auth would break it). Once the Phase 2 OSCRSJ admin Publish button ships (separate `^handoff-sushant-html-body-editor-phase-2` or earlier), tighten `/api/publish/[id]` to require `Bearer ${RENDERER_SHARED_SECRET}` matching the preview endpoint's pattern. Update the cleanup pane fetcher in `CleanupPane.tsx` to attach the bearer header (server-rendered page passes it as a prop into `PublishProgress` to avoid leaking secret to client bundle). Without this, anyone with the Tailscale Funnel URL + a valid manuscript UUID can trigger a real publish, overwriting `published_pdf_storage_path` + uploading arbitrary content. ~30 min Sushant fix. Surfaced Session 62
- [ ] **Codify "Sandbox npm ENOSPC pivot" convention §3** — Surfaced Session 62 when `npm install node-stream-zip` errored with `ENOSPC: no space left on device` mid-build. Don't burn cycles retrying; pivot immediately to pure-Node or already-installed alternatives. Suggested wording: "When sandbox `npm install` errors with `ENOSPC` (`/sessions/peaceful-hopeful-ptolemy/.npm` full), do NOT retry — disk-space failures don't self-resolve mid-session. Pivot same-turn to: (a) pure-Node implementation if the dep's scope is narrow (zip readers, hash functions, regex parsers); (b) already-installed alternatives (`grep` the repo's `package-lock.json` for similar modules); (c) skip the optimization entirely and proceed with a slower but working code path. Communicate the pivot decision to the user inline; don't hide it."
- [ ] **Codify "Pure-JS smoke port substitute when sandbox npm broken" convention §3** — Surfaced Session 62. When sandbox-side `tsx` / `ts-node` / `vitest` is unavailable due to npm install failures, writing a tight pure-JS port of the same logic (`.mjs`, no TS) and exercising it against real fixtures captures equivalent algorithmic behavior. ~10 min to write a port for any single-module algorithm vs ~5 min wasted retrying npm install. Suggested wording: "When end-to-end TS-runner smoke (tsx, ts-node, vitest) is blocked from sandbox, write a pure-JS port of the algorithm into `.mjs` form and exercise against real fixtures. The port stays in `/tmp` (not committed); the original TS is the canonical source. Validates algorithmic behavior on real data without transpilation."
- [ ] **Codify "FUSE stale-lock proactive cleanup" convention §3** — Surfaced Session 62 (third recurrence of the same FUSE pattern after Sessions 32 + 59). Proactive `mv .git/*.lock .git/.stale-junk/ 2>/dev/null` BEFORE the first `git add` / `git commit` saves one round-trip every time. Suggested wording: "Before any `git add` / `git commit` from sandbox, run `mkdir -p .git/.stale-junk && mv .git/*.lock .git/refs/heads/*.lock .git/.stale-junk/ 2>/dev/null` defensively. The `mv` is a no-op when no lock file exists. FUSE mount inhibits `rm` but allows `mv`; the proactive sweep avoids the lock-file-blocks-commit retry cycle Sessions 32 + 59 + 62 all hit."
- [ ] **Codify "Session-number collision pre-check" convention §3** — Surfaced Session 62. When multiple Cowork sessions run on the same day, the second-to-wrap can inherit the wrong session-number if it doesn't check what's already landed. Session 62 caught a collision mid-wrap-up because I had numbered the commit + narrative as Session 61, only to discover during M3 that a Franklin Cowork session earlier today had already claimed Session 61. The amend + narrative-rename cost ~5 minutes. Suggested wording: "Before writing the M1 narrative or committing with a session-number reference, run `grep '^### Session NN' docs/session-history.md | head -3` to verify the next-available session-number. The chronological-by-date assumption breaks when multiple sessions run on the same calendar day."
- [ ] **Visual spot-check Session 63 surfaces post-Vercel-deploy** (~60s after `dfd5bd0` push) — load `https://www.oscrsj.com/scholars` and verify each new structural change: (a) **"Why we created this program"** section renders three paragraphs with the linked NRMP source URL (`https://www.nrmp.org/wp-content/uploads/2026/03/Advance-Data-Tables-2026_Public.pdf`) opening in a new tab — verify the link styling matches the brown hover-underline pattern; (b) **"Who should apply"** section renders nested `<ol>` with brown decimal markers + `<ul>` with brown disc markers; pre-medical group renders as single paragraph (no nested `<ul>`), medical-student + IMG groups render with sub-bullets; copy is professional and matches the spec wording; (c) **WHAT_YOU_GET** grid shows exactly 3 cards (no "A real peer-reviewed journal" card) — verify the 2-column grid balance at `sm+` breakpoints (3 cards in a 2-col grid leaves one card on its own row; layout should not look broken); (d) **Tracks section** heading reads "Three tracks" (no "one cohort"), 3 track cards (Pre-Med / Med Student / IMG) still render unchanged; (e) the prior **"Publication is conditional on peer review"** section is fully absent — no orphan label, heading, or paragraph; (f) responsive layout at narrow mobile / tablet / desktop — verify nested-list indentation doesn't overflow on mobile + section spacing reads correctly. Surfaced Session 63
- [ ] **Codify "Design-token Tailwind variant compliance" convention §3** — Session 63 caught a `marker:text-tan` Tailwind variant during pre-commit review before tsc/commit. Neutral Elegance v2.3 bans `tan` from text classes (fails WCAG AA on cream). The rule extends to Tailwind's `marker:` variant by analogy (markers are decorative-text-adjacent but still text classes), but the §7 Design System table doesn't explicitly state which Tailwind variants the ban covers. Suggested wording: "Design-token text/background bans extend to ALL Tailwind variants that style text or decorative-text properties — `text-X`, `placeholder:text-X`, `marker:text-X`, `selection:text-X`, `caret-X`, `decoration-X`, `accent-X`, hover/focus/disabled state variants of all the above. Before committing any styling change, grep for the banned token across all such variants in changed files." Surfaced Session 63
- [ ] **NRMP source URL re-verify in 12-18 months** — `https://www.nrmp.org/wp-content/uploads/2026/03/Advance-Data-Tables-2026_Public.pdf` referenced from `/scholars` "Why we created this program" section. NRMP rotates `/wp-content/uploads/YYYY/MM/` URLs at year-end; if the 2026 link 404s by mid-2027, replace with the Wayback Machine snapshot OR the then-current-year Advance Data Tables URL (refresh the analysis to match new numbers if URL rotates). Low priority; calendar reminder for 2027 Q3. Surfaced Session 63

### 🏆 Post-publication (Session 62 publication arc)

- [ ] **Confirm verapdf real result post re-publish** — after the Session 69 re-publish, check the new `render-report.json` in Supabase Storage (`submissions/published/{manuscriptId}/render-report.json`) and verify `verapdf.result` is now `"pass"` (not a false positive). If it reads `"fail"`, the stored PDF itself fails PDF/A-1b and WeasyPrint's PDF/A output needs investigation. This is the first real verapdf determination since the C3 parser fix landed. Surfaced Session 69
- [ ] **Sushant — C2 pipeline fix: deduplicate patient consent sections in JATS** — Session 68 (C2) resolved the editorial issue for e0001 by updating the metadata variant. But the underlying pipeline bug remains: when a manuscript's `.docx` body includes a Patient Consent section AND the metadata editor also has a consent variant selected, the JATS synthesizer emits both body-extracted `<sec id="patient-consent">` AND back-matter declaration `<sec sec-type="patient-consent">` — contradictory or duplicate. Fix: in `lib/publish/synthesize.ts` JATS body assembly, when a non-null `patient_consent_type` is present in the metadata, suppress the body-extracted patient consent section (or vice versa — metadata declaration is authoritative). ~30 min Sushant. Surfaced Session 68
- [ ] **Verify article 1 displays correctly on public site** — open `https://www.oscrsj.com/articles/in-press` in an incognito window. Confirm: (a) the article landing card renders with title + authors + date; (b) clicking through opens the article page; (c) the published PDF (3.6 MB, 7 pages) downloads cleanly; (d) DOI placeholder text reads sensibly (real DOI doesn't exist until Crossref membership lands); (e) Google indexes the landing page within ~48-72h (check `site:oscrsj.com/articles` in 3 days). Surfaced Session 62 publication arc.
- [x] ~~**Push renderer commit `6078130` from Mac terminal**~~ — ✅ done 2026-05-21 mid-Session-70. `git rev-parse origin/main` from sandbox confirmed `8f1888b` (past `6078130`) on `origin/main`. The commit landed on origin via Kanwar's interactive push during the session.
- [⚠️] **Activate the cleanup-pane Publish button fix on Kanwar's Mac — PARTIALLY done** — production mode IS running on the Mac as of Session 70 wrap-up (`npm run build && npm start` foreground in a Terminal tab; post-fix curl returned production HTML with zero `hmr-client` / `next-devtools` / `"b":"development"` markers). BUT: (a) running foreground means closing the Terminal tab or rebooting kills the renderer — Kanwar reruns the command when needed; (b) the launchd plist is in an unloaded state and won't auto-start on next boot until the launchd-permission issue (see next follow-up) is fixed. Verification still pending: Kanwar to test the Publish button on the next manuscript publish — click "Publish manuscript…" should open the confirmation modal; checking the confirmation box should start streaming NDJSON events through the chain. Surfaced 2026-05-21, partial-resolution 2026-05-21
- [ ] **Sushant — Diagnose and fix the launchd-start.sh `Operation not permitted` issue** — when Session 70's `launchctl load` fired the plist, the wrapper script `/Users/parhar/Documents/oscrsj-renderer/scripts/launchd-start.sh` failed with `/bin/bash: ... launchd-start.sh: Operation not permitted` plus `shell-init: error retrieving current directory: getcwd: cannot access parent directories: Operation not permitted` repeated dozens of times in a KeepAlive restart loop. Manual `bash scripts/launchd-start.sh` from Terminal worked fine. Hypothesis: the file was created via Cowork sandbox FUSE mount (owner `vibrant-clever-rubin:vibrant-clever-rubin` at the sandbox-FS level) and macOS Sequoia/Tahoe's stricter Documents-folder TCC restrictions on launchd's exec context refuse to execute. Three plausible fix paths to investigate: (a) **Copy the script outside the FUSE mount** — `cp ~/Documents/oscrsj-renderer/scripts/launchd-start.sh ~/bin/oscrsj-renderer-start.sh` + update the plist `ProgramArguments` to point at `~/bin/...` (keeps the wrapper invariant outside FUSE; sandbox sessions edit a *template* version that's manually re-copied when changed — annoying); (b) **Grant launchd Full Disk Access** via System Settings → Privacy & Security → Full Disk Access → add `/bin/bash` or `/sbin/launchd` (overkill but may work); (c) **Inline-bash in plist** — replace `ProgramArguments` with `["/bin/bash", "-c", "cd ~/Documents/oscrsj-renderer && exec /bin/bash scripts/launchd-start.sh"]` (forces a fresh bash invocation with explicit working directory; sometimes sidesteps the getcwd error). Investigation should start by `tail ~/Library/Logs/oscrsj-renderer/stderr.log` post-failure and `xattr -l scripts/launchd-start.sh` + `stat -f '%Mp%Lp %Su:%Sg' scripts/launchd-start.sh` to verify the file has no quarantine xattr and reasonable permissions on the macOS side (not just the sandbox view). Surfaced Session 70
- [ ] **Visual spot-check the dev-mode warning banner** — temporarily run `npm run dev -p 3001` from the renderer directory + reload `/render/<some-id>`; verify the red `border-red-300 bg-red-50` banner renders above the existing payload-warnings banner with the launchctl reload command + the publish-one.mjs workaround. Kill the dev server (`pkill -9 -f 'next dev'`) when done and restart production (`npm run build && npm start` until launchd permission issue is fixed). Surfaced 2026-05-21
- [ ] **Test cleanup-pane Publish button on next manuscript publish** — Session 70's fix is theoretically complete (production mode running, button code unchanged) but unverified end-to-end. On next manuscript publish: load `https://macbook-pro-5.tail55b3cd.ts.net/render/<some-id>`, click "Publish manuscript…", confirm modal opens, check the confirmation box, confirm NDJSON streams through the chain. If button STILL doesn't work in production mode, dig further — production-mode bundle may have its own Funnel issue (unlikely but possible). Surfaced Session 70
- [ ] **Codify "curl + grep `"b":"development"` is the first diagnostic for any Funnel-served UI button bug" convention §3** — Session 70 saved hours of speculative debugging by curling the URL and finding `"b":"development"` in 30 seconds. The CLAUDE.md follow-up's hypothesis blamed React Strict Mode + Turbopack file-confusion; the actual answer was visible in one HTTP response. Suggested wording: "When any UI button on a Funnel-served or Vercel-served Next.js page mysteriously stops responding (modal doesn't open, click handler silent, but page otherwise renders), the FIRST diagnostic step is `curl -s <url> | grep -oE '\"b\":\"production\"|\"b\":\"development\"|hmr-client|next-devtools|turbopack'`. A non-empty `hmr-client` / `next-devtools` result + `\"b\":\"development\"` marker indicates dev mode behind a WSS-incapable proxy (Tailscale Funnel, ngrok free tier, some Cloudflare configs) — the broken HMR handshake prevents selective event-handler attachment. The fix is always 'run production mode,' not 'restart dev server.' Speculating about Strict Mode / Turbopack file-confusion / hydration mismatches BEFORE running this curl wastes hours." Surfaced Session 70
- [ ] **Codify "Convention §6 extension: working-tree files that are USED but not TRACKED are an under-noticed bug class"** — `scripts/launchd-start.sh` was created Session 58 (used by the launchd plist daily) BUT never tracked in git. Anyone cloning the repo fresh would have a broken launchd plist (file missing) — silent fail. Convention §6 covers staged-index orphans and working-tree-modification orphans; should extend to the "exists on disk + actively referenced + untracked" case. Suggested wording: "At session wrap-up, run `git status --short | grep '^??'` and inspect any `?? scripts/` / `?? config/` / `?? bin/` entries — files in those directories are usually meant to be tracked. If an untracked file in those locations is referenced by any committed code (grep the repo for its name), it MUST be tracked before session-end." Surfaced Session 70
- [ ] **Codify "Mac-terminal command blocks: strip comments before pasting OR use column-zero `#`"** — Session 70's first paste-block (with `# 1. Push the renderer fix`, `# 2. Evict the stuck dev server`, etc.) showed zsh interpreting each comment line as `command not found: #` (mostly noise but the `~30s` expansion was a real bug — zsh treats `~30s` as user-home expansion → "no such user or named directory: 30s"). zsh DOES treat `#` at column zero as a comment, but comments-with-leading-whitespace are foot-guns. Suggested wording: "When delivering a Mac-terminal command block for the user to paste: either (a) strip all comment lines so only executable commands remain (preferred — cleaner output); or (b) ensure every `#` is at column zero with no leading whitespace, and avoid any `~` followed by non-alphanumeric (no `~30s`, no `~5min`, etc. — zsh tries to expand them). Test the block by pasting into a sandbox terminal once before delivering." Surfaced Session 70
- [ ] **Tell people about article 1.** First publication is a marketing moment that compounds. Suggested fan-out: (a) LinkedIn post from Kanwar's personal account linking to the article + tagging Madhan + co-authors; (b) email blast to all 21 editorial board members announcing the first publication with the article URL; (c) trigger Arjun's Project Social Media lane — first cross-platform post on Instagram + LinkedIn + X; (d) update homepage "Latest article" section if not already wired; (e) optional: short post in the AI-in-Ortho hub linking the first case report. Surfaced Session 62 publication arc.
- [x] ~~**Diagnose cleanup pane Publish button UI bug.**~~ — ✅ diagnosed + fix-committed 2026-05-21 (Sushant Cowork). Root cause was NOT React Strict Mode / Turbopack confusion — it was the renderer running `next dev` instead of `next start`. Confirmed by curling `https://macbook-pro-5.tail55b3cd.ts.net/` and seeing `"b":"development"` in the HTML response + `[turbopack]_browser_dev_hmr-client_hmr-client_ts` preload script. Tailscale Funnel cannot proxy WebSocket (WSS) connections, so the HMR client fails to handshake, and in Next.js 16 / React 19 the next-devtools overlay + Turbopack HMR pipeline fail to attach React event handlers to some client components. The textarea + preview iframe stayed working (their handlers attached) but the Publish button's `onClick` did not. Why dev mode despite the plist's `npm start`: a manual `npm run dev` instance was winning the port-3001 race, blocking the plist's `npm start` from binding (noted in Session 64 wrap-up). Renderer commit `6078130` on local `main` (NEEDS PUSH from Mac terminal — see next follow-up) lands two fixes: (1) `scripts/launchd-start.sh` (was created Session 58 but never committed) now kills any PID listening on port 3001 + `pkill -9 -f 'next dev'` before starting production, so the plist always wins; (2) `CleanupPane.tsx` shows a red dev-mode banner with the launchctl reload command if `NODE_ENV !== 'production'` so future regressions are diagnosable at-a-glance. Closes the Session 62 publication-arc follow-up.
- [x] ~~**Renderer payload synthesizer — wire `/api/publish/[id]` end-to-end**~~ — ✅ done 2026-05-19 Session 62 publication arc. Three commits `fb914ae` + `d44ef23` + `4b49660` together produce a fully operational publish path: synthesizer → payload GET → renderer POST → 3-tier figure resolution → PDF/A-1b + JATS → Storage upload → manuscripts row writeback. First article published 2026-05-19.
- [x] ~~**First manuscript reaches `accepted` → wire payload synthesizer**~~ — ✅ done Session 54 (synthesizer); end-to-end-verified Session 62 publication arc against `f8596ce8` "Bilateral acute exertional compartment syndrome of the forearms in a sport climber: a case report".
- [x] ~~**Janine — Compliance review of first published artifact**~~ — ✅ done 2026-05-21 (Session 68). All 3 🔴 critical items resolved (Sessions 68–69). Article re-published 2026-05-21 with C1+C2+C3 fixes. Open: 🟡 A1 author ORCIDs, A2 DTD validation (escalated priority), A3 placeholder DOI. Full audit note at vault `02 - OSCRSJ/Indexing/Manuscripts/0001 — OSCRSJ-2026-0001 [Case Report]/Janine First-Publish Compliance Audit.md`.
- [ ] **Manvir — Publish-pipeline-operational confirmation** (`^handoff-manvir-publish-pipeline-operational-2026-05-16`, filed Session 57) — **TRIGGER FIRED 2026-05-19** by article 1 landing. Cross-domain confirmation: update CEO Dashboard Active Workstreams (Submission Portal moves toward Recently Completed; new Operational Rhythm workstream activates), surface the milestone in next briefing, draft post-publication operational checklist (review cadence, monthly issue assembly, manuscript pipeline tracking).
- [ ] **ISSN application path now active** — Library of Congress requires 5 published OA articles before assigning ISSN (per Janine Agent.md line 79). 1/5 complete as of 2026-05-19. Track each subsequent publication; once at 5, Janine drives the ISSN application. ~6 months at 1 article/month cadence; faster if invited SR/MA + Track C blasts produce real submissions. ISSN Application Prep vault note created Session 68 at `02 - OSCRSJ/Indexing/ISSN/ISSN Application Prep.md`. Surfaced Session 62 publication arc.
- [ ] **Crossref membership decision now meaningful** — DOI registration becomes the canonical post-publish workflow once Crossref membership is in place. Brad: ~$275/yr membership fee + per-DOI registration costs. Article 1 already has a DOI placeholder slot in the PDF; once Crossref lands, retroactively register the existing DOI for `e0001` + automate for future articles. Brad-territory; gated on LLC formation. Surfaced Session 62 publication arc.
- [ ] **Operational rhythm — define publication cadence target** — Manvir-territory. With article 1 published, decide: (a) target rate (monthly issue / rolling continuous publication / quarterly?); (b) review SLA (target X days from submission to decision); (c) issue assembly process when article count clears 3-5 per issue; (d) editor-in-chief workflow integration with Madhan. Surfaced Session 62 publication arc.

### Session 64 (Phase 2 HTML body editor MVP — 2026-05-19)

- [x] ~~**Run migration 024 in Supabase Studio**~~ — ✅ done 2026-05-19 same-session (Kanwar confirmed `Success. No rows returned.` in Studio SQL editor). `manuscripts.manuscript_body_cleaned_html TEXT NULL` is live in prod + PostgREST schema cache refreshed via terminal NOTIFY. BodyEditor save action unblocked.
- [ ] **Visual spot-check Session 64 surface post-Vercel-deploy** (~60s after `a01ef4e`) — load https://www.oscrsj.com/dashboard/admin/manuscripts/f8596ce8-2424-4a47-8537-5af9e8a2f5cd; Cmd+Shift+R; verify (a) "Body HTML editor" panel renders between "Pre-render metadata editor" and "Publish pipeline" panels; (b) toolbar shows 5 button groups (text formatting / heading / list+quote+code / link+image / undo+redo); (c) empty-state amber notice reads "Auto-extraction is active. No saved HTML override..."; (d) typing into the editor enables the Save button (isDirty); (e) Save persists (will FAIL until migration 024 runs!); (f) Discard reverts to last saved value; (g) Clear-saved-HTML button only appears when something is saved; (h) Coexistence notes `<details>` block expands cleanly. Surfaced Session 64
- [ ] **First-edit smoke test against f8596ce8** — once migration 024 is live: type minor body HTML edit (e.g., one paragraph rewrite or italic word), click Save, then click "Open preview ↗" in the metadata-editor panel — verify the rendered PDF preview uses the editor-curated HTML, not the renderer's auto-extract output. Then click "Clear saved HTML (revert to auto-extract)" and re-open preview to verify the fallback path still produces the Session 62 + 60 result. Surfaced Session 64
- [ ] **Sushant — Wire renderer cleanup pane to seed textarea from `payload.manuscriptBodyCleanedHtml`** — `^handoff-sushant-renderer-cleanup-pane-seed-from-cleaned-html-2026-05-19`. The OSCRSJ payload endpoint `/api/publish/payload/[id]` now carries `manuscriptBodyCleanedHtml: string | null` (Session 64). The renderer-side cleanup pane at `~/Documents/oscrsj-renderer/app/render/[id]/page.tsx` (or wherever its textarea is server-mounted) should read this field and pre-fill the textarea when non-null. ~10 LoC: `<textarea defaultValue={payload.manuscriptBodyCleanedHtml ?? extractedFallback}>`. Without this, admins who curate HTML in the OSCRSJ admin BodyEditor still need to manually copy-paste into the renderer cleanup pane before clicking Publish. ~10-min Sushant fix. Surfaced Session 64
- [ ] **Sushant — Phase 2 figure drag-to-reposition** — deferred from Phase 2 MVP per Session 64 scope decision. TipTap has `@tiptap/extension-drag-handle` + figure-anchor APIs that let editors drag a figure inline to the position where it's first referenced in body text. Pattern: render figure blocks with TipTap node-views, attach drag handle, on-drop reflow paragraph order. Defers until Kanwar uses MVP and finds the gap. ~2-3h Sushant session if scope holds. Surfaced Session 64
- [ ] **Sushant — Phase 2 native table cell editing** — TipTap StarterKit does NOT include the table extension family (`@tiptap/extension-table` + `@tiptap/extension-table-row` + `@tiptap/extension-table-cell` + `@tiptap/extension-table-header`). MVP renders tables read-only (the renderer's extractBody emit and any paste-from-cleanup-pane preserve `<table>` HTML; users can't edit cell content in-editor). Adding the extensions enables click-to-edit cells, add/delete row/col, merge/split, etc. ~1-2h Sushant session including UI for the table commands in the toolbar. Surfaced Session 64
- [ ] **Sushant — Phase 2 auto-seed editor from accepted .docx** — when the OSCRSJ admin first opens a manuscript with `manuscript_body_cleaned_html IS NULL`, optionally call a new renderer endpoint that returns Pandoc-rendered body HTML and seed the BodyEditor with it. Today's MVP mounts empty in that case — user must paste from `/render/[id]` or type. Auto-seed gives the editor a one-click "Load body from .docx" button that fetches the Pandoc HTML output (similar to `/api/extract-metadata/[id]` but returning raw HTML, not structured fields). Pairs with retiring `/render/[id]` long-term. ~2h Sushant session including new renderer endpoint + button + loading state. Surfaced Session 64
- [ ] **Sushant — Eventually retire renderer-side `/render/[id]` cleanup pane** — once Phase 2 has 2-3 publishes of production confidence via the OSCRSJ admin BodyEditor and the auto-seed feature lands, redirect `/render/[id]` to `/dashboard/admin/manuscripts/[id]` (or remove the route entirely). Single canonical body-edit surface. Locked behind explicit Kanwar decision after observing real workflow. Surfaced Session 64
- [ ] **Codify "Sandbox npm cache redirect for `/sessions` full disk" convention §3** — Sessions 62 + 64 both hit `/sessions` filesystem at 100% disk during `npm install`. The pivot is `--cache /tmp/npm-cache` against root (which has free space). Suggested wording: "When sandbox `/sessions` is full (`df -h /sessions` shows 100%) and `npm install` errors with `ENOSPC`, pivot the npm cache to `/tmp/npm-cache` via `npm install --cache /tmp/npm-cache <pkg>`. Don't `npm cache clean` against the full mount; the operation can't free enough space and adds round-trip cost. Communicate the pivot to the user inline." Surfaced Session 64
- [ ] **Codify "Forward-compatible cross-repo API additions" convention §3** — Session 64 extended `/api/publish/payload/[id]` with an additive `manuscriptBodyCleanedHtml` field. Older renderers that ignore it produce unchanged output; the renderer-side opt-in lands in a small follow-up. This decouples cross-repo ship cadence (OSCRSJ ships fully ready, renderer follows separately). Suggested wording: "When extending a cross-repo API (renderer ⇄ OSCRSJ, future webhook contracts, etc.) with a new field, prefer forward-compatible addition (optional / nullable, ignored by consumers that don't know about it) over breaking change. Ship the producer first, file a follow-up for the consumer. Avoids the lockstep-deploy-trap of needing both repos to land simultaneously." Surfaced Session 64
- [ ] **Cross-session orphan — 2 Franklin commits not captured in CLAUDE.md §9** — commits `2209886` (IMG tier card + 5 copy tweaks across scholar tracks, 2026-05-19) + `8de63c8` (remove AI-use policy acknowledgment, 2026-05-19) landed on `main` after Session 63's `dfd5bd0` but never had their own M1-M4 wrap-up. Franklin's next Cowork session should backfill their narratives into docs/session-history.md (as Sessions 63.5 and 63.7, or roll them into a single "Session 65 retroactive" entry). Carried to next Franklin session. Surfaced Session 64
- [ ] **Dependency hygiene — sanitize-html → htmlparser2 vulnerability cluster** — TipTap + sanitize-html install (Session 64) added 187 transitive deps including 10 reported vulnerabilities (2 moderate, 7 high, 1 critical) primarily in the `htmlparser2` chain under sanitize-html. `npm audit fix` may break the install; `npm audit fix --force` may bump sanitize-html to a major version that changes the API. Focused Sushant session: review CVE details, decide whether to upgrade sanitize-html major or live with the warnings until next quarterly hygiene pass. Not urgent — sanitize-html itself runs server-side only against trusted admin input, so the immediate attack surface is narrow. ~30 min Sushant work. Surfaced Session 64

### Session 65 (Desk reject promoted to primary radio — 2026-05-20)

- [ ] **Visual spot-check Session 65 surfaces post-Vercel-deploy** (~60s after `d09e541`) — (a) load any admin manuscript page where status is `submitted` AND `reviewCount === 0` (e.g., a freshly-submitted manuscript). Verify: (i) the decision-type fieldset shows 5 radio options laid out as 2-col grid for the first four (Accept / Minor Revisions / Major Revisions / Reject (post-review)) + full-width red-tinted card on its own row for "Reject (without external review)"; (ii) selecting the desk_reject radio swaps the red informational banner above the fieldset summarizing "this returns the manuscript without external peer review..."; (iii) the Status-transition line updates to `submitted → desk rejected`; (iv) the Submit button restyles red and reads "Submit desk reject"; (v) clicking Submit opens the safety confirmation modal; (vi) the modal's "Continue and send" button fires the actual transition. (b) Load a manuscript currently in `under_review` status with ≥1 submitted review. Verify: (i) the desk_reject radio still renders on the row but greyed out at `opacity-60` with `cursor-not-allowed`; (ii) the inline italic marker "(available only before peer review)" appears in the status-target line; (iii) hovering the card shows the tooltip "Available only before peer review (status 'submitted' with no submitted reviews)"; (iv) attempting to click the radio is a no-op. (c) Confirm the small red link-style "Desk reject without review" button at the bottom of the panel is gone. (d) Confirm the per-decision hint under "Reject (post-review)" pointing at the old button is gone. Surfaced Session 65
- [ ] **End-to-end smoke test the new desk-reject flow** — pick a low-stakes test manuscript in `submitted` status with 0 reviews. From admin manuscript page: (a) select the "Reject (without external review)" radio; (b) click "Load template" to populate the textarea with the new template prose; (c) replace the `[bracket placeholder]` with a few sentences of actionable feedback (scope concern + specific formatting fix the authors should make for a hypothetical resubmission elsewhere); (d) tick the confirmation checkbox; (e) click "Submit desk reject"; (f) verify the safety modal opens with the refreshed copy referencing the 15-minute rescind window; (g) click "Continue and send"; (h) verify the success flash + page refresh; (i) verify the corresponding-author email lands at the test address with the `isDeskReject` framing (subject "[OSCRSJ] Decision on your submission... Not Accepted", heading "Decision: Not Accepted (Editorial Review)", lead paragraph mentions "will not proceed to external peer review"); (j) verify the manuscript card now shows status `desk_rejected` + the decision appears in the manuscript-history panel; (k) within the 15-minute rescind window, verify the "Undo decision" banner offers rescission. Surfaced Session 65
- [ ] **Decide whether to promote desk-reject in the `BulkDecisionModal` too** — `app/dashboard/admin/manuscripts/BulkDecisionModal.tsx` carries its own decision picker for batch editorial actions across multiple manuscripts at once. Session 65 only updated the single-manuscript composer. Open question for Manvir: should bulk desk-reject be a thing? Arguments for: an editor reviewing 5 submissions in a batch can clear scope-mismatched ones quickly. Arguments against: desk-reject is the most editorial-discretion-heavy decision in the workflow and each one warrants a manuscript-specific letter (boilerplate desk rejects look careless). Recommended posture: leave bulk desk-reject OFF, force editors into the one-at-a-time composer for each desk reject, but explicit Kanwar/Manvir decision needed. ~10-min Franklin or Sushant edit either way once decided. Surfaced Session 65
- [ ] **Codify "Built-but-hidden UX gap — grep before scoping" convention §3** — Session 65's pre-flight diagnosed within 2 grep calls that desk-reject already existed end-to-end and only needed UX surfacing. If I'd scoped a new build instead, the session would have taken ~3 hours instead of ~40 minutes and produced a parallel duplicate of existing infrastructure. Suggested wording: "When a feature ask sounds like 'we need to create X', grep the codebase for X and near-synonyms BEFORE scoping the work. Built-but-hidden is a common UX gap class — capability exists in the data model and server actions, but the UI affordance is missing or visually subordinate. A 60-second `grep -i 'X|near-synonym' lib/ app/` saves hours of duplicate scoping. Always probe the existing surface and pre-flight via AskUserQuestion: 'did you know X exists at <location>? where's the actual gap?' The user's response is the diagnostic." Surfaced Session 65
- [ ] **Codify "Standalone constants that mirror Record shape are a code-smell" convention §3** — Session 65 folded `DESK_REJECT_TEMPLATE` (standalone const) into `TEMPLATES.desk_reject` (Record entry) once desk_reject became a peer decision type. The standalone constant was a leftover from when desk_reject was structurally not-a-decision-type (mode-toggled with its own branch). Suggested wording: "When a standalone constant has the same shape as entries in a parallel Record / Map / enum and exists in the same file, it's often a code-smell signaling that the abstraction will eventually collapse. The standalone form is appropriate when the entry is structurally distinct (different shape, different consumer, different lifecycle). When the entry becomes structurally identical to its peers (same shape, same consumer, same flow), folding into the Record removes the asymmetry. Watch for this when promoting an exceptional case to a peer of the routine cases." Surfaced Session 65

### Session 66 (Narrative Review site copy sweep — 2026-05-20)

- [ ] **Visual spot-check Session 66 surfaces post-Vercel-deploy** (~60s after `d1207bc`) — verify across 4 pages at 3 breakpoints (narrow mobile / tablet / desktop): (a) `/aims-scope` — lead `<p>` reads "OSCRSJ publishes seven article types"; 7-bullet `<ul>` with Narrative Review at position 4 between SR/MA and Surgical Technique; bullet copy starts with "Narrative Reviews: expert syntheses of a defined orthopedic topic..."; metadata + openGraph descriptions enumerate narrative reviews (view-source check). (b) `/guide-for-authors` — Comparison Table at top has **7 columns** (was 6); on narrow mobile the table wraps horizontally inside `overflow-x-auto -mx-4 px-4` without breaking the cream-background card around it; column-header `<a href="#narrative-review">` jump link works; new Narrative Review detail section rendered between SR/MA and Surgical Technique with 9 specs in 2-column grid + 15-item required-sections ordered list + SANRA checklist card (download button will 404 — that's expected); ethics section last bullet mentions SANRA. (c) `/templates` — Reporting Checklists grid is 1-col mobile / 2-col tablet / **4-col desktop** with new SANRA card visible after PRISMA card; intro paragraph reads ".. SANRA Self-Rating is required for every Narrative Review"; Templates by Article Type section shows Narrative Review row at position 4 with download buttons (both will 404 — expected); submissionFiles "Reporting Checklist" entry filename ends with "SANRA-self-rating.pdf"; Publication License section says "Creative Commons Attribution 4.0 International License (CC BY 4.0)" with URL `creativecommons.org/licenses/by/4.0/`. (d) `/faq` — Q1 "We accept seven article types" + Narrative Review at position 4; two new Q&As immediately after Q1 about Narrative Review + Mentored Review track; checklist Q includes SANRA; word-limit Q includes "Narrative Review: 4,000 words." Surfaced Session 66
- [ ] **Rich Results Test on `/guide-for-authors` + `/faq` FAQ JSON-LD** post-deploy at [search.google.com/test/rich-results](https://search.google.com/test/rich-results). The new `/guide-for-authors` Q4 ("What is a Narrative Review at OSCRSJ?") and the two new `/faq` Q&As ("What is a Narrative Review at OSCRSJ?" + "What is the Mentored Review track?") should validate clean as FAQPage entities. Worth running both URLs because FAQ JSON-LD has historically been sensitive to quote-escape bugs and an unclosed string would silently invalidate the entire schema block. Surfaced Session 66
- [ ] **Sushant — Bundle homepage `app/page.tsx` TYPE_LABELS entry into migration 025 commit** — `^handoff-sushant-narrative-review-launch-2026-05-20` already owns "10+ TYPE_LABELS surface updates" + the `ManuscriptType` union extension. Franklin's handoff (item 4) spec'd the homepage `TYPE_LABELS` entry as a Franklin deliverable, but adding the key without extending the union breaks tsc, and extending the union without the matching migration either breaks ~12 other `Record<ManuscriptType, ...>` consumers OR ships a type union drifted from the Postgres enum. Real handoff over-spec; Session 66 deferred the 1-line addition to Sushant's commit. **What to do:** when running migration 025, also add `narrative_review: 'Narrative Review',` to `app/page.tsx` `TYPE_LABELS` (between `letter_to_editor` and `review_article` for alphabetical-ish placement, or wherever the surface sweep finds the homepage consumer). Tree-wide grep `Record<ManuscriptType` will surface every consumer needing the same treatment. Surfaced Session 66
- [ ] **Janine — SANRA self-rating PDF (`^handoff-janine-narrative-review-launch-2026-05-20`) is the blocker for 1 of 3 expected 404s post-deploy** — Session 66 shipped the linking surfaces but the download target `/downloads/oscrsj-sanra-self-rating.pdf` doesn't exist yet. Two surfaces will 404: (a) `/guide-for-authors` → `articleTypes[3].checklist.downloadHref`; (b) `/templates` → new SANRA reporting-checklists card. Both surfaces clear when Janine's PDF lands at `public/downloads/oscrsj-sanra-self-rating.pdf`. No code change needed on flip — just file existence at the URL path. Surfaced Session 66
- [ ] **Franklin — Build `.docx` template + worked example for Narrative Review (Phase 2 of `^handoff-franklin-narrative-review-launch-2026-05-20`)** — Session 66 spec'd `templateFile: 'oscrsj-template-narrative-review.docx'` + `exampleFile: 'oscrsj-example-narrative-review.docx'` on the `/templates` page; both download links will 404 until built. Required work: (a) update `scripts/build-article-templates.py` to add a `narrative_review` block mirroring the existing `review_article` block but using Narrative Review structure — Scope and Search instead of Methods, Synthesis and Clinical Implications instead of Discussion, Limitations standalone subsection, Author Track Declaration block, AI Tools Used block; (b) update `scripts/build-article-examples.py` similarly with a worked example (clinical topic TBD; coordinate with Janine for clinical authenticity); (c) run both scripts; (d) commit the regenerated `.docx` files via explicit-path stage. Estimated ~1-2 Cowork sessions (build + Janine topic coordination). Surfaced Session 66
- [ ] **Codify "Type-system check before constant-table edits" convention §3** — Session 66 caught a cross-agent over-spec at pre-flight by reading the type signature of `TYPE_LABELS: Record<ManuscriptType, string>` BEFORE editing. The 1-line "add a key" looked simple until the type system revealed the dependency on the union extension (which lives in another agent's lane). Suggested wording: "Before adding a key to any `Record<UnionType, ...>` constant (or extending an enum-driven exhaustive Map / Switch), grep for the union definition and verify whether extending the union is in your agent lane. If the union extension belongs to another agent's migration commit, defer the constant entry to that commit; don't ship a half-migration. The grep takes 30 seconds and surfaces every consumer that would also need treatment." Surfaced Session 66
- [ ] **Codify "Handoff over-spec detection via cross-agent grep" convention §3** — Session 66 surfaced that Manvir-authored handoffs filed same session can double-spec the same file across agent lanes (both Franklin and Sushant were assigned `app/page.tsx` TYPE_LABELS). Suggested wording: "At scope-read time, for any file you're about to edit, grep the other agents' `Inbound Handoffs` blocks for references to that file (`grep -l '<path>' 02\ -\ OSCRSJ/Resources/Agents/*.md`). Catches double-spec before duplicated work or merge conflict. If two agents claim the same file, the agent whose lane owns the underlying invariant (DB schema, type union, migration) wins; the other defers and files a §11 follow-up referencing the owner's handoff anchor." Surfaced Session 66
- [ ] **Codify "Commit messages for partial-spec sessions name every deferral" convention §3** — Session 66 named 3 explicit deferrals in the commit body (app/page.tsx TYPE_LABELS to Sushant + .docx template to follow-up + .docx worked example to follow-up). Future grep over git history will surface what wasn't done and why. Suggested wording: "When a Cowork session ships only part of an inbound handoff's spec, the commit message body MUST name every deferred deliverable explicitly with (a) what wasn't done, (b) why (blocker / lane crossing / size split / explicit Kanwar decision), and (c) who owns the follow-up (agent name or handoff anchor). Closes the loop for future bisecting; mirrors the §11 follow-up entry at the git layer." Surfaced Session 66
- [ ] **DOAJ + Crossref `<article-type>` mapping audit for Narrative Review (Janine's `^handoff-janine-narrative-review-launch-2026-05-20` deliverable)** — JATS Publishing 1.3 maps both systematic reviews AND narrative reviews to `<article-type>review-article</article-type>` (no narrower vocabulary). DOAJ's metadata fields offer "Review article" as an article type but don't distinguish narrative vs systematic. Crossref's record-type vocabulary also collapses these. Janine needs to: (a) verify the JATS mapping is correct (defer to NLM JATS reference); (b) confirm DOAJ won't reject submissions tagged "Review article" when content is a narrative synthesis; (c) draft a vault note documenting the indexing-system collapse so Sushant's renderer code knows to emit `<article-type>review-article</article-type>` for both `manuscript_type='review_article'` AND `manuscript_type='narrative_review'`. Surfaced Session 66 (referenced from Janine's handoff)

### Session 67 (Full public article display pipeline — 2026-05-21)

- [ ] **Visual spot-check Session 67 surfaces post-Vercel-deploy** (~60s after `a77920d`) — verify at 3 breakpoints (narrow mobile / tablet / desktop): (a) `/articles` — the published article card shows a peach "Full Text (PDF)" button (NOT the italic "Full text available soon" span); clicking the button opens the PDF in a new tab / inline viewer; article title is a clickable link. (b) `/articles/f8596ce8-2424-4a47-8537-5af9e8a2f5cd` (article detail page) — renders with dark hero header (type chip "Case Report" + subspecialty chip, title, authors, affiliations, elocation, "DOI pending registration"), peach Download PDF button in hero, abstract body, Keywords, Declarations section, sidebar citation block + CC BY 4.0 card. (c) **Homepage** — latest article card shows either a real figure image (radiograph/MRI) OR the gradient placeholder with "Figure" label (gradient is correct if no figure files exist in Supabase for the manuscript). Title and "Read article" button both link to `/articles/[id]`. (d) `/articles/in-press` — should show "No Articles in Press Yet" for now (first article is `published`, not `accepted`/`in_production`); this page will auto-populate for the NEXT accepted manuscript. Surfaced Session 67
- [ ] **Audit `manuscript_files` in Supabase Studio for figure availability** — run `SELECT id, file_type, file_order, storage_path, original_filename FROM manuscript_files WHERE manuscript_id = 'f8596ce8-2424-4a47-8537-5af9e8a2f5cd' AND file_type = 'figure' ORDER BY file_order ASC` in Supabase Studio SQL editor. If rows exist: homepage card should display the actual figure (verify post-deploy). If no rows: gradient placeholder is expected behavior — figures were not uploaded as separate `file_type='figure'` submissions entries. To retroactively add figures: insert rows into `manuscript_files` with `file_type='figure'` pointing at the existing figure storage paths under `submissions/`. Surfaced Session 67
- [ ] **Verify in-press flow with next accepted manuscript** — when the second manuscript reaches `status='accepted'`, it should automatically appear on `/articles/in-press` (the fix changed the query from `.eq('status', 'in_production')` to `.in('status', ['accepted', 'in_production'])`). First article won't appear there because it jumped directly to `published`. Confirmation available only when a second submission clears peer review. Surfaced Session 67
- [x] ~~**John — Wire `/articles/[id]` dynamic URLs into sitemap**~~ — ✅ done 2026-05-22 (Session 71, John Cowork). `app/sitemap.ts` converted to async; Supabase Admin client queries `manuscripts` where `status='published'`, maps each to a `priority: 0.9` entry. `.returns<{ id: string; updated_at: string | null }[]>()` type override fixes TypeScript `never` inference. Try/catch falls back to static pages only if Supabase unavailable at build time. Bundled into commit `9f590f2`. Surfaced Session 67
- [ ] **Franklin — OG image for article detail pages** — currently `/articles/[id]` social sharing uses text metadata only. ~5 LoC: add `images: [{ url: \`https://www.oscrsj.com/api/articles/${id}/figure\`, width: 1200, height: 630, alt: manuscript.title }]` to `openGraph` in `generateMetadata()`. Falls back gracefully when the figure proxy 404s (no figure uploaded). Surfaced Session 67; priority re-raised Session 71 discoverability audit as a 🟡 attention item.
- [ ] **Codify "RSC `onError` client-island extraction" convention §3** — Session 67 hit the constraint that `<img onError={...}>` and other browser-event handlers cannot live in React Server Components. The pattern is always: extract a `'use client'` island file (~30-50 LoC) that owns the state + event handler; import it into the RSC as a leaf component with only serializable props. Suggested wording: "When a server component page needs a browser event handler on an HTML element (onError, onClick, onLoad, etc.), always extract a minimal `'use client'` island. Keep the island's props serializable (string, number, boolean) — no function props, no unserializable objects. The island file is the right boundary; adding `'use client'` to the page itself converts the entire page tree to client rendering." Surfaced Session 67
- [ ] **Codify "Status spec vs workflow reality pre-flight" convention §3** — Session 67 found that `/articles/in-press` queried `status='in_production'` which was never set in the real workflow (manuscripts go `accepted` → `published` directly). A 30-second `SELECT DISTINCT status FROM manuscripts` before writing the query would have caught it. Suggested wording: "Before writing any WHERE clause that filters on a status/state column, run `SELECT DISTINCT <status_column> FROM <table>` against the production or staging DB to confirm which values actually exist. Schema enums define what's allowed; real data shows what's used. Status mismatches between spec and workflow are a silent-bug class that only surfaces when the feature is tested against real data." Surfaced Session 67

### Session 71 (Article discoverability audit — 2026-05-22)

- [ ] **Kanwar — Fix elocation_id NULL in DB** — `SELECT elocation_id FROM manuscripts WHERE id = 'f8596ce8-2424-4a47-8537-5af9e8a2f5cd'` currently returns NULL. The `citation_firstpage` tag in `generateMetadata()` conditionally includes it, so it won't emit until this is set. Fix: `UPDATE manuscripts SET elocation_id = 'e0001' WHERE id = 'f8596ce8-2424-4a47-8537-5af9e8a2f5cd'` in Supabase Studio. Once set, the next Vercel visit will re-render `generateMetadata()` with `citation_firstpage` content="e0001". ~1 minute. Surfaced Session 71
- [ ] **John — Set up Bing Webmaster Tools** — submit sitemap to Bing at https://www.bing.com/webmasters, connect via Google Search Console verification (single-click if GSC is already verified), submit `https://www.oscrsj.com/sitemap.xml`. Bing + DuckDuckGo (Bing-powered) + Yahoo (Bing-powered) are the second-largest search engine cluster. ~15 min via Chrome MCP. Surfaced Session 71
- [ ] **John — Run the 5-surface manual AI citation audit** — now that `MedicalScholarlyArticle` JSON-LD is live, re-run the 30-cell capture template at `02 - OSCRSJ/Notes/2026-05-03 John — AI Citation Audit Baseline.md` §5 (ChatGPT / Claude / Perplexity / Bing Chat / Gemini × 6 queries). Pre-JSON-LD baseline vs post-JSON-LD comparison will quantify whether AI tools are now surfacing the article. Allow 2-4 weeks post-deploy for crawl coverage before running. Surfaced Session 41; priority re-raised Session 71 as now-actionable.

---

## Where to Look

Pointers, not duplicates. Filesystem commands return current truth.

- **Session history:** `docs/session-history.md` — `grep '#session-NN-slug' docs/session-history.md` for any entry; `grep '^### Session' docs/session-history.md | head -30` for chronological scan; `grep '^### ' docs/session-history.md` for topic + section heads.
- **Routes:** `find app -name 'page.tsx'` or `tree -L 3 app/`. Active count ~64 (drift acceptable).
- **Components:** `ls components/ app/dashboard/admin/manuscripts/[id]/ app/dashboard/submit/ app/review/[token]/`.
- **Migrations:** `ls supabase/migrations/`. Migration index with shipped-by-session map at [docs/session-history.md#migration-index](docs/session-history.md).
- **Schema types:** `lib/types/database.ts`.
- **Architecture plan:** vault `02 - OSCRSJ/Projects/Submission Portal Architecture Plan.md`.
- **CLAUDE.md refactor brief + add/move rules (M1-M4):** vault `02 - OSCRSJ/Projects/CLAUDE.md Refactor.md`.
- **Renderer repo:** `~/Documents/oscrsj-renderer/`. Render chain shipped Session 18.
- **Editorial board schema:** `lib/schema/editorialBoard.ts`. 21 confirmed members as of Session 48.
- **Email templates:** `lib/email/templates/`. v2 dark-brown shell from Session 30.
- **Brand assets:** `public/brand/` (Session 15 lockup library) + `/media` Press Kit page.
- **Vault startup ritual:** read `_Meta/Organization Plan.md` then `_Meta/Vault State.md` at every session start (per user preferences).
